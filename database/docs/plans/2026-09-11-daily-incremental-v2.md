# 일일 증분 파이프라인 플랜 v2 — 당일 저녁 스코어링

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 매 거래일 19:00까지 그날 장마감 데이터로 멀티팩터 스코어를 내고 텔레그램으로 보고한다. 다음 날 아침 KRX 공식 데이터로 확정판을 만든다. 정착 후 kael-system-v3 의 20:05 체인은 종료한다.

**Architecture:** 원장은 소스별 **가장 이른 확정 시각**에 받는다 — 18:05 저녁 슬롯(키움 투자자·공매도 당일, DART 당일, WISE 스냅샷)과 아침 슬롯(06:00 대차·마스터·KIS, 07:10 외국인 보유, 08:10 KRX). 저녁 키움 행은 KRX 대조 없이 원장에 바로 넣는다(사용자 결정). stage·equity 는 저녁에 잠정판(`basis=evening`, 종가는 키움 값), 아침에 확정판(`basis=krx`)을 만든다. 스코어링은 Kael-alpha 가 equity 잠정판 위에서 돌린다.

**Tech Stack:** v1 과 동일 — Python 3.12 · sqlite3 · duckdb · bash/cron(UTC) · flock · 텔레그램. 서버 `kael-server:~/quant-ledger`.

**v1 과의 관계:** `2026-09-09-daily-incremental.md` 의 P0~P3 결과·결정 R1~R10·결정 6·7 은 그대로 유효하다. v1 의 시간표(§10)와 P4~P6 은 이 문서가 대체한다. v1 은 갱신을 멈추고 이 문서의 상태 블록이 정본이 된다.

---

## 상태 (작업이 끝날 때마다 이 블록과 체크박스를 갱신한다)

| 항목 | 값 |
|---|---|
| 최종 갱신 | 2026-09-11 12:00 — 결정 V2-1~8 반영, C.2 해석·위키, B.3 추적 규약, D.2 v3 DB 처리 등재. 관찰 2/5 통과, 페이즈 A 승인 대기 |
| 페이즈 A 저녁 원장 슬롯 | 미착수 |
| 페이즈 B 잠정·확정 빌드 | 미착수 (v1 P4·P5 재설계) |
| 페이즈 C 스코어링 | Kael-alpha 인계 완료(`~/orca/projects/Kael-alpha/handoff/2026-09-10-evening-scoring-pipeline/`) |
| 페이즈 D 컷오버 | 미착수 |
| 선행 조사 | 프로브 09-10 밤 판독: 투자자 순매수 18:56 이후 불변, 공매도 거래량 불변(누적 필드만 자정 창 이동), **대차 당일 행 20:05 확정**, **외국인 보유 당일 행 21:05 확정**(다음 날 07:05 까지 불변). 남은 것 = 16:05~18:05 판독(09-11 저녁) |

---

## 0. 근거 (2026-09-10 실측)

| 사실 | 근거 |
|---|---|
| v3 팩터 5개(모멘텀·리비전·수급·퀄리티·밸류)의 재료가 전부 우리 원장에 있다 | `reviews/2026-09-10-intake-audit-summary.md`, Kael-alpha 인계 §1~§2 |
| 키움 투자자 순매수·종가·거래량은 18:10 에 v3 가 20:05 이후 받은 값과 12축 전부 동일, 18:56~22:05 불변 | 프로브 `kw_timing.db` target=20260910, v3 `investor_detail_flows`·`daily_prices` 대조 |
| 키움 공매도 거래량은 18:10 에 있고 22:05 까지 불변 | 프로브. 잔고는 이 TR 에 없다(KRX T+2) |
| 키움 외국인 보유는 T 행이 전일 복사본, T-1 행이 다음 날 06:05~07:05 정정 | 결정 7 |
| WISE 는 하루 변화가 장중에 일어나고 저녁값 = 다음 날 06:00 값(10/10) | v3 `consensus_revision_daily` 대조 |
| DART 접수는 ~18시 마감(09-09 접수 525건 중 24% 가 16:40 이후), 늦은 접수분은 수일~16일 뒤 등장 | `dart_disclosure` 최초 수집 시각 |
| KRX 공식 시세는 T+1 08:00 | `krx_timing.db` |
| KIS 신용잔고는 T+2 저녁 이후 공표. v3 미사용 | 09-10 18:01 콜 |
| 키움 콜 속도 ≈ 11콜/초(TR 당 1스레드). 2 TR × 2,655 = 5,310콜 ≈ 8분 | 09-10 06:04 실행 |
| stage 전량 재빌드 22.5분, equity ≈ 8분 | v1 §0 |
| v3 `daily_all` 20:05~20:47(키움 ≈5,068콜), 토큰 재발급 07:00·20:05 | P0 task08 |

## 1. 결정 (사용자, 2026-09-10)

| # | 결정 | 내용 |
|---|---|---|
| V2-1 | 저녁 키움 행은 **원장 직행, KRX 대조 없음** | 오염 게이트(전일 복사본 비율)만 유지. KRX 는 가격 정본·기업행위 탐지용으로 아침에 받는다 |
| V2-2 | 잠정 가격 축 = 키움 종가 | 당일 종가는 키움 투자자·공매도 행의 값. 그날 가격이 뛴 종목(±30% 초과, 기업행위 의심)은 저녁 스코어에서 제외, 아침 확정판에서 복구 |
| V2-3 | WISE 스냅샷 06:00 → **18:05** | 당일 리비전 1일 랙 제거. 값 동일성 확인됨 |
| V2-4 | 완료 목표 19:00, 상한 19:30 | 키움·DART 병렬. 임계 경로 = 키움 8분 + 빌드 30분 + 스코어링 15분 |
| V2-5 | 프로브는 슬롯 시각 파라미터만 정한다 | 구현은 18:05 기준으로 지금 시작. 확정 시각이 이르면 크론·`--not-before` 만 당긴다 |
| V2-6 | **WISE 리비전은 전날 장종료 기준값**(사용자, 09-10) | WISE 가 주는 값 자체가 T-1 마감 기준이라 리비전 팩터의 1일 랙은 WISE 고유. 스냅샷 시각(18:05)은 임계 경로 밖이며 "그날 WISE 가 공개한 T-1 기준값" 을 당일 안에 확보하는 뜻 |
| V2-8 | **스코어링 입력은 스냅샷으로 보관해 추적 가능해야 한다**(사용자, 09-10) | 스코어 하나에서 입력을 되짚을 수 있어야 한다. §4 Task B.3 의 3층 규약: 실행 manifest(영구) · 팩터 패널(영구) · equity 입력 판 핀(30일 + 월말 영구). 원장은 append-only + observed_date 축이라 어느 시점으로도 재빌드 가능 |
| V2-7 | **조용한 실패 금지 — 항상 텔레그램 보고**(사용자, 09-10) | 모든 체인·빌드·스코어링은 성공·실패·건너뜀·부분 완료를 전부 보고한다. 예정 시각까지 보고가 없으면 워치독이 crit 를 보낸다. 예외는 `--dry-run` 뿐. §2-1 알림 정책 |

## 2. 하루 흐름 (KST · 크론은 UTC = KST − 9)

| 시각 | 주체 | 단계 | 소요 | 산출 |
|---|---|---|---|---|
| 15:30 | KRX | 정규장 마감 | | |
| 18:00 | DART | 접수 마감 | | |
| **18:05** | 우리 | **저녁 원장 슬롯** `daily_evening.sh`: 키움 ka10060·ka10014 당일 fetch → 원장 직행 ∥ DART 당일 스윕·상세 ∥ WISE 스냅샷(full) | 키움 8 · DART 30 · WISE 5 (병렬) | 원장 T 행 |
| 18:15 | 우리 | **잠정 빌드** `build_evening.sh`: 키움·WISE 끝나면 stage 재빌드 → equity 갱신(`basis=evening`) | 22 + 8 | equity 잠정판, 텔레그램 "데이터 준비" |
| 18:45 | Kael-alpha | 팩터·스코어링·보고 | 15 | 텔레그램 스코어 |
| **19:00** | | 완료 목표 (상한 19:30) | | |
| 20:05 | v3 | (병행기) 자기 수집·스코어링 | | 비교 대상 |
| 06:00 | 우리 | 아침 슬롯 1 `daily_ledger.sh`: 캘린더 → 키움 마스터 → 대차(ka20068) → KIS 신용 → DART 재스윕(늦은 접수분) | ≈40 | |
| 07:10 | 우리 | 아침 슬롯 2: 키움 외국인 보유(ka10008) — `daily_build.sh` 안 | 4 | |
| 08:10 | 우리 | **확정 빌드** `daily_build.sh`: KRX → 외국인 보유 → 머지(ka10008·ka20068) → 건전성 → stage → equity(`basis=krx`) | ≈45 | equity 확정판 09:00 |

### 2-1. 알림 정책 (V2-7)

| 언제 | 등급 | 내용 |
|---|---|---|
| 각 체인 종료(저녁 슬롯·잠정 빌드·아침 슬롯·확정 빌드) | info | 단계별 rc·행수·콜수·소요. 실패면 crit + 실패 단계·로그 경로 |
| 건전성 판정 | info/warn/crit | 20항목 결과. warn 도 보낸다(오늘처럼 통과만 요약하지 않는다) |
| 부분 완료(예: WISE 커버 222/639 같은 급감, DART 상세 결측, 키움 행수 < 0.98) | warn | 게이트가 건수를 적어 보낸다 |
| 휴장일 건너뜀 | info | "휴장 — 건너뜀" 도 보낸다(무소식 = 고장과 구분) |
| **워치독** | crit | 18:50 까지 "잠정판 준비" 없음 · 19:30 까지 스코어 보고 없음 · 09:15 까지 확정판 없음 → 별도 크론이 `deliver/*.json` 시각으로 판정 |
| dry-run | 없음 | 유일한 예외 |

토큰: 우리 18:05·06:00·07:10 발급, v3 07:00·20:05 발급 — 서로 무효화하나 양쪽 자동 복구(P0 실측). 저녁 슬롯은 20:05 훨씬 전에 끝난다.
콜 총량: 키움 우리 10,620/일(변동 없음, 시간대만 이동) + v3 12,701(병행기). v3 종료 시 12,701 감소.

## 3. 페이즈 A — 저녁 원장 슬롯 (파이프라인, 1주)

### Task A.1: `kw_daily` 저녁 직행 머지

- [ ] **Step 1** 실패 테스트(`tests/test_daily_kw.py`): `--fetch --tr ka10060,ka10014 --date T --commit` 가 fetch 뒤 두 TR 을 **KRX 대조 없이** `merge_tr` 로 본 테이블에 넣는다(신규 (ticker, dt) 만, 결정 6-1). 오염 게이트는 ka10008 이 없는 호출이라 `basis=skipped`. runlog `source=kiwoom_fetch` detail 에 `commit=1 new=… changed=…`.
- [ ] **Step 2** 구현: `--commit` 플래그. `fetch()` 결과가 ok 면 `merge_tr` 를 선택 TR 에 대해 호출. `--merge`(08:10)는 남은 incoming(ka10008·ka20068)만 처리하도록 이미 커밋된 TR 의 incoming 을 비운다.
- [ ] **Step 3** 테스트·린트·커밋.

### Task A.2: `scripts/daily_evening.sh` (18:05)

- [ ] **Step 1**: raw 락 → 캘린더 동기화 → D = 오늘(거래일 아니면 info 후 종료) → 병렬 3갈래: ① `kw_daily --fetch --tr ka10060,ka10014 --date D --commit --not-before "${QL_EVENING_NOT_BEFORE:-18:00}"` ② `dart_daily --date D` ③ `daily_wise.sh`(WISE 만, 마스터는 06:00 유지) → 세 갈래 rc 취합 → 알림(crit/info). ①·③ 종료 시각을 로그에 남긴다(빌드 트리거 근거).
- [ ] **Step 2**: `daily_ledger.sh`(06:00)에서 키움 3 TR → **ka20068 만**(`--tr ka20068`), `daily_wise.sh` 호출에서 WISE 제거·마스터만. `daily_build.sh` 는 변경 없음(ka10008 fetch → merge → 건전성).
- [ ] **Step 3**: `ledger_health` — `kiwoom.ka10060.rows`·`ka10014.trend` 는 D 행이 저녁에 이미 들어와 있으므로 판정 그대로. WISE 항등식은 run_day 기준이라 18:05 스냅샷을 다음 날 08:10 판정에서 `date(run_at,'+9h') = D` 로 읽도록 조정(테스트).
- [ ] **Step 4**: crontab `5 9 * * 1-5 daily_evening.sh`(18:05 KST). 첫 실행 전 `--dry-run --limit 3` 서버 스모크. 백업 `logs/crontab_backup_v2_<date>.txt`.
- [ ] **Step 5**: `dart_daily.plan` 대상을 "접수일 = D" 에서 "**이번 스윕에서 처음 본 공시**"(`collected_at >= 런 시작`) 로 바꿔 늦은 접수분의 상세 축을 받는다(검수 후속). 테스트 동반.

### Task A.3: 문서·기록

- [ ] `README.md` 크론 표, `DECISIONS_PENDING.md` 결정 8 확정, v1 상태 블록에 "v2 로 이관" 표기, WISE 스냅샷 시각 전환일(`ws_run_log` 로 판별 가능)을 STAGE_SPEC WISE 절에 한 줄.

### Task A.4: 워치독·알림 정책 (V2-7)

- [ ] `scripts/watchdog.sh`: 18:50·19:35·09:15 KST 에 `data/deliver/latest_evening.json`·스코어 보고 로그·`latest_morning.json` 의 시각을 보고 없으면 crit. 휴장일은 info.
- [ ] 모든 체인의 "건너뜀" 경로(휴장·이미 완료·락 실패)가 info 를 보내는지 점검. 건전성 warn 항목을 요약에 포함.

### 게이트 GA (5거래일, 09-12 ~)

| # | 검증 | 기대 |
|---|---|---|
| 1 | 저녁 슬롯 rc 0, 키움 두 TR D 행 ≥ 0.98×유니버스, DART 당일 ≥ 400건, WISE 15,578 항등식 | 5/5 |
| 2 | 저녁 값 = v3 저녁 값 | `investor_detail_flows` 12축·`daily_prices` 종가·거래량 전건 일치(비교 스크립트, 읽기 전용) |
| 3 | 프로브 T 판독 | 18:05 이후 값 변경 0 (더 이른 확정 시각이 나오면 V2-5 로 슬롯 당김) |
| 4 | 호출 총량 | 키움 우리 ≤ 유니버스×4+100, `kael` 0 |
| 5 | 아침 체인 | 08:10 머지가 ka10008·ka20068 만 처리, 건전성 통과, `changed=` 0 |
| 6 | 종료 시각 | 키움+WISE 18:15 전, DART 18:40 전 |

## 4. 페이즈 B — 잠정·확정 빌드 (v1 P4·P5 재설계, 2주)

### Task B.1: stage 저녁·아침 2회 빌드

- [ ] `build_evening.sh`(18:15, 키움·WISE 완료 신호 뒤): 빌드 락 → stage 전량 재빌드 → equity → 텔레그램 "잠정판 준비 hh:mm". DART 가 아직 쓰는 중이면 dart.db 는 **직전 스냅샷**을 쓴다(저녁 스코어링 경로 밖).
- [ ] 아침 `daily_build.sh` 뒤 stage·equity 확정 빌드(v1 P4·P5 의 stage_daily/equity_daily 를 그대로 쓰되 이름을 `build_morning`).
- [ ] 스냅샷 GC keep=3 → 저녁·아침 2판/일이므로 keep=6.
- [ ] 단축 옵션(GA 뒤 결정): 저녁은 키움·WISE 관련 stage 테이블만 재빌드하는 증분 경로 — 22분 → ≈10분.

### Task B.2: equity 잠정 행 규약

- [ ] `price_daily` 에 T 행을 키움 종가·거래량으로 채우고 `basis='evening'`. 아침 KRX 행이 `basis='krx'` 로 덮는다(EG 게이트: evening 행이 krx 행과 다르면 metric 기록, 폐기 아님).
- [ ] 기업행위 의심 종목(T 수익률 절대값 > 30% 또는 주식수 급변) 은 `flag=corp_action_pending` 으로 표시 — 소비자가 제외한다. 아침 `adj_factor` 산출 뒤 해제.
- [ ] `dataset_profile` 에 `basis` 와 잠정판 생성 시각. 워크벤치·커널 어댑터가 잠정판을 구분해 표시.
- [ ] v1 Task 5.5(검수 후속 5건: KIS 최초 관측판·액면병합 확인·잔고율 라벨·WISE 커버 이력·정정 링크)를 여기서 함께.

### Task B.3: 추적 가능성 규약 (V2-8) — "스코어 하나에서 입력까지"

실측(09-10): equity 전량 1판 ≈ 1.2 GB(28표, price_daily 298 MB·credit_daily 400 MB·flow 330 MB), stage 1판 ≈ 1.5 GB(66표), 원장 스냅샷 세트 6.6~17 GB, 디스크 여유 283 GB. 매일 2판을 전부 영구 보관하면 연 ≈600 GB 라 불가 → 3층으로 나눈다.

| 층 | 무엇 | 어디 | 크기 | 보관 |
|---|---|---|---|---|
| ① 실행 manifest | equity 빌드 id + 표별 판 id·내용 해시, 팩터 레지스트리 버전, 모델 설정 해시, 유니버스 정책 id, 코드 커밋, 제외 종목 목록, 생성 시각 | Kael-alpha `out/scores/<model>/<date>_<basis>.manifest.json` + 파이프라인 `deliver/history/<date>_<basis>.json` | 수 KB | **영구** |
| ② 팩터 패널 | 종목×팩터 값(long parquet), basis 표식 | Kael-alpha `out/factors/<date>_<basis>.parquet` | < 1 MB/일 | **영구** — 모델 재계산은 equity 없이 여기서 |
| ③ equity 입력 판 | 스코어링이 참조한 빌드 전체(28표) | `data/equity/<표>/v=e_…`, `v=m_…` + `_pinned` | 1.2 GB/판 | 참조된 판은 GC 제외 **30일**(≈72 GB), **월말 확정판은 영구 봉인**(≈15 GB/년), 그 밖은 GC |
| ④ 원장 | raw 5 DB | `data/raw` (append-only, KIS·DART 판본 보존, 키움 신규 행만, WISE 일별 스냅샷) | 45 GB | 영구. observed_date 축으로 어느 시점 stage 도 재빌드 가능(G6). 온라인 백업 7일 + 월 1회(P6) |

- [ ] 파이프라인: `deliver/latest_*.json` 을 `deliver/history/` 에 날짜·basis 별로 복사. equity GC(`gc_pinned`)가 `deliver/history` 와 Kael-alpha manifest 가 가리키는 판을 30일간 보호하고 월말 판은 영구 보호. 스냅샷 GC keep=6 유지.
- [ ] Kael-alpha: 실행마다 ①·② 기록. manifest 없는 스코어는 게이트 GC 실패로 간주.
- [ ] 검증: 임의의 과거 스코어 1건을 골라 manifest → 팩터 패널 → equity 판 순으로 되짚어 같은 스코어가 재현되는지(연 1회 이상, 첫 회는 GB 안에서).

### 게이트 GB (5거래일)

| # | 검증 | 기대 |
|---|---|---|
| 1 | 잠정 빌드 완료 시각 | 18:45 전 5/5 |
| 2 | 잠정 vs 확정 | 차이 행 = 기업행위 종목 + KRX 미거래 정정뿐 |
| 3 | equity 게이트 | 잠정·확정 둘 다 EG 통과, `credit_daily` 판본 규칙 적용 |
| 4 | 소비자 | 어댑터가 `basis` 를 노출, 잠정판 표식 없이 확정값처럼 보이는 경로 0 |
| 5 | 추적 | 스코어 1건을 manifest → 팩터 패널 → equity 판으로 되짚어 재현(B.3) |

## 5. 페이즈 C — 스코어링 (Kael-alpha)

파이프라인이 주는 것: 18:45 전 equity 잠정판 + 텔레그램 "준비" 신호, 09:00 확정판. Kael-alpha 가 정할 것은 인계 문서 §3(팩터 정의·엔진·저녁 실행·병행 비교·컷오버 기준). 파이프라인 쪽 후속 요구(리비전 1w 축, GPA·FCF 파생, 커버 이력 표)는 B.2 에 반영.

### C.2 스코어 파일 발송 → LLM 해석 → kael-wiki (사용자 요구 09-11, v3 실측)

v3 의 스코어 후처리 순서(09-10 실측, 전부 `CHAT_ID_LOG`): ① `export_and_send.py` — 스코어 xlsx(v3·v2)를 텔레그램 문서로 발송 ② `insight` — 지수·시장폭·프로그램·섹터·매크로 수집 → 국면 판정 → 해설 리포트 + 변곡 알림(22:16) ③ `kael-wiki/scripts/ingest.sh` — `claude -p` 3회: Step 1 분석(≈9분, 22:14→22:23) → Step 2 위키 페이지 갱신(가설·패턴·국면, ≈9분) → Step 2.5 수치 기계 정정 → Step 3 텔레그램 요약 생성·발송(core+detail, ≈2분, 22:34). v3 는 20:05 → 22:34 로 2.5시간이 걸린다.

새 파이프라인의 순서와 예산: **19:00 스코어 파일 발송(문서) → 19:00~19:20 해석(위키 ingest 3단계, ≈20분) → 19:20 해석 발송**. 상한 19:30 은 해석 발송까지 포함한다. 병렬화 여지: Step 1 은 스코어가 있어야 하므로 스코어 뒤, 위키 Step 2 는 발송 뒤로 미룰 수 있다(해석 core 를 먼저 보내고 위키 갱신은 뒤에).

- [ ] **데이터 어댑터**: `ingest.sh` 의 헬퍼(`judge_returns.py`·`build_thesis_stats.py`·`verify_numbers.py`)와 `claude -p` 프롬프트가 v3 `quant.db`(`score_history_v2`·`investor_detail_flows`·`daily_prices`·`consensus_*`)를 직접 읽는다. 컷오버용으로 **같은 테이블명의 호환 SQLite** 를 파이프라인이 매일 내보내는 것(빠름, 위키 스크립트 무변경)을 먼저, equity parquet 직접 읽기는 뒤로.
- [ ] 스코어 파일 발송은 Kael-alpha `out/scores` 에서, 해석·위키는 `~/kael-wiki` 그대로(입력만 교체). 발송 순서 보장: 파일 → 해석(core) → detail → 위키 갱신 완료 알림.
- [ ] **v3 종료 범위 결정 필요**: `daily_all` 에는 스코어링 외에 `insight`(국면·변곡, 수집기가 우리 원장에 없는 시장폭·프로그램·섹터·ECOS 매크로를 쓴다)·아침/점심/마감 브리핑·뉴스·이벤트가 붙어 있다. 스코어링만 대체하고 나머지는 v3 에 남길지, 전부 옮길지 사용자 결정.

### 게이트 GC (5거래일)

19:30 전 **스코어 파일 + 해석 발송** 5/5 · v3 스코어와 순위 상관·상위 N 겹침 기록 · 실패 시 아침 확정판으로 재계산되는 경로 확인.

## 6. 페이즈 D — 컷오버

- [ ] GA·GB·GC 통과 뒤 사용자 결정으로 v3 `daily_all` 크론 제거(백업). 키움 콜 −12,701/일, 토큰 충돌 −2회/일.
- [ ] v3 종료 뒤 슬롯 재조정: 저녁 슬롯을 프로브 확정 시각까지 당기고, 06:00 슬롯의 마스터·대차·KIS 를 저녁으로 옮길지 재검토(대차 당일 20:05·외국인 보유 21:05 확정 실측).
- [ ] **종료 범위 결정**: `daily_all` 의 insight(국면·변곡)·아침/점심/마감 브리핑·뉴스·이벤트를 옮길지 남길지. insight 입력 6개 중 3개는 우리 KRX 원장에 있고 3개(시장 투자자별·프로그램·ECOS 매크로)는 하루 ≈6콜 소형 수집기.
- [ ] 키·토큰을 `~/quant-ledger/.env` 로 이관, 원장 정기 백업(온라인 백업·7일 보관), `_tmp`·`_export` 잔재 정리(P6).

### Task D.2: v3 데이터베이스 처리 (사용자 질문 09-11 — `raw/` 에 섞지 않는다)

원칙: `data/raw` 는 우리 수집기가 우리 규약(원문 보존·관측 시각·판본)으로 쌓은 원장만. v3 DB(`~/kael-system-v3/data`, 활성 4개 ≈1.5 GB)는 컷오버 때 `~/quant-ledger/data/legacy/kael-v3/<날짜>/` 에 SQLite 온라인 백업 + 체크섬으로 **읽기 전용 동결**. 병행기에는 손대지 않는다(v3 가 매일 쓴다).

| 처리 | 대상 | 이유 | 할 일 |
|---|---|---|---|
| 아카이브만(대체됨) | `quant.db` 시세·수급·컨센서스·재무, `dart.db` | 우리 원장이 더 길고 상세 | 동결 사본, 수집 중단 |
| 이력 1회 임포트 + 수집기 이관 | `quant.db` 국면 입력 3개(`market_investor_flows`·`program_trading`·`macro_data`) | 우리 원장에 없고 v3 이력 2025~ | 우리 raw 에 `market.db` 신설, 과거분 `src='v3_import'` 표식 임포트, 하루 ≈6콜 수집기(키움 ka10051·ka90010, ECOS) |
| 읽기 전용 보존(기록 가치) | `score_history`·`score_history_v2`(109일), `news.db`(2021~), `research_reports`·`broker_reports`, `hypotheses.db` | 재수집 불가 또는 병행 비교·가설 판정 기준 | 동결 사본. 스코어 이력은 Kael-alpha 병행 비교, 가설은 Kael-alpha 원장으로 이관 검토, 뉴스·리포트 수집 지속 여부는 D 결정 |
| 삭제 | `trade.db`(06-30 정지, 451 MB), `agenda.db`·`board.db`(7월 실험), `kg.db`·`market.db`(0 B) | 사용처 없음 | 백업 1회 뒤 정리 |

## 7. 리스크

| 리스크 | 대응 |
|---|---|
| 키움 당일 행 확정 시각을 18:10 이전엔 관측 못 함 | 09-11 프로브 판독. 18:05 는 관측된 안전선 |
| 병행기 공유 앱키 하루 ≈23,300콜 | 이틀간 오류 0. 오류 시 저녁 슬롯을 v3 뒤(21:00)로 임시 이동 — 목표 미달이지만 데이터는 확보 |
| 잠정판을 확정값으로 오인 | `basis` 표식·어댑터 노출(GB-4) 필수 |
| stage 22분이 19:00 목표를 위협 | B.1 증분 경로. 그 전엔 18:05 → 18:15 → 18:45 → 19:00 으로 여유 15분 |
| DART 상세 30분이 저녁 슬롯을 늘림 | 스코어링 경로 밖. 빌드는 키움·WISE 완료 신호로 시작 |

## 8. v1 에서 넘어온 미결

- 09-09 외국인 보유 대형주 잠정값 → 09-11 `changed=` 로 종목 수 확인 뒤 1회 갱신.
- 프로브 크론 제거 시점: GA-3 판독 뒤(09-15 목표 유지).
- G3(v1) 관찰은 09-11 부터 5거래일 — 페이즈 A 와 겹쳐서 진행하되 저녁 슬롯 도입일(크론 등록일)부터 GA 로 대체.
