# 일일 증분 첫 실행분 검수 종합 — 2026-09-10 (거래일 D=09-09)

- 대상: 09-10 06:00·08:10 KST 체인이 적재한 원장 5개(KRX·키움·KIS·DART·WISE). 서버 읽기 전용 질의만.
- 방법: 소스별 검수 4건(Opus 병렬) → 종합자가 high 판정 전건과 코드 지적 전건을 재검산. 소스별 보고서:
  `2026-09-10-intake-audit-{A-krx,B-kiwoom,C-kis,D-dart-wise}.md` (검사 항목·수치·재현 SQL 은 거기).
- 이 문서는 **판정과 조치 제안**만 담는다. 코드 수정은 하지 않았다(사용자 승인 대상).

## 0. 한 줄 결론

**수집기는 오늘 들어온 값을 원천 그대로 무손실로 적재했다**(KRX 시총합 = 지수 시총 7일 연속 오차 0, 키움↔KRX
종가·거래량 2,650/2,650, KIS 거래량↔KRX 전건 일치, DART 로그 = 원장 100%, WISE 15,578행 정확).
결함은 전부 **원천 특성을 하류가 아직 모르는 것**과 **P1 에서 급히 만든 증분 코드의 설계 구멍**에서 나왔다.
high 5건 중 즉시 코드 수정이 필요한 것은 WISE 커버 오탐·DART 분기 창·KIS 판본 선택 3건이다.

## 1. 발견 (심각도 순)

검증 열: **재검산** = 종합자가 서버 질의·코드로 직접 확인 / **정정** = 에이전트 판정을 고쳤다 / **채택** = 보고서 근거가 충분해 그대로 채택.

### HIGH — 계산 결과를 틀리게 하거나 결측을 만든다

| # | 소스 | 무엇이 | 근거·위치 | 검증 | 조치 제안 | 처리 |
|---|---|---|---|---|---|---|
| H1 | KRX→하류 | **액면병합 2건**(001290 상상인증권 1,000→5,000 · 273060 와이즈버즈 100→500, 주식수 ÷5, 09-09 재개). 원장은 정확. 종가를 그대로 이으면 +345%/+380%, 실제 −10.9%/−4.1% | A §2 A01. `krx_*_isu_base_info.PARVAL·LIST_SHRS` 09-08→09-09 | 재검산: equity `sql/corp_event.sql:216-246` KRX 축이 "액면가 변경 + 직전 거래일 행 존재" 로 reverse_split ratio 0.2 를 만든다. 정지 중에도 base_info 행이 매일 있으므로(집합 차 0) 다음 stage·equity 재빌드에서 잡히는 구조 | ① P4·P5 첫 재빌드 뒤 `adj_factor` 0.2 두 건 확인 ② ledger_health 에 `PARVAL`/`LIST_SHRS` 일변동 게이트(warn) 추가 — 오늘 09-09 전수는 6건(A02) | P4/P5 |
| H2 | 키움 | **폐지 직전 구간 영구 결측** 3종목: 096610 알에프세미 7세션(08-25~09-02 정리매매, 종가 2,965→141), 471050 5세션, 082640 4세션. 08-20 정지 뒤 09-09 갭 메우기 때 이미 폐지돼 키움이 0행 | B §2 F-1 | 채택(incoming 0행·KRX 시세 존재 확인) | 복구 불가(키움 API 폐지종목 조회 불가). KRX 가격축은 있으므로 외국인·투자자·대차·공매도 4축만 결측. **BLOCKED/TECH_DEBT 에 알려진 구멍으로 등재**, 생존편향 주의 문구 | 문서 |
| H3 | KIS | **재수집 판본 중복 536군**(2026년 505군·29종목, 전부 09-09 캐치업이 생성, 오늘 0). 원인: `stck_prpr` 등 가격이 조회 시점 수정주가로 소급 환산돼(001290 07-31~08-18 행이 969 vs 4,845 두 판) 기업행위 뒤 재수집하면 payload 가 달라진다 → `store_new_facts` 가 정정으로 보존 | C §2 HIGH-3·HIGH-2. `src/daily/kis_daily.py` 40일 창 | 재검산: 001290 09-01~09-04 행 `stck_prpr=5050`(=1,010×5) vs `oprc=1010` 확인. stage `stg_credit_daily` 는 append_only·`key_unique=False` 라 접지 못하고(`rules_kis.py:289`), equity `sql/credit_daily.sql:141` 이 판본 선택 없이 조인 → `gates.py:249-259` 격자 유일성에서 **빌드 실패 예상** | ① equity credit_daily 에 판본 선택 규칙(`observed_date` 최신 = 소급 환산판, 또는 최초 = PIT 판 — **사용자 결정**) ② 매 기업행위마다 최대 40행씩 재발하므로 규칙은 영구 | P5 |
| H4 | WISE | **커버 판정 오탐(false-none)**: `backfill_wise.py:263-270` 이 `ymms[0]`(FY2026) 의 `cF5001` 한 해만 보고 종목을 `none` 확정, 나머지 13 ep 를 안 받는다. 09-10 커버 상실 4건 중 3건(036010·190510·342870)은 09-09 원문에 FY2027 추정치 실재. 연말로 갈수록 당해 연도가 먼저 비어 오탐 증가 | D §2 H1 | 재검산: 코드 확인(`covered = is_covered(body)` 가 `pk == ymms[0]` 에서만, 이후 `continue`) | 3개년 `cF5001` 을 다 보고 하나라도 커버면 covered. 비용 +2콜×1,759 ≈ +65초. 과거 스냅샷은 복구 불가, 수정 즉시 다음 실행부터 회복 | **즉시**(P3 중 핫픽스) |
| H5 | DART | **10-01 부터 3분기 창이 스윕에서 빠진다**: `dart_daily.py:474-478` `sweep_from_for(D)` = D 의 분기. 오늘도 442페이지에 8월 접수분 10건이 새로 나타났으므로 늦게 반영되는 접수분이 결측된다. 별개로 3분기 창이 `mismatch`(적재 고유 44,162 > total_count 44,156 — DART 측 삭제 공시 6건, 페이지 시프트 시그니처로 확정)라 매일 442콜 전량 재스윕 | D §2 H3. `sweep_disclosure.py:213`(mismatch 면 page 1) | 재검산: 코드 확인. 창 대조는 `수신 == total_count` 는 통과, `적재 > total_count` 가 실패 사유 | ① `--from` 을 직전 분기 포함(예: D−45일이 속한 분기)으로 ② 대조 규칙을 "수신 == total_count 면 ok, 적재 초과분은 note 에 기록"으로 — 삭제 공시는 정당 | **10-01 전**(P6 이전) |

### MID — 특정 종목·일에 국한되거나 설계 판단이 필요한 것

| # | 소스 | 무엇이 | 위치 | 검증 | 조치 제안 | 처리 |
|---|---|---|---|---|---|---|
| M1 | 키움 코드 | **유예(grace) 결함 3종**: ⓐ 마스터에서 사라진 종목을 grace 에 넣는 분기가 없다(첫 실행 시드에만) ⓑ `missing_days` 가 거래일이 아니라 `requested()` 호출 횟수(kw+kis 가 같은 `universe_kw.json` 을 써 하루 2회, 실효 2.5거래일) ⓒ `last_seen` 이 시드일에 고정돼 폐지 종목 4개(082640·096610·269620·471050)가 만료 조건 `max(dt) ≥ last_seen` 을 영원히 못 넘어 고착(하루 16콜 낭비) | `src/daily/universe.py:82-103`, `kis_daily.py:475` | 재검산: 코드·서버 상태 파일(`missing_days=3`, `last_seen=20260909`) 확인 | ⓐ 직전 요청 집합 − 오늘 마스터 집합을 grace 에 추가 ⓑ 카운트를 `asof` 날짜 변화에만 증가 ⓒ `last_seen` 을 ka10008 실제 max(dt) 로 | P3 중 |
| M2 | 키움 설계 | merge 가 `INSERT OR REPLACE` 로 **매일 전체 이력 재기록**(ka10014 90만 행 2008년~, 4 TR 합 155만 행) · `collected_at` 이 오늘 값으로 덮여 최초 관측 시각 소실 · 원천 정정이 무기록 반영 | `src/daily/kw_daily.py:304·549` | 재검산: 코드 확인(주석이 의도된 설계라고 명시) | **사용자 결정**: (a) 유지(정정 자동 반영) (b) 신규 (ticker,dt) 만 삽입 + 기존 행과 값 차이 건수를 runlog 에 기록 — stage MANIFEST 재현성 관점에선 (b) | P4 전 |
| M3 | KIS 정의 | `whol_loan_rmnd_rate` 분모 = **공표일(T+2) 시점** 상장주식수. 에이전트는 "조회 시점 재계산"이라 했으나 **정정**: 같은 콜(09-09 16:33)에서 온 363260 모비데이즈 09-03 행(0.40 = 병합 전 분모)과 09-04 행(0.61 ≈ 병합 후 분모)이 갈리므로 조회 시점이 아니라 공표 시점 값이다. look-ahead 아님. 다만 deal_date 주식수와 다르므로 잔고율 검산 게이트는 T~T+2 주식수 변동 종목(09-07 기준 15종목)에서 어긋난다 | C §2 HIGH-1 | 재검산(정정) | stage 컬럼 설명에 "공표일 주식수 기준" 명기, equity 잔고율 검산은 T+2 주식수로 | P5 문서 |
| M4 | KIS 원천 | 액면병합 종목에서 **잔고 항등식**(rmnd = prev + new − rdmp)이 깨진다(잔차 >1만주 95건/53종목). 주수가 병합 전후로 조정되지 않음 | C §2 MID-1 | 채택 | equity 항등식 게이트는 `adj_factor` 사건일을 제외 | P5 |
| M5 | KIS 원천 | 증거금 100% 지정 5종목(148780·294630·321370·328380·415640)이 거래는 되는데 신용잔고 행만 소멸. 09-07 결측 126 중 118 은 원천 특성(스팩 64·증거금100% 26·정지 8·신규상장 7·관리 6·환기 4·경고 1·위험 1·마스터없음 1), **8종목은 사유 미확인**(종목별 콜 로그가 없어 빈 응답인지 판별 불가 — L3) | C §1 #3·§2 MID-2 | 채택 | fill_kind `src_omitted` 로 이미 다룸. 8종목은 내일 실행 뒤 재확인 | 관찰 |
| M6 | DART | 후속 축 결측 3건 — 목록엔 있으나 상세 API 가 안 준다: 20260909000036 스모트로닉(대량보유 기재정정) · 20260909000270 태웅(대량보유) · 20260909000161 TS트릴리온(임원). 콜은 전부 `000` 정상 | D §2 | 채택 | 원천 특성. 문서층(L1) ZIP 이 대안. 건수만 기록 | 문서 |
| M7 | DART | 정정 대응 비대칭: `[기재정정]` 은 축 테이블에 정정본 rcept_no 만, `[첨부정정]` 은 원본만 남는다. `dart_disclosure` 는 `rm` 이 바뀔 때마다 새 행(3분기 1,513건이 2~3행) | D §2 | 채택 | stage `stg_rcept_dt_map` 은 rcept_no 기준이라 무관. equity 공시 판본 테이블(`disclosure_version`)이 두 경우를 모두 잇는지 P5 에서 확인 | P5 확인 |
| M8 | DART 범위 | 주요사항보고서 39건 중 21건은 수집 엔드포인트가 없다(자기주식 신탁 체결·해지, 타법인 주식 양수도, 유형자산 양수, 소송 등) | D §2 | 채택 — **결함이 아니라 DART_CENSUS 의 범위 결정** | 필요해지면 문서층(L1)으로 | 범위 |
| M9 | P3 게이트 | 06:00 체인 76분(G3 #3 45분 초과, 토큰 충돌 없음) · 키움 10,620콜(#4 의 10,500 은 옛 유니버스 기준) | 플랜 Task 3.2 관찰 기록 | 재검산 | G3 #3 을 "KIS·키움 07:00 전 종료 + 전체 ≤ 90분"으로, #4 를 유니버스×4 로 갱신 — **사용자 확인** | P3 |

### LOW — 기록·미관·소비자 정책

- **KRX**: ETF 265690 ACE 러시아MSCI 거래정지 잔존가(종가 8,535 vs NAV 48.41) · 121850 코이즈 정리매매 +140%(제한폭 밖, 상폐 인접) · 스팩 기준가 절상으로 `FLUC_RT` 와 종가 체인 ≤3원 차이(2025년 이후 350건, 원장은 정확) · `(외국주포함)` 지수 2건 종가 상시 공란 · **시세 `ISU_CD` 는 6자리, base_info `ISU_CD` 는 ISIN 12자리** — 조인 키는 `ISU_SRT_CD`(순진한 조인은 교집합 0).
- **키움**: `collected_at` 이 배치 상수(행 단위 아님) · `ka10014.shrts_trde_prica` 천원 반올림이 소량 공매도 평균가를 깨뜨림 · 보통주 유니버스에 리츠 23·인프라 2 포함(정책표 결정 사항). 단위 확정: `ka10060.acc_trde_prica` = **거래량(주)**(KRX 2,650/2,650, stage 는 이미 `volume_shr`), 투자자 13컬럼 백만원, `ka20068.remn_amt` 백만원, `ka10014.ovr_shrts_qty` = 누적 공매도량.
- **KIS**: 오늘 실행이 `kis_ingest_log`·`kis_call_log` 에 행을 남기지 않는다(`daily_run.db` 요약 1행만) — 종목별 사후 판별 불가(M5 의 8종목). `*_amt` 총계가 주수×체결가의 0.855배인 것은 **취득금액 기준이라 정상**(09-09 키움 ka10013 대조로 단위 확정).
- **DART**: 축 테이블 `rcept_dt` 가 `2026-09-09`(ISO), 목록은 `20260909` — stage 는 `KIND_DATE_ISO` 로 파싱하므로(`rules_dart.py`) 원장 직접 조인 때만 주의. 09-10 접수 3건이 09-09 스윕에 섞인 것은 설계대로(내일 D=09-10 처리). 문서 ZIP 대상 0건은 규칙상 맞음.
- **v3 미러**: `v3_*` 4표가 09-02 에서 멈춘 것은 **결함이 아니라 문서화된 결정**(STAGE_DESIGN 결정 ⑤ "동결 사본만", `daily_wise.sh:5`). 09-02 부분 스냅샷(130행/50종목)은 stage 가 "두 판본"으로 인지(`rules_wise.py:273`). 에이전트의 high 판정을 **정정**.

## 2. 결측 목록 (오늘 기준으로 "없는 것")

| 어디 | 무엇 | 복구 |
|---|---|---|
| 키움 4축 | 096610 7세션 · 471050 5세션 · 082640 4세션 (08-25~09-02, 폐지 직전) | 불가 — H2 |
| WISE 09-10 스냅샷 | 036010·190510·342870 의 13 ep(오탐 none) | 스냅샷은 불가, 코드 수정 뒤 09-11 부터 — H4 |
| KIS 09-07 | 126종목 중 8종목 사유 미확인 | 09-11 실행 뒤 재확인 — M5 |
| DART 축 | 09-09 대량보유·임원 3건 | 불가(원천) — M6 |
| DART 목록 | 3분기 창 늦은 접수분(10-01 이후) | 예방 — H5 |

## 3. 정상으로 확인한 것

KRX 결측 0·중복 0·OHLC 위반 0·시총 등식 0건 위반·ingest_log 전건 일치 | 키움 미래 dt 잔존 0·KRX 대조 100%·대차 항등식 2종 100%·투자자 합계 잔차 ±2·값 형식 오류 0·누락 5종목 전부 사유 확인(신규상장 1 = `0197V0` 09-10 상장, 폐지 4) | KIS 오늘 정정 0·신규 중복 0·`acml_vol` KRX 전건 일치·`stlm_date` 2거래일 예외 0·값 범위 위반 0·**288행은 정정이 아니라 시드 파일에 없던 23종목(외국주권 21 등)의 최초 도착** | DART 게이트 4/4·`ingest_log` 오늘 595행 error 0·kael 0·530행 vs 419건 = 비상장 106 + 내용개정 중복 5 | WISE 15,578 = 804×15 + 1,759×2·bytes 0 없음·전 ep 갱신 중.

## 4. 에이전트 판정을 고친 것 (3건)

1. C HIGH-1 "잔고율 분모 = 조회 시점" → **공표 시점**(M3). 같은 콜의 두 행이 다른 분모를 쓰는 것이 근거.
2. D H2 "v3 복사 8일 정지" → 문서화된 동결 결정. 결함 아님.
3. D mid "축 테이블 rcept_dt 포맷 불일치로 조인 0행" → stage 가 ISO 파서를 선언해 처리 중. 원장 직접 조인 주의로 강등.
4. (보강) B F-7 "유니버스 2,627 미재현" → 09-09 낮 실행은 옛 규칙(`upSizeName` 필수), 19:14 커밋 `ddf57da` 로 신규상장 포함 규칙이 들어가 오늘 2,650. 조용한 축소가 아니라 규칙 변경 시차.

## 5. 제안 순서

1. **즉시(P3 중 핫픽스, 승인 필요)**: H4 WISE 커버 판정 · M1 유예 3종 · H5 DART 창(`--from` 직전 분기 포함 + 대조 규칙). 셋 다 수집기 코드이고 테스트로 잠글 수 있다.
2. **P4 전 결정**: M2 키움 merge 방식.
3. **P5 에서**: H3 KIS 판본 선택 규칙(결정 필요) · H1 adj_factor 확인 + PARVAL 게이트 · M3·M4 · M7.
4. **문서**: H2 알려진 구멍 등재, LOW 항목의 정책표 반영(관리종목·정리매매·거래정지 ETF·리츠).
