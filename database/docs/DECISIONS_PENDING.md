# DECISIONS_PENDING — 사람이 정해야 할 것 (2026-09-06 작성 · **2026-09-09 갱신**)

> **상태(2026-09-09)**: **결정 5(일일 증분 플랜 R1~R10) 전부 승인** — R1 은 사용자 수정판(06:00 수집 + 08:10 빌드), R8 은 CHAT_ID_LOG 로 변경. **R5 후속(키움 앱키 추가 발급)은 사용자 행동 대기.**
> 본문은 `plans/2026-09-09-daily-incremental.md` §1, 요약은 이 문서 맨 끝.
>
> **상태(2026-09-07)**: 결정 **2·3·4 확정 완료** — 근거와 반영 커밋은 각 절과 아래 요약표에
> 있다. **결정 1은 질문 자체가 틀려 전면 재작성**했다(§결정 1 §0).
>
> 옛 판은 결정 1을 「미조정 27.44% 를 어디까지 버릴 것인가」로 물었다. 그것은 **판정**이고,
> WORKFLOW §0-2 가 이 층이 하지 않을 일로 명시한 「판정 결과를 팩트에 굽기」에 해당한다.
> 이 층의 목적은 **「날짜 D에, 그 시점에 알 수 있던 정보만으로, 그날 실존한 전 종목의 상태를
> 줘」** 다 — 상태를 주는 것이지 골라 주는 것이 아니다. 무엇을 버릴지는 팩터층·커널이 정한다.

이 문서는 **결정을 내리기 위한 문서**다. equity 층은 지금 잘 돌아가지만, 몇 자리에서
"기계가 정할 수 없는 값"을 비워 둔 채 서 있다.

읽는 순서는 상관없다. 각 항목은 ⑴ 무엇을 정하는 문제인가 ⑵ 지금 값과 그 근거
⑶ 선택지별 수치 ⑷ 권고와 이유 ⑸ 틀렸을 때 생기는 일 로 되어 있다.

- 측정 환경: 서버 `kael-server:~/quant-ledger`, 2026-09-06 커밋판 parquet 직접 조회.
- 조사 범위: 읽기만 했다. 코드·데이터·설정을 바꾸지 않았다.
- 근거 쿼리는 본문 각주에 어느 표의 어느 컬럼을 어떻게 셌는지로 남겼다.

---

## 결정 1 — 계수를 못 낸 사건을 **어떻게 적을 것인가** (2026-09-07 전면 재작성)

> **이 항목은 09-06 판에서 「어디까지 버릴 것인가」였고, 그 질문 자체가 틀렸다.**
> 아래 §0 이 왜 틀렸는지 적는다. 옛 판을 읽고 온 사람은 §0 부터 볼 것.

### 0. 09-06 판이 틀린 이유 — 이 층은 판정하지 않는다

옛 판은 조정계수가 없는 행을 **「오염 27.44%」** 라 부르고 "이 27%를 어떻게 버릴 것인가" 를
물었다. 두 군데가 틀렸다.

**(1) 이름이 판정이었다.** `n_unadjusted_events > 0` 은 「계수를 못 낸 사건이 이 행 앞에 있다」는
**사실**이지 「이 행이 틀렸다」가 아니다. 그것을 오염이라 부르는 순간 줄여야 할 수가 되고, 그
뒤로는 무엇을 버릴지만 생각하게 된다. WORKFLOW §0-2 는 이 층이 하지 않을 일에
**「판정 결과를 팩트에 굽기」** 를 명시해 두었다.

**(2) 버리는 것은 소비자의 일이다.** 배당락에 주가가 3% 빠진 것이 조정해야 할 잘못인지,
실제 매도 압력이라 이벤트 신호인지는 **전략이 정한다**. CB 발행 공시 뒤 하락은 희석이라
**진짜 하락**이고 조정하면 오히려 사실을 지운다. equity 는 「그날 무슨 일이 있었는가」를
적어 보내고, 무엇을 쓸지는 팩터층·커널이 고른다.

이 층의 목적은 **「날짜 D에, 그 시점에 알 수 있던 정보만으로, 그날 실존한 전 종목의 상태를
줘」** 다(WORKFLOW §0). 상태를 주는 것이지 상태를 골라 주는 것이 아니다.

### 1. 그래서 진짜 문제는 무엇인가

**소비자가 「무슨 일이 있었는지」를 알 수 없다.** `adj_factor.factor_source` 는 「우리가 왜
계수를 못 냈는가」를 적는다 — `unknown_price_only`·`no_price_match` 같은 값이다. 이건 우리
사정이지 시장 사실이 아니다. 소비자가 보면 **전부 「모름」** 이다.

서버 실측(2026-09-07, 비율 복구 반영 후): 계수를 못 낸 사건 **3,539건**. 그 사건일에 주가가
실제로 얼마나 움직였는지 재면:

| 임계 | 실제로 그만큼 움직인 사건 |
|---|---|
| 2% | 2,272 |
| 5% | 1,703 |
| **10%** | **1,162** |
| 20% | 784 |

**3,539건 중 1,162건만 주가가 10% 넘게 움직였다.** 나머지 2,377건은 계수가 없어도 값이 멀쩡
하다 — 조정할 것이 애초에 없었다. 옛 판의 27.44% 는 위험을 **세 배 부풀린 수**였다.

### 2. 사유별 — 라벨과 실제가 따로 논다

| `factor_source` | 사건 | 10% 넘게 움직인 것 | 비율 |
|---|---|---|---|
| `krx_base_inconsistent` | 720 | 653 | **90.7%** |
| `same_day_suppressed` | 10 | 8 | 80.0% |
| `unknown_price_only` | 1,737 | 418 | 24.1% |
| `no_price_match` | 464 | 66 | 14.2% |
| `ratio_null` | 394 | 17 | 4.3% |
| `near_dup_suppressed` | 186 | 0 | **0%** |
| `capred_paid` | 28 | 0 | **0%** |

**사유로 등급을 나누면 양쪽으로 틀린다.** `unknown_price_only` 를 통째로 버리면 멀쩡한
1,319건까지 버리고, `no_price_match` 를 통째로 허용하면 실제로 움직인 66건이 그냥 지나간다.

`near_dup_suppressed`·`capred_paid` 214건은 **고칠 것이 아니라 정상 동작**이다 — 앞의 것은
같은 사건을 다른 행이 이미 조정하고 있고, 유상감자는 시가총액이 실제로 줄어드는 사건이라
조정하면 안 된다. 그런데 지금 셈은 이 셋을 똑같이 센다.

### 3. `unknown_price_only` 1,737건의 정체 — 실측으로 갈렸다

「기준가는 바뀌었는데 같은 날 주식수가 안 바뀐」 행이다. 무엇인지 재 보았다.

**월별 분포와 사후 주식수 변화가 두 덩어리로 정확히 가른다.**

| 덩어리 | 건수 | 중앙 배수 | 10% 넘는 낙폭 | 사후 90일 안 주식수 증가 |
|---|---|---|---|---|
| **12월** | 617 | 0.9683 | 61 (10%) | 148 (24%) |
| **그 외** | 1,120 | 0.9353 | 343 (31%) | **991 (88%)** |

12월 617건은 중앙 낙폭 3.2% 로 배당수익률 수준이고 주식수가 안 늘어난다 → **배당락**.
나머지는 88% 가 나중에 주식수가 늘어난다 → **권리락**(신주가 몇 주 뒤 상장된다).

근처 공시를 전수 대조하면 권리락의 정체가 나온다(사건일 −90 ~ +40일):

| 공시 | 건수 |
|---|---|
| **유상증자 결정** | **537** |
| 유무상증자 결정 | 70 |
| 전환사채 발행 | 54 |
| 무상증자 결정 | 21 |
| 주식소각 | 1 |

**가장 큰 덩어리가 유상증자다.** 유상증자는 신주를 대가를 받고 파는 것이라 시가총액이 실제로
늘어난다 — 무상증자와 **같은 공식으로 조정하면 안 된다**. CB 발행 뒤 하락도 희석이라 진짜다.

### 4. 권고 — 버리는 규약이 아니라 **사건 유형을 싣는다**

equity 가 할 일은 `unknown_price_only` 라는 「모름」 라벨을 **시장 사실로 바꾸는 것**이다.

| 지금 | 바꾼 뒤 |
|---|---|
| `unknown_price_only` × 1,737 | `배당락` 617 · `유상증자 권리락` 537 · `CB 발행` 54 · `무상증자 권리락` 91 · `미규명` 나머지 |

`corp_event` 는 어휘에 **13유형을 선언해 두고 4유형만 싣는다**(DESIGN §4-2). 유상증자·CB·배당락을
싣는 것이 이 결정의 실체이고, **팩터 로드맵 2차의 E05·E06(자사주·유상증자 적재)과 같은 작업**
이다 — 원자료 실측은 `BLOCKED_FACTORS.md` §4-2 에 이미 있다(`stg_event_piic` 5,538 ·
`stg_event_cvbd_is` 5,386 · `stg_event_tsstk_aq` 1,951).

**임계·필터는 정하지 않는다.** 소비자가 유형과 그날 가격 배수를 보고 고른다. 이 층은 둘 다
싣기만 한다.

### 5. 같이 정리할 것

- **「오염률」이라는 말을 쓰지 않는다.** `n_unadjusted_events` 는 사실이고 이름도 사실이다.
  해석은 이름에 얹지 않는다.
- `adj_factor` 에 **적용일 가격 배수**를 열로 싣는 것을 검토한다(반나절). 소비자가 매번
  가격 표와 조인하지 않아도 되고, 「계수는 없지만 가격은 안 움직였다」를 한 열로 읽는다.
  이것도 판정이 아니라 사실이다.

### 6. 작업량

| 단계 | 내용 | 시간 |
|---|---|---|
| 1 | `corp_event` 에 유상증자·CB 유형 적재(E05·E06 과 동일 작업) | 2~3일 |
| 2 | 배당락 축 연결(`dividend_event` 는 이미 있다) | 반나절 |
| 3 | `adj_factor` 적용일 가격 배수 열 | 반나절 |

### 7. 이 결정이 틀렸을 때 생기는 일

유형을 잘못 붙이면 소비자가 잘못된 사실을 받는다 — 유상증자를 무상증자로 적으면 조정하면
안 될 것을 조정한다. 그래서 **유형 판정은 공시 원천에 직접 근거해야 하고, 근처 공시로
추정한 것은 그 사실을 basis 컬럼에 적어야 한다**(이 층의 basis 규약 그대로).

반대로 유형을 안 붙이고 지금처럼 두면, 소비자는 3,539건을 전부 「모름」으로 받아
**전량 배제하거나 전량 무시하는** 두 극단밖에 못 고른다. 지금이 그 상태다.

---

## 결정 2 — 승인 대기 상수 4개 (유니버스 상태 판정)

### 무엇을 정하는 문제인가

매일 종목마다 "오늘 이 종목은 살아 있는가, 정지 상태인가"를 판정한다. 판정에는
**거래가 없는 날의 이유를 추정하는 규칙**이 들어가고, 그 규칙에 숫자 네 개가 박혀 있다.
네 값 다 "일단 이 값으로 두고 서버 실측 보고 사람이 확정"한 채 남아 있다.

| 상수 | 지금 값 | 우리말 뜻 |
|---|---:|---|
| `no_trade_run_k` | 5 | 이유를 모르는 무거래가 **5거래일 연속**이면 정지로 본다 |
| `corp_action_lookahead_sessions` | 45 | 무거래일 기준 **앞으로 45거래일** 안에 기업행위가 있으면 그 무거래는 기업행위 탓 |
| `corp_action_lookback_sessions` | 5 | 기업행위 **뒤 5거래일**까지도 같은 창으로 본다 |
| `admin_signal_window_sessions` | 5 | 관리종목 지정 공시 **뒤 5거래일** 안의 무거래는 관리종목 탓 |

### 지금 값과 그 근거

- `no_trade_run_k` = 5 : "상장 후 첫 거래까지 걸린 날의 중앙값 4일 + 1"에서 나왔다.
  하루·이틀 안 거래되는 건 비유동 우선주의 일상이라 정지로 보지 않겠다는 뜻이다.
- `corp_action_lookahead_sessions` = 45 : 감자·병합은 **적용일 앞에** 거래가 정지된다는
  성질에서 나왔다. 절단 표본에서 정지 길이 중앙 13.5세션·최대 48세션을 봤다.
- `corp_action_lookback_sessions` = 5 : 조정 배수를 찾을 때 쓰는 뒤쪽 탐색 창과 같은 값.
- `admin_signal_window_sessions` = 5 : 관리종목 "상태"는 몇 달씩 가는데 그 기간 무거래를
  전부 관리종목 탓으로 부르면 비유동과 구분이 사라지므로, **지정 직후 국면만** 그렇게 부른다.

### 선택지별 수치

**① `no_trade_run_k` — 실질 영향이 거의 없다**

| k | 정지 판정 행 | 정지 종목 | `krx.common-stock` 행 | `krx.investable` 행 | **`krx.liquid` 행** | `krx.liquid` 종목 |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 324,828 | 3,074 | 7,827,625 | 7,670,043 | **3,838,889** | 2,903 |
| 2 | 285,227 | 2,687 | 7,830,292 | 7,672,592 | **3,838,889** | 2,903 |
| 3 | 270,539 | 2,590 | 7,830,978 | 7,673,249 | **3,838,889** | 2,903 |
| **5 (현재)** | **257,248** | **2,501** | **7,831,878** | **7,674,102** | **3,838,889** | **2,903** |
| 10 | 245,058 | 2,404 | 7,833,817 | 7,675,914 | **3,838,889** | 2,903 |
| 20 | 237,051 | 2,246 | 7,836,478 | 7,678,416 | **3,838,889** | 2,903 |
| 40 | 232,277 | 2,198 | 7,838,972 | 7,680,806 | **3,838,889** | 2,903 |

**핵심: `krx.liquid` 는 k에 전혀 반응하지 않는다.** 1이든 40이든 3,838,889행·2,903종목으로
같다. 이유는 유동성 정책이 이미 "그날 거래가 없으면 뺀다"는 조건을 따로 갖고 있어서,
k가 건드리는 행(이유를 모르는 무거래)은 어차피 전부 빠지기 때문이다.
`krx.common-stock`·`krx.investable` 만 움직이고, k를 1에서 40까지 다 흔들어도 폭은
**11,347행 = 0.14%** 다.

참고로 이유 없는 무거래의 연속 길이 분포는 1일 39,601행, 2일 14,688행, 3일 8,067행,
5일 3,700행이고 **40일 이상이 31,833행**이다. 두 달 넘게 한 번도 안 거래되는데 공시로는
아무 신호가 없는 종목이 그만큼 있다는 뜻이다.

**② 기업행위 창(앞 45 / 뒤 5) — 근거는 정지 길이 분포에 있다**

기업행위 적용일 **직전에 연속으로 몇 세션이나 거래가 멎어 있었는가**를 사건 3,757건에 대해 쟀다.

| 직전 정지 길이 | 사건 수 | 누적 커버 |
|---|---:|---:|
| 정지 없음(0) | 1,484 | — |
| 1~5세션 | 448 | 448 / 2,147 (20.9%) |
| 6~20세션 | 1,070 | 1,518 (70.7%) |
| 21~45세션 | 184 | **1,702 (79.3%)** |
| 46~60세션 | 18 | 1,720 (80.1%) |
| 61~90세션 | 38 | 1,758 (81.9%) |
| 90세션 초과 | 389 | 2,147 (100%) |

사건 유형별 중앙값 — 액면분할 12 · 병합 15 · KRX 기준가 사건 17 · 감자 2세션.
**45세션은 정상적인 변경상장 정지(중앙 12~17세션)를 충분히 덮는다.** 60으로 늘려도 18건만
더 잡히고, 그 위 389건은 "관리종목·상장폐지 심사로 몇 년째 정지"인 종목이라 어차피 다른
규칙(공시 기반 정지·관리종목)이 이미 걸러 간다.

창을 바꿨을 때 실제로 달라지는 양:

| 앞 창(lookahead) | `corp_action_window` 행 | 종목 | `illiquid` 행 | 정지 판정 행 |
|---:|---:|---:|---:|---:|
| 20 | 20,367 | 960 | 127,098 | 252,428 |
| 30 | 21,832 | 960 | 125,638 | 252,834 |
| **45 (현재)** | **23,087** | **965** | **124,384** | **253,018** |
| 60 | 24,180 | 968 | 123,292 | 253,178 |
| 90 | 26,075 | 972 | 121,397 | 253,576 |

20에서 90까지 다 흔들어도 정지 판정 행은 1,148행(전체의 0.01%) 안에서 움직인다.
뒤 창(lookback)을 0↔20으로 흔들면 2,219행이 움직인다.

**③ `admin_signal_window_sessions` — 사실상 작동하지 않는 상수**

| 창 | `admin` 으로 분류되는 행 |
|---:|---:|
| 1 | 4 |
| **5 (현재)** | **13** |
| 10 | 29 |
| 20 | 58 |
| 60 | 273 |
| 250 | 1,021 |

전체 1,089만 행 중 13행이다. 관리종목 지정 직후에 거래가 멎는 경우가 드물고(대개 정지
공시가 먼저 나서 앞 규칙에 잡힌다), 250세션으로 늘려도 1,021행(0.01%)이다.

### 너무 작으면 / 너무 크면 무슨 일이 나는가

| 상수 | 너무 작으면 | 너무 크면 |
|---|---|---|
| `no_trade_run_k` | 비유동 우선주가 하루 이틀 안 거래됐다고 "정지"로 찍혀, 실제로는 살 수 있었던 종목이 유니버스에서 빠진다 | 몇 달째 거래가 없는 사실상 죽은 종목이 "정상 상장"으로 남아, 백테스트가 사고 팔 수 없는 주식을 사고판다 |
| `corp_action_lookahead_sessions` | 감자·병합 앞 장기 정지가 "이유 모를 비유동"으로 분류돼, 기업행위 때문에 값이 튀는 구간을 못 막는다 | 기업행위 앞 몇 달의 정상적인 비유동 구간까지 통째로 "기업행위 창"이 되어 멀쩡한 관측이 정지로 찍힌다 |
| `corp_action_lookback_sessions` | 변경상장 직후 며칠 더 이어지는 정지를 놓쳐, 재개 직후 가격 급변을 정상 등락으로 읽는다 | 사건 뒤 비유동 구간이 통째로 기업행위 창으로 넘어간다 |
| `admin_signal_window_sessions` | 관리종목 지정 직후 무거래가 "이유 모를 비유동"이 되어 진단이 흐려진다 | 관리종목 상태가 지속되는 몇 달 전체가 `admin` 이 되어, 비유동과 관리종목의 구분이 사라진다 |

### 권고

**네 값 모두 현재 값 그대로 확정한다** (5 / 45 / 5 / 5).

이유:
1. **판단이 필요할 만큼 영향이 크지 않다.** 실전에서 쓰는 유동성 유니버스는 k에 대해
   완전히 무반응이고, 나머지 세 값은 전 구간을 통틀어 0.01~0.15% 범위에서만 움직인다.
   여기서 최적값을 찾는 건 노력 대비 얻는 게 없다.
2. **45는 데이터가 지지한다.** 정상적인 변경상장 정지의 중앙값 12~17세션 대비 3배 여유가
   있고, 사건의 79%를 덮는다. 60으로 올려서 얻는 건 18건이다.
3. **관리종목 창은 유의미하게 만들 수 없다.** 13행짜리 상수를 튜닝하는 건 의미가 없다.
   이 값은 "관리종목 상태 전체를 무거래 이유로 삼지 않는다"는 **원칙을 코드에 박아 두는
   역할**이지 조절 손잡이가 아니다.
4. **다만 `no_trade_run_k` 는 "확정"이 아니라 "확정 보류 후 재검토"로 두는 편이 정직하다.**
   40세션 이상 무거래가 31,833행이나 되는 것은 "거래소 매매거래정지 현황"을 아직 수집하지
   않아 공시 텍스트로 안 잡히는 정지가 `illiquid` 로 남기 때문이다. 그 원천이 들어오면
   이 분포가 통째로 바뀌므로, **그때 다시 본다**는 조건을 붙인다.

### 이 결정이 틀렸을 때 생기는 일

영향 범위가 작아서 **틀려도 손실로 이어지지 않는다.** 최악의 경우 `krx.common-stock`
유니버스에서 1만 행 안팎(0.14%)이 잘못 분류되고, 실전 전략이 쓰는 `krx.liquid` 는
영향이 0이다. 다만 확정을 미루면 baseline 잠금 검사(`check_baseline_lock.py`)가 계속
"승인 대기" 상태를 끌고 가고, 나중에 누군가 근거 없이 값을 바꿔도 아무도 못 알아챈다 —
**확정 자체가 값보다 중요하다.**

---

## 결정 3 — 팩터 준비도 최소 기준(`ready_min`)을 등재할 것인가

### 무엇을 정하는 문제인가

우리는 최종적으로 54개의 팩터(밸류·퀄리티·모멘텀 등 주식을 고르는 지표)를 만들려고 한다.
`factor_readiness` 표는 매 빌드마다 **"지금 이 데이터로 54개 중 몇 개를 실제로 계산할 수
있는가"** 를 한 줄씩 판정한다. 지금은 **35개 계산 가능(ready) · 19개 막힘(blocked)** 이다.

`ready_min` 은 그 숫자의 **하한선**이다. 등재하면 "빌드 결과가 이 숫자보다 적으면 그
빌드를 통째로 폐기한다"는 뜻이 된다. 정할 것은 **그 하한을 지금 걸 것인가**다.

### 무엇을 막는 장치인가 (코드 확인)

`database/src/equity/rules_s20.py` 의 `eg10_factor_readiness` 가 판정한다. 이 게이트는
하한 말고도 여러 검사를 한다 — 54행이 다 있는가, 막힌 팩터에 사유·담당자가 붙어 있는가,
"계산 가능"이라고 표시된 팩터의 **재료 컬럼이 실물 데이터에 실제로 존재하는가**(표가
거짓말하는 것을 막는 검사) 등이다.

`ready_min` 이 미등재면 **그 하한 검사 하나만 건너뛰고 나머지는 전부 그대로 돈다.**
등재하면 검사 하나가 늘어난다: `ready 수 < ready_min` 이면 **FAIL**.

**FAIL 하면 실제로 무슨 일이 나는가** — 빌드는 완성된 결과를 버리고 이전 판을 유지한다.
그런데 `factor_readiness` 는 전량 재빌드 순서(`equity_rebuild_all.sh` 의 `ORDER`)에서
**맨 마지막 28번째**다. 그래서 여기서 실패하면:

- 앞선 27개 표는 이미 새 판으로 교체가 끝났다.
- `factor_readiness` 만 **옛날 판으로 남는다.**
- 결과: "준비도 표는 A를 계산할 수 있다고 말하는데 실제 데이터는 바뀌어 A의 재료가
  사라진 상태" 라는 **불일치**가 생긴다. 되돌리는 자동 장치는 없다.

즉 이 하한은 "나쁜 데이터를 막는 방패"가 아니라 **"준비도 표만 낡게 만드는 브레이크"** 다.

### 등재 vs 미등재

| | 등재 (예: 35) | 미등재 (현재) |
|---|---|---|
| 얻는 것 | 재료가 조용히 사라져 계산 가능 팩터가 줄면 빌드가 멈춘다 | 다른 검사는 그대로 돈다(재료 컬럼 실재 검사 포함) |
| 잃는 것 | 정당한 감소도 빌드를 멈춘다 | 계산 가능 수가 줄어도 자동으로 아무 일도 안 일어난다 |
| 실패 시 상태 | 준비도 표만 낡아 나머지 27표와 어긋난다 | — |
| 감지 경로 | 게이트 FAIL(자동) | 빌드 로그의 "팩터 준비도 35/54 ready" 문구를 사람이 읽어야 함 |

**"정당한 감소"가 실제로 자주 생길 수 있는가** — 그렇다. 지금 데이터 필드 72개 중 16개가
`requires_confirmation`(단위·산출 규칙 미결) 표시를 달고 있고, 이 표시가 하나라도 늘면
그 필드를 쓰는 팩터가 `ready` 에서 `blocked` 로 내려간다. 즉 **"우리가 문제를 더 정직하게
표시할수록 빌드가 깨지는"** 구조다.

또 하나: 35개 ready 중에도 **시작일이 늦은 것들**이 섞여 있다. E01(내부자 순매수)·E02(5%
대량보유 변동)는 2024-08-26부터고, 2016년 이후 시작이 3개, 2021년 이후가 2개다.
"ready"는 "계산 가능"이지 "긴 역사가 있다"가 아니다.

### 권고 — **미등재 유지**

- 지금 하한을 35로 걸면 실측과 하한이 같아져 **여유가 0**이다. 필드 하나에 확인 표시가
  붙는 순간 빌드가 멈춘다.
- 34 이하로 걸면 "회귀 감시"라는 목적 자체가 헐거워진다(35→34 하락은 못 잡는다).
- 실패해도 시스템을 지켜 주지 않고 **준비도 표만 낡게 만든다.** 방패로서 값을 못 한다.
- 막힌 19개 중 5개가 `field_unavailable`(원천 자체가 없음)이다. 키움 외국인 보유
  3컬럼·대차잔고·벤치마크 지수가 들어오면 F02·F04·F05·F08·R04 다섯이 풀려 **ready가 40
  전후로 올라간다.** 그 뒤에 여유를 두고 거는 편이 낫다.

**등재 시점의 조건**: `blocked_reason` 에 `field_unavailable` 이 0이 되는 시점.
그때 실측값에서 2~3 낮춰 등재한다(예: 실측 40이면 `ready_min` 37).

**대신 지금 해 둘 것**: 전량 재빌드 요약(`logs/equity/rebuild_*/summary.tsv`)에 ready 수를
한 열로 남겨, 사람이 눈으로 회귀를 잡을 수 있게 한다. 게이트를 못 걸어도 기록은 남는다.

### 이 결정이 틀렸을 때 생기는 일

- **미등재로 뒀는데 회귀가 났다면**: 어느 날 데이터 원천이 조용히 끊겨 계산 가능 팩터가
  35 → 28로 줄어도 빌드는 통과한다. 백테스트는 그냥 7개 팩터를 못 쓰게 되는데, 팩터층이
  "이 팩터는 원래 없던 것"으로 오해할 수 있다. **데이터 손실이지 데이터 오염은 아니다** —
  잘못된 값이 흘러가는 게 아니라 값이 없어지는 쪽이라, 조용하되 덜 위험하다.
- **등재했는데 값이 빡빡했다면**: 정당한 개선(필드 하나를 더 정직하게 표시)이 빌드를
  멈추고, 27표는 새 판·준비도 표만 옛 판인 어긋난 상태가 남는다. **이쪽이 더 나쁘다** —
  틀린 준비도 표를 보고 팩터층이 없는 재료를 쓰려 든다.

---

## 결정 4 — 컨센서스 두 원천이 12.7% 어긋난다. 어떻게 다룰 것인가

### 무엇을 정하는 문제인가

증권사 애널리스트들의 실적 추정치(컨센서스)를 두 군데서 받는다. 편의상 **wise**(월 단위
수집본)와 **v3**(일 단위 수집본)라고 부른다. 겹치는 구간에서 둘의 값이 같은지 검사하는데,
**87.3%만 일치**한다(5,054 / 5,789). 검사 통과 기준(`v3_wise_match_min`)은 0.65로 아주
느슨하게 잡혀 있어 통과는 하지만, 왜 12.7%가 어긋나는지 아무도 몰랐다.

정할 것은 둘이다. **⑴ 기준을 얼마로 올릴 것인가 ⑵ 두 원천을 계속 나란히 실을 것인가.**

### 지금 값과 그 근거

| 상수 | 값 | 뜻 |
|---|---:|---|
| `v3_wise_value_tol_rel` | 0.001 | 두 값의 차이가 **0.1% 이내**면 "같다"고 본다 |
| `v3_wise_match_min` | 0.65 | 겹침 중 "같다"의 비율이 **65% 이상**이면 통과 |

0.65는 "일단 통과시켜 놓고 나중에 규명한다"는 임시값이다. 근거 기록이 없다.

### 불일치 735건의 원인 — 규명 결과

원천 원장(`stg_consensus_monthly`·`stg_v3_revision_daily`)까지 내려가 하나씩 갈랐다.
우선순위를 두고 배타적으로 분류한 결과다.

| 원인 | 건수 | 비중 | 중앙 편차 | 최대 편차 |
|---|---:|---:|---:|---:|
| ① 주식 수 기준 차이 (분할·병합·무상증자 미반영) | 23 | 3.1% | 80% | **903%** |
| ② v3가 주당 값을 **정수로 반올림**해 저장 | 151 | 20.5% | 0.21% | 10.3% |
| ③ 관측 시점 어긋남 (±15일 안 다른 날 값과는 일치) | 224 | 30.5% | 0.6~1.1% | 46% |
| ④ 미규명 | 337 | 45.8% | 0.4~1.0% | 345% |

지표별로 보면 원인이 더 뚜렷하다. **불일치는 사실상 EPS(주당순이익) 문제다.**

| 지표 | 겹침 | 불일치 | 불일치율 |
|---|---:|---:|---:|
| eps (주당순이익) | 2,908 | 581 | **20.0%** |
| revenue (매출액) | 2,881 | 155 | 5.4% |

**⑴ 저장 정밀도가 첫 번째 원인이다.** v3 원장의 EPS 컬럼은 **정수(원) 저장**이고
(`DECIMAL(8,0)`) 매출액은 0.1억원 단위다(`DECIMAL(10,1)`). wise는 소수 넷째 자리까지
싣는다(`DECIMAL(20,4)`). 즉 v3는 애초에 "1234.56원"을 "1235원"으로만 적을 수 있다.
0.1%라는 허용치는 **v3가 저장할 수 있는 정밀도보다 촘촘하다.** EPS 값이 작을수록
이 문제가 커지는 것도 실측으로 확인된다 — 값이 10 미만이면 불일치율 91%, 100~1,000이면
25%, 10,000 이상이면 18%다.

**⑵ 주식 수 기준 차이가 가장 위험한 원인이다.** 편차가 배수로 딱 떨어지는 23건은
5배·1/5배·2배·10배 같은 정수배로 갈린다. 그 종목들의 최근 기업행위를 확인하면
**전원이 액면분할·병합·무상증자·감자를 했다.**

| 종목 | 관측일 | wise | v3 | 배수 | 최근 기업행위 |
|---|---|---:|---:|---:|---|
| 011930 | 2026-04-30 | 1,033.34 | 103 | 10.0배 | 병합(reverse_split) |
| 446840 | 2026-06-30 | 390.57 | 78 | 5.0배 | 감자·병합 |
| 091810 | 2026-06-30 | −1,739.62 | −348 | 5.0배 | 감자 |
| 036800 | 2026-06-30 | 1,058.00 | 5,290 | 0.2배 | 액면분할 |
| 001230 | 2026-04-30 | 166.00 | 830 | 0.2배 | 감자·분할 |
| 183300 | 2026-06-30 | 1,269.39 | 3,173 | 0.4배 | 무상증자 |

즉 **한쪽은 새 주식 수로 EPS를 다시 계산했고 다른 쪽은 안 했다.** "미규명" 337건 중
편차가 큰 것들도 대부분 같은 종목군이다(036800·183300·356860·327260·091810 등).
기업행위가 있었던 종목만 따로 보면 일치율이 확연히 낮다.

| 최근 기업행위 | 겹침 | 일치율(0.1%) | 일치율(1%) | 일치율(5%) |
|---|---:|---:|---:|---:|
| 있음 | 652 | 81.8% | 89.4% | 92.8% |
| 없음 | 5,137 | 88.0% | 95.8% | 98.8% |

**⑶ 관측 시점**도 실재한다. 어긋난 735건 중 **224건은 ±15일 안의 다른 날 v3 값과 0.1%
이내로 일치한다.** wise는 월말 한 번, v3는 매일 찍는데, 애널리스트 추정치는 수시로
바뀌므로 같은 날짜 라벨이라도 반영 시점이 며칠 다를 수 있다.

**가설 검증 결과 정리** — 원 질문의 세 가설 중,
- "참여 증권사 수(추정 모집단)가 다르다" → **직접 확인 불가**. 두 원천 다 참여 증권사 수를
  싣지 않는다(v3의 `analyst_count` 는 별도 지표로 이번 겹침에 안 들어온다). 다만 "미규명"
  337건의 대부분이 1% 미만 소편차라 이 가설과 모순되지 않는다.
- "관측 시점이 다르다" → **확인됨**, 224건(30.5%).
- "단위·정의 차이" → **부분 확인**. 단위(억원/원) 차이는 없었고, **주당 지표의 주식 수 기준**
  차이가 실체였다(23건 + 미규명 대편차 일부).

### 허용 오차별 일치율 — 결정에 직접 쓰는 표

| 허용 오차 | 전체 일치율 | eps | revenue |
|---:|---:|---:|---:|
| **0.1% (현재)** | **87.29%** | 80.02% | 94.62% |
| 0.2% | 89.76% | — | — |
| 0.5% | 93.11% | 88.79% | 97.47% |
| **1%** | **95.06%** | **91.61%** | **98.54%** |
| 2% | 96.68% | — | — |
| 5% | 98.13% | 96.63% | 99.65% |

허용 오차를 1%로 열어도 배수 오류(2배·5배·10배)나 단위 오류(100배)는 **그대로 다 잡힌다.**
5% 초과로 남는 것은 107건(1.87%)이고 그중 46건이 기업행위 종목이다.

### 반드시 같이 알아야 할 사실 — wise는 과거 시점에 존재하지 않았다

조사 중 별도로 확인된 것이다. `consensus_daily` 의 두 원천을 "언제부터 알 수 있었는가"
(`available_date`) 로 보면:

| 원천 | 행 수 | 관측 월 범위 | **알 수 있게 된 날** |
|---|---:|---|---|
| wise | 119,407 | 2023-09 ~ 2026-09 | **전부 2026-09-01 ~ 09-02** |
| v3 | 25,909 | 2026-04 ~ 2026-08 | 2026-04-03 ~ 2026-09-01 (관측 직후) |

wise 원장은 수집 날짜(`fetched_date`)가 **2026-09-01과 09-02 딱 두 개**다. 3년치를 한 번에
긁어 왔기 때문이다. 시스템은 이를 정직하게 처리해 "2026-09-01 이전에는 이 데이터를 알 수
없었다"고 표시한다. 그 결과:

- **2026-04 이전 시점의 백테스트에는 컨센서스 데이터가 하나도 없다.**
- 2026-04~08 구간에서는 v3만 보인다(소비자 뷰가 "먼저 알 수 있었던 쪽"을 고르는데
  v3의 공개 시점이 항상 이르다).
- 즉 실전에서 **wise는 아직 한 번도 쓰이지 않는다.** 겹침 검사 자체가 "실전에 쓰지 않는
  데이터"와 "쓰는 데이터"를 맞대 보는 셈이다.

### 권고

**⑴ 허용 오차를 0.1% → 1%로 넓히고, 일치율 하한을 0.65 → 0.93으로 올린다.**

| 상수 | 지금 | 권고 | 근거 |
|---|---:|---:|---|
| `v3_wise_value_tol_rel` | 0.001 | **0.01** | v3 EPS는 정수 저장이라 0.1%는 저장 정밀도보다 촘촘하다. 순수 반올림 151건이 여기서 나온다 |
| `v3_wise_match_min` | 0.65 | **0.93** | 1% 허용 시 실측 95.06%. 2%p 여유 |

두 상수 모두 **게이트만 보는 값**이라 재빌드 없이 재판정만으로 반영된다.
겹침 표본이 5개월·5,789건으로 아직 얇으므로 여유를 2%p 이상 둔다.

*(대안)* 허용 오차를 그대로 두고 하한만 0.85로 올리는 방법도 있다(실측 87.29%, 여유 2.3%p).
권하지 않는 이유는 그 경우 게이트가 **반올림 잡음에 지배**되기 때문이다 — v3의 저장
정밀도가 바뀌면 통과 여부가 데이터 품질과 무관하게 흔들린다.

**⑵ 두 원천은 지금처럼 나란히 싣는다(우선순위 고정하지 않는다). 단 두 가지를 명시한다.**

- 이 검사(EG8)의 목적을 **"값이 같은지"가 아니라 "축·단위·부호가 어긋나지 않는지"** 로
  다시 쓴다. 두 원천은 애초에 같은 값을 낼 수 없다 — 반올림 정밀도도, 관측 시점도,
  주당 지표의 주식 수 기준도 다르다. 잡아야 하는 것은 **자릿수·배수·부호 오류**다.
- **주당 지표(EPS)의 주식 수 기준 차이를 알려진 한계로 문서에 못박는다.** 기업행위를 한
  종목에서 EPS 컨센서스는 두 원천이 최대 10배까지 갈린다. 컨센서스 기반 팩터
  (G04~G10)가 이미 전부 `partial_support`(보류) 상태인 것과 같은 뿌리다.

**⑶ 별도 후속으로 올릴 것**: wise 전 구간의 공개 시점이 2026-09-01이라는 사실은
"컨센서스 팩터는 사실상 2026-04부터만 쓸 수 있다"는 뜻이다. 이건 상수 문제가 아니라
**수집 이력의 한계**이고, 소비자 계약 문서에 눈에 띄게 적어야 한다. 지금은 어디에도 없다.

### 이 결정이 틀렸을 때 생기는 일

- **하한을 너무 낮게 두면**(현재 0.65): 어느 날 한쪽 원천의 단위가 바뀌어 값이 100배로
  틀어져도 게이트가 통과한다. 실측 일치율이 87%인데 하한이 65%라 **22%p의 오염을
  흡수할 여유**가 있다는 뜻이다. 컨센서스 기반 팩터(예상 PER, 이익 수정 방향)가 조용히
  뒤집히고, 백테스트는 "실적 전망이 좋아지는 회사를 산다"는 전략을 정확히 반대로 실행한다.
- **하한을 너무 높게 두면**(예: 0.97): 정상적인 반올림·시점 차이만으로 빌드가 멈춘다.
  겹침 표본이 아직 5개월치라 달마다 몇 %p씩 흔들릴 수 있고, 그때마다 컨센서스 표가
  갱신되지 않아 데이터가 낡는다.
- **허용 오차를 지나치게 넓히면**(예: 10%): 실제 사고인 배수 오류(2배)를 못 잡는다.
  1%는 배수 오류를 전부 잡으면서 반올림만 통과시키는 지점이다.

---

## 요약 — 한 장짜리 결정표

| # | 결정 | 상태 | 무엇으로 정해졌나 |
|---|---|---|---|
| **1** | 계수를 못 낸 사건을 어떻게 적을 것인가 | **절반 완료 (09-07)** | 질문이 「어디까지 버릴 것인가」에서 「사건 유형을 어떻게 실을 것인가」로 바뀌었다. **자사주·CB 는 적재 완료**(e1.9.0, 준비도 35→37). **유상증자 537건은 미결** — `stg_event_piic` 에 신주배정기준일이 없고 `ssl_bgd`/`ssl_edd` 가 5.9% 라 효력일을 못 정한다 |
| 2 | 유니버스 상수 4개 | **확정 (09-07)** | 5 / 45 / 5 / 5 그대로. `no_trade_run_k` 는 k=1~40 전 구간에서 `krx.liquid` 가 **완전히 무반응**이고 나머지는 0.01~0.15%. 거래소 정지 원천이 들어오면 재검토 |
| 3 | `factor_readiness.ready_min` | **확정 — 미등재 (09-07)** | 이 표가 빌드 순서 **28번째(마지막)** 라 EG10 이 FAIL 해도 앞선 27표는 이미 교체된 뒤다. 하한을 걸어도 아무것도 되돌리지 못한다 |
| 4 | 컨센서스 일치 기준 | **확정 (09-07)** | 오차 **1%** · 하한 **0.93**. 0.1% 는 v3 의 `DECIMAL(8,0)` 저장 정밀도보다 촘촘해 정상 데이터를 불일치로 잡고 있었다. 서버 실측 일치율 **0.950768** |

반영 커밋: 결정 2·3·4 = `0faf4eb`. 서버 `baseline.json` 은 09-07 갱신 완료
(백업 `baseline.json.bak_s17_tol`), `consensus_daily` 2회 재빌드 EG5a PASS.

### 결정 1 을 실행에 옮길 때 필요한 것

- `corp_event` 가 어휘 13유형 중 **4유형만 싣는다**(DESIGN §4-2). 유상증자·CB·배당락을 싣는 것이
  실체이며 원자료는 이미 실측돼 있다(`BLOCKED_FACTORS.md` §4-2).
- 어댑터 필드 어휘(`EQUITY_FIELD_MAP` §2)에 사건 유형 전달 경로가 필요하다 —
  **지금은 소비자가 `n_unadjusted_events` 도 사건 유형도 볼 수 없다.**
- 임계·필터는 **정하지 않는다.** 소비자가 유형과 그날 가격 배수를 보고 고른다.

---

### 근거 데이터 출처

전부 2026-09-06 서버 커밋판 parquet 직접 조회(`data/equity/<표>/MANIFEST.json` 의
`current_build` → 파티션 경로). 주요 조회 축:

- 결정 1: `adj_factor`(5,733행) × `price_daily`(10,890,251행) × `price_adj_daily` ×
  `security_span` × `universe_daily`. 미조정 사건 꼬리 재계산이 표의 `n_unadjusted_events`
  와 **전건 일치**(불일치 0)함을 먼저 확인한 뒤 필터 시나리오를 셌다.
- 결정 2: `universe_daily`(10,890,251행) × `adj_factor` × `trading_calendar`.
  현재 상수(5/45/5)로 재계산한 결과가 표에 저장된 값과 일치함을 확인한 뒤
  (`corp_action_window` 23,087 · `admin` 13 · `illiquid` 124,384) 상수를 흔들었다.
- 결정 3: `factor_readiness`(54행) · `dataset_profile`(72행) ·
  `factor_readiness/MANIFEST.json` 의 EG10 metrics · `rules_s20.py:352-433`.
- 결정 4: `consensus_daily`(145,316행) × `stg_v3_revision_daily`(63,175행) ×
  `stg_consensus_monthly`(222,499행). 게이트가 쓰는 겹침 정의를 재현해 5,789건 중 736건
  불일치를 확인(게이트 기록 735건과 1건 차이는 값 0 처리 경계 때문이며 결론에 영향 없음).

---

## 결정 5 — 일일 증분 플랜의 결정 10건 (2026-09-09 등재 · 승인 대기)

원장(KRX·키움·KIS·DART)이 2026-08-20 에 멈췄고 매일 갱신하는 체계가 없다. 플랜
`plans/2026-09-09-daily-incremental.md` 가 아래 권고를 전제로 쓰였다. 근거는 서버 실측 조사
`reviews/2026-09-09-daily-findings-{A..E}-*.md`. **바꾸면 해당 페이즈의 작업이 바뀐다.**

| # | 쟁점 | 권고 | 대안 |
|---|---|---|---|
| R1 | 실행 시각 | **사용자 확정(09-09)**: **06:00 수집 체인**(KRX 제외 전 소스 — 다른 플랫폼은 06:00 이면 전날 데이터가 있다는 추정, 키움 시계열은 P0 프로브로 확인) + **08:10 빌드 체인**(KRX → 키움 대조·머지 → stage → equity) | 08:30 단일 체인(초안) · 19:00 당일 배치(KRX 미공표라 불가) |
| R2 | stage 갱신 | **매일 전량 재빌드**(3.8분 + 22.5분) | 영향 테이블만(DART 재개 후 21.5분으로 차이 없음) |
| R3 | equity 갱신 | 매일 전량 + catalog + contract. **날짜 상수 5개를 파생값으로**(규칙 e1.15.0) | 매일 사람이 락 파일 커밋 |
| R4 | DART | `daily_dart.sh` 폐기, `dart_daily.py` 신설(스윕 전량 + 이벤트 corp 만 재호출), **v3 키 폴백 제거** | 기존 스크립트 재등록 |
| R5 | 키움 | `kw_daily.py` 신설, 유니버스 `ka10099` ∪ 직전 유니버스, `dt<=D` 만 머지, 오염 게이트. 앱키 분리는 P0 실측 후 | `backfill_kw.py` 에 `--to` |
| R6 | KIS | credit 만 일일, 사실 중복 차단 저장. 폐지축 4테이블 제외 | KIS 보류 |
| R7 | 휴장일 | v3 `.kis_holidays.json` 복사본, 실패 시 영업일 가정 | `holiday.py` 이식 |
| R8 | 알림 | 텔레그램 `CHAT_ID_LOG`(사용자 지정 09-09), `notify.sh` | n8n · 없음 |
| R9 | GC | 스냅샷 keep=3 빌드 내장(≈60 GB), `_pinned` keep=3, `stage.baseline` 자동 실행 금지 | — |
| R10 | 범위 밖 | 문서층 파싱 증분 · share 크론 · KIS 폐지축 · 키움 ETF/우선주 · DEFECT-B02 과거분 | — |

**승인 방식**: 그대로 가면 "R1~R10 승인" 한 줄이면 된다. 바꿀 항목만 번호로 적어 주면 플랜을 고친다.
→ **2026-09-09 R1~R10 전부 승인.** P0 진행 중.

### R5 후속 — 키움 앱키 추가 발급 (사용자 행동 필요, 2026-09-09 등재)

P0 Task 0.8 실측(`reviews/2026-09-09-p0-task08-appkey-usage.md`): kael-system-v3 가 같은 키움 앱키로 **평균 12,701콜/일**(daily_prices 2콜 + investor_flows 3콜 × 2,534종목, 20:05~20:47 KST)을 쓴다. 우리 일일 증분 10,410 을 얹으면 **23,113 > 검증 안전 한도 20,000**(+15.6%). 시각은 안 겹치지만 총량이 문제라 시각 분리로는 부족하다. 앱키 1개에 활성 토큰 1개라 06:00(우리)·07:00(v3) 재발급이 서로를 무효화해 **8005 가 매일 나고 있다**(1회 재시도로 복구 중) — 분리하면 함께 해소.

| 선택지 | 내용 | 비고 |
|---|---|---|
| **(a) 별도 앱키 발급 (권고)** | quant-ledger 전용 키움 앱키를 발급해 `.env` 에 `KIWOOM_APP_KEY_QL`·`KIWOOM_SECRET_KEY_QL` 로 두고 `api.py` 가 그것을 우선 읽게 한다 | 사용자가 키움 개발자센터에서 발급 |
| (b) v3 콜 축소 | v3 `investor_flows` 3페이지 → 1페이지(5행만 쓴다), −5,068 → 18,043 | v3 저장소 수정, 상목·카엘 합의 필요 |
| (c) 우리 콜 축소 | ka10008 제외 등 | 데이터 손실 |

**P3 의 키움 단계는 (a) 또는 (b) 확정 전에는 켜지 않는다.**

→ **2026-09-09 18:50 사용자 결정: "지금 키움 콜해도 됨".** 공유 앱키로 즉시 갭 메우기를 실행하고 일일 체인의 키움 단계를 켰다(`QL_SKIP_KW` 해제). 검증 한도 20,000 초과(23,000/일)는 사용자가 수용. (a)·(b)는 오류가 실제로 나면 다시 꺼내는 후속 옵션으로 남긴다. 토큰 8005 상호 무효화(06:00 우리 ↔ 07:00 v3)는 그대로이며 양쪽 1회 재시도로 복구 중.

## 결정 6 — 09-10 첫 적재분 검수 후속 3건 (2026-09-10 사용자 결정 완료)

근거는 `reviews/2026-09-10-intake-audit-summary.md` §1(M2·H3·M9). 사용자 답변 원문: "1. 키움에서 데이터는 수정되지 않을거라고 가정하고 새로들어온 데이터들만 원장에 적재하는게 맞다고 봄. 2. 응 그렇게 하는게 맞다고 봄. 3. 응 진행해."

| # | 결정 | 내용 | 반영 |
|---|---|---|---|
| 6-1 | 키움 머지는 **신규 (ticker, dt) 만 적재** | 키움은 과거를 고치지 않는다고 가정. 이미 있는 행은 값이 달라도 덮어쓰지 않고 `changed` 건수만 runlog 에 남긴다(0 이 아니면 가정을 다시 본다). 원장의 `collected_at` = 최초 관측 시각이 보존된다 | `src/daily/kw_daily.py merge_tr` — 09-10 구현·배포 |
| 6-2 | KIS 신용잔고 재수집 판본은 **최초 관측판** | 같은 (ticker, deal_date) 에 값이 다른 판본이 여럿이면 equity `credit_daily` 는 가장 먼저 관측된 행을 쓴다(그날 시점에 볼 수 있던 값). 가격 조정은 `adj_factor` 몫이고 KIS 가격 컬럼은 가격 축이 아니다(STAGE_SPEC) | P5 `credit_daily.sql` 판본 선택 CTE + EG3 metric `n_versions_folded` — **미구현** |
| 6-3 | G3 #3·#4 개정 | #3 "KIS·키움 07:00 전 종료 + 전체 ≤ 90분", #4 "키움 ≤ 유니버스×4 + 100, v3 합산 상한 안" | 플랜 §6 게이트 G3 표 — 09-10 반영 |

같은 자리에서 사용자 요청 하나 더: **"애널리스트가 몇 명 붙어 추정치를 내는지"** 를 데이터 밀도 지표로 쓰고 싶다. 확인 결과 이미 갖고 있다 — 원장 `ws_raw.c1010001`(정적 페이지 cTB15 추정기관수·cTB24 제공처별 의견) → stage `stg_analyst_summary.analyst_count`·`stg_analyst_broker` → equity `opinion_daily.analyst_count`(field_id `consensus.analyst_count`). 후속은 소비자 쪽(팩터 정의에서 `analyst_count` 하한을 두는 것)이지 수집이 아니다.

## 결정 7 — 키움 외국인 보유(ka10008)는 08:10 체인에서 받는다 (2026-09-10 사용자 결정 완료)

- **근거**: 매시 프로브(`src/probe_kw_timing.py`, 09-09 15:05~)가 ka10008 의 T-1 행이 다음 날 06:05~07:05 사이에 한 번 정정되는 것을 잡았다. 06:04 수집분과 18:10 재조회를 대조하니 삼성전자(2,736,410,506 → 2,736,456,466주)·SK하이닉스(+30,660주)가 달랐고 무작위 30종목·대형주 10종목은 같았다. 07:05 이후 18:05 까지 판독 11회 불변. 다른 세 TR(ka10060·ka10014·ka20068)의 T-1 행은 00:05 부터 19회 판독 불변.
- **선택**: `daily_ledger.sh`(06:00)는 `--tr ka10014,ka20068,ka10060`, `daily_build.sh`(08:10)는 KRX 직후 `--fetch --tr ka10008 --not-before 07:10` 뒤 머지. 오염 게이트(b)는 ka10008 호출에서만 판정(`basis='skipped'` 로 06:00 분 통과). incoming 은 자기 TR 만 비운다.
- **대안**: 장 마감 직후 저녁 수집(사용자 제안) — 18:10 실측에서 투자자·공매도·가격은 당일 확정값이 오지만 외국인 보유는 전일 복사본(변동 0)·대차는 전부 0 이라 채택하지 않음. 06:00 전체 유지 — 대형주 외국인 보유가 잠정값으로 굳는다.
- **토큰**: v3 가 07:00 에 키움 토큰을 재발급하므로 08:10 우리 발급이 한 번 더 튕긴다. P0 실측(`reviews/2026-09-09-p0-task08-appkey-usage.md`)대로 양쪽 자동 복구. 09-11 첫 실행에서 8005 흔적 확인.
- **후속**: 09-09 ka10008 행 일부(대형주)가 06:04 잠정값으로 남아 있다. 결정 6-1(신규 행만 적재)로 09-11 08:10 머지가 `changed=` 로 건수만 세므로, 그 수를 본 뒤 09-09 행만 1회 갱신한다.

## 결정 8 — 목표 개정: 당일 저녁 스코어링 (2026-09-10 사용자 확정 · 플랜 v2 `plans/2026-09-11-daily-incremental-v2.md`)

**추가 확정(09-11 14:30, "진행해")**: PR #102 머지 · 페이즈 A 착수 · v3 교체 범위 = 전부 옮기되 잡 하나씩(플랜 v2 §6). **확정(09-10 23:00)**: V2-1 저녁 키움 행 원장 직행·KRX 대조 없음 · V2-2 잠정 가격 축 = 키움 종가(가격 급변 종목은 저녁 제외) · V2-3 WISE 스냅샷 18:05 이동 · V2-4 완료 목표 19:00(상한 19:30), 키움·DART 병렬 · V2-5 프로브는 슬롯 시각 파라미터만(구현은 18:05 기준 즉시). 아래는 제시 당시 기록.

사용자 원문: "kael-system-v3 멀티팩터 모델에 들어가는 데이터들을 빠르게 받아서 멀티팩터 스코어링 크로스섹셔널 모델을 당일 저녁에라도 받기 위함임. … 20시까지 애프터 마켓이 열리니까 그전에 데이터를 받아서 모델을 만들면 제일 베스트임."

**v3 가 쓰는 입력(서버 `~/kael-system-v3` 읽기, 09-10)** — `config.yaml` 가중치: 모멘텀 .30(r1m~r12m ← `daily_prices` adj_close) · 리비전 .30(영업이익·순이익 1w/1m/3m ← `consensus_revision_daily/compare`) · 수급 .20(기관·외국인·연기금 5d/20d 순매수/시총 ← `investor_detail_flows`) · 퀄리티 .10(GPA·ROA·FCF/자산·부채비율·GPA 변화 ← `financial_summary`, std_20d ← 가격) · 밸류 .10(PER·PBR·EV/EBITDA·배당 ← `financial_summary`). v3 체인 `daily_all` 은 **20:05 KST**(stock_master → daily_prices 키움 ka10081 ≈5,068콜 → investor_flows 키움 → consensus WISE → adj_prices → scoring → export), P0 실측으로 20:47 경 종료 — 즉 v3 자체도 20시 전에는 못 낸다. 재무는 별도 크론(dart_incr 18:15·07:45 KST).

**우리 원장의 저녁 가용성(09-10 18:10 실측)** — 키움 투자자 순매수·공매도·가격·거래량: 당일 확정값 있음 / 외국인 보유: 전일 복사본(다음 날 07시 전후 확정) / 대차: 0 / KIS 신용: T+2 저녁 이후 / KRX 공식 시세: T+1 08:00 / DART: 접수 ~18시 마감 / WISE: 일중 갱신(우리 스냅샷은 06:00).

**설계 방향(안)**: ① 18:30 저녁 체인 — 키움 3 TR 당일(T) 행 + DART(T) + WISE 저녁 스냅샷 → ② 19:00 stage·equity 잠정 빌드(`basis=evening` 표식) → ③ 19:30 스코어링(20시 전) → ④ 다음 날 아침 체인이 KRX 공식 시세·외국인 보유·대차·KIS 로 검증·확정. 선행 조사: 프로브에 당일(T) 대상 판독을 추가해 키움 T 행이 몇 시에 확정되는지(15:40? 16:30?) 측정 — 저녁 체인 시작 시각의 근거. 미결: v3 가 자기 수집(키움 5,068콜)을 유지할지 우리 원장을 읽을지(공유 앱키 콜 절감), 모멘텀의 수정주가를 우리 `adj_factor` 로 줄지.

## 결정 9 — 보관 디스크 예산 (2026-09-11 제안, 사용자 결정 대기)

- **상황**: 서버 `/` 466 GB 중 여유 281 GB(09-11 16:30). 원장 5 DB 18.3 GB(dart 6.8·krx 3.9·kiwoom 3.8·kis 3.4·wise 0.4), 스냅샷 24 GB(3판), stage 4.8 GB, equity 9.8 GB, `~/backups` 28 GB(v3 것).
- **페이즈 B·P6 가 그대로 돌 때의 상한**(검수 R4-06 반영): 원장 백업 7일 ≈ 128 GB, 뜨는 중 순간 8세트 ≈ 146 GB + 월 1회 18 GB/월 누적 · 스냅샷 keep=6 = 48 GB(현재 실측 8 GB/판; 플랜 §4 의 상단 17 GB/판이면 102 GB) · `_pinned` 30일 보호(하루 2판 × 1.5 GB) ≤ 90 GB · equity keep 10 ≈ 12 GB · stage 3판 ≈ 5 GB → **≈ 300~355 GB + 월 18 GB 증가**. **여유 281 GB 를 넘는다.** 자동 방어는 두 겹이다 — `backup_raw.sh` 는 여유 < 60 GB 면 뜨기 전에 crit 후 중단(보관 정리는 그 전에 돈다), `daily_report.py` 는 여유 < 50 GB 면 crit. 조용히 차지는 않지만 몇 주 안에 crit 이 온다.
- **선택지**:
  | | 내용 | 절감 |
  |---|---|---|
  | (a) 백업 3일 + 월 1회 (권고) | 원장은 append-only 라 3일이면 사고 복구에 충분. 월 1회는 유지 | −72 GB |
  | (b) `_pinned` 보호 15일 | 추적 창을 절반으로. 월말 영구는 유지 | −45 GB |
  | (c) 스냅샷 keep=4 | 2거래일치. 잠정 vs 확정 대조에는 충분 | −16 GB |
  | (d) 디스크 증설 | 변경 없음 | — |
- **적용 전까지**: 구현값 그대로(백업 7일·`_pinned` 30일·스냅샷 6판). 상한이 여유를 넘으므로 **(a)+(c) 를 즉시 권고**한다 — 합계 ≈ 212~266 GB 로 내려온다. 사용자 결정 전에는 crit 두 겹이 막는다.

