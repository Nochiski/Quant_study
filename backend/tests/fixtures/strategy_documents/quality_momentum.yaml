# P0-01 golden authoring fixture — WORKFLOW 2.2의 완전한 verbose YAML 예시 (schema 1.2).
# identity(strategy_id/revision)는 문서 밖 revision envelope가 소유한다.
# 실행 설정(시장·기간·유니버스·체결·비용·결측)은 문서가 아니라 요청의 environment가 소유한다.
schema_version: "1.2"
title: "퀄리티 모멘텀"
description: ""
eligibility:
  rules: []
factors:
  - factor_id: momentum
    label: "모멘텀"
    direction: high
    weight: 0.6
    graph:
      nodes:
        - kind: field
          node_id: close
          field_id: price.close
        - kind: time_series
          node_id: mom_252
          operator: momentum
          input_node_id: close
          window: 252
      output_node_id: mom_252
portfolio:
  selection_count: 20
  rebalance: monthly
risk:
  max_name_weight: 0.05
parameters: []
