# 문서층 P1 구현 계획 — 보고서 ZIP 을 stage 소스로 (stg_doc_meta · section · correction · parse_log)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** DART 정기보고서 ZIP 171,179건을 한 번 파싱해(프리패스) stage 규약대로 4개 parquet 테이블(`stg_doc_meta`·`stg_doc_section`·`stg_doc_correction`·`stg_doc_parse_log`)을 만들고, 기존 게이트 G0~G9 와 문서 전용 검사(D0·D2·D3·D6·D8·D10·D12)를 통과시킨다.

**Architecture:** `src/stage/parsers_doc.py`(순수 함수: 디코딩 → 정제 5규칙 → expat/관대 파서 → 헤더·목차·정정 첫 장·표 계상 → 4테이블 행) + `src/stage/doc_prepass.py`(스냅샷 `doc_store` 의 `zip_ok=1` 접수번호를 연도 샤드로 나눠 3워커가 파싱, 테이블 × 연도 JSON Lines 캐시 + `summary.json`) + `model.FileSource`(BlobSource 옆의 새 소스 종류) + `build._load_file_source`(캐시 JSONL → `src_all`) + `rules_doc.py`(4테이블 선언). 게이트는 기존 `gates.run_all` 그대로: G1 은 `n_src`= 캐시 행수라 등식이 자동 성립하고, G8 은 프리패스 요약을 `parse_metrics` 로 받는다. 설계 근거는 `docs/DOC_DESIGN.md` v1.1 §2·§3·§4.

**Tech Stack:** Python 3.12 표준 라이브러리(`zipfile`·`re`·`xml.etree`·`html.parser`·`json`·`multiprocessing`·`sqlite3`) + duckdb 1.5.5(기존). 새 의존성 없음. 검증: `.venv/bin/pytest`, `.venv/bin/ruff check`, `.venv/bin/pyright`(워크트리 루트 `.venv`, uv 로 생성됨).

**작업 규약 (매 태스크 공통)**
- 브랜치 `stage/doc-p1`(이미 생성). 커밋은 태스크마다, 메시지는 영문 `feat(doc-stage): …`.
- 실행 위치는 `workspace/dongmin`. 테스트: `cd workspace/dongmin && ../../.venv/bin/python -m pytest tests/<file> -q`. 린트: `../../.venv/bin/ruff check src/stage/<file> tests/<file>` 와 `../../.venv/bin/pyright src/stage/<file> tests/<file>`(둘 다 0 이어야 커밋).
- 모든 파서 출력 값은 `str | None`(VARCHAR) — 캐스팅은 stage 규칙이 한다. 불린은 `"true"/"false"`, 날짜는 `YYYYMMDD`, JSON 은 문자열.
- 실패는 값으로(`ParseMode`·`status`), 예외는 버그·환경 오류에만(`.claude/rules/python.md`). 메시지에 입력값·식별자 포함(`error-messages.md`).
- 산출물 파일을 단언하는 테스트 금지 — 테스트는 테스트 안에서 만든 XML 조각·임시 ZIP·임시 sqlite 만 쓴다(`testing.md`).

---

## 파일 구조

| 파일 | 책임 |
|---|---|
| `src/stage/doc_vocab/__init__.py` | 사전 로더 `load_vocab()` → `DocVocab`(태그 화이트리스트, XBRL ACLASS→stmt/scope, 단위 정규식, 절 코드→section_kind) |
| `src/stage/doc_vocab/tags.json` · `xbrl_aclass.json` · `units.json` · `section_kind.json` | 사전 데이터 (DOC_DESIGN §2.4) |
| `src/stage/parsers_doc.py` | 문서 1개(ZIP 1개)를 4테이블 행으로 — 디코딩·정제·파싱·추출. 순수 함수, I/O 없음 |
| `src/stage/doc_prepass.py` | 스냅샷 `doc_store` → ZIP 파일 → `parsers_doc` → `_tmp/doc/<snapshot>/<table>/year=YYYY.jsonl` + `summary.json`. CLI |
| `src/stage/model.py` | `FileSource` 추가, `TableRule.file_source` 필드 |
| `src/stage/build.py` | `_load_file_source`, `build_table` 분기 한 곳 |
| `src/stage/gates.py` | G0 이 `file_source.required_columns` 를 검사 |
| `src/stage/rules_doc.py` | 4테이블 `TableRule` 선언 + `TABLES` |
| `src/stage/rules.py` | `rules_doc` 등록 |
| `src/stage/doc_checks.py` | 빌드 후 기록형 검사 D6·D8·D12 → JSON 보고 |
| `src/stage/fixtures/stg_doc_meta.json` | G4 골든 픽스처(S26) |
| `tests/test_stage_doc_parsers.py` · `test_stage_doc_prepass.py` · `test_stage_doc_build.py` · `test_stage_doc_checks.py` | 테스트 |

---

### Task 1: 어휘 사전 패키지 `doc_vocab`

**Files:**
- Create: `src/stage/doc_vocab/__init__.py`
- Create: `src/stage/doc_vocab/tags.json`, `xbrl_aclass.json`, `units.json`, `section_kind.json`
- Test: `tests/test_stage_doc_vocab.py`

- [ ] **Step 1: 실패하는 테스트**

```python
"""doc_vocab — 사전 로더 (DOC_DESIGN v1.1 §2.4)."""
from stage import doc_vocab


def test_vocab_loads_41_tags_and_known_xbrl_classes() -> None:
    v = doc_vocab.load_vocab()
    assert len(v.tags) == 41 and "TABLE-GROUP" in v.tags and "SECTION-3" in v.tags
    assert v.xbrl_stmt("{XBRL}BS_S") == ("BS", "S")
    assert v.xbrl_stmt("{XBRL}IS_C1") == ("IS", "C")
    assert v.xbrl_stmt("{XBRL}NT_S_D810005") == ("NT", "S")
    assert v.xbrl_stmt("{XBRL}BS") == ("BS", None)          # 접미 없음 → scope 는 문맥(§1.9)
    assert v.xbrl_stmt("{XBRL}ZZ") == ("?", None)


def test_vocab_unit_scale_and_section_kind() -> None:
    v = doc_vocab.load_vocab()
    assert v.unit_scale("(단위 : 천원)") == ("천원", "1000")
    assert v.unit_scale("(단위:백만원, %)") == ("백만원", "1000000")
    assert v.unit_scale("단위 : 주") == ("주", None)
    assert v.unit_scale("아무 말") is None
    assert v.section_kind("D-0-3-3-0") == "notes" and v.section_kind("L-0-2-4-L1") == "biz"
    assert v.section_kind("D-0-7-0-0") == "shareholder" and v.section_kind("X-9") is None
```

- [ ] **Step 2: 실패 확인**

Run: `cd workspace/dongmin && ../../.venv/bin/python -m pytest tests/test_stage_doc_vocab.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'stage.doc_vocab'`

- [ ] **Step 3: 사전 데이터 4개**

`src/stage/doc_vocab/tags.json` (DOC_DESIGN §1.6 의 41종):
```json
["A", "APPENDIX", "BODY", "COL", "COLGROUP", "COMMENT", "COMPANY-NAME", "CORRECTION", "COVER",
 "COVER-TITLE", "DOCUMENT", "DOCUMENT-HEADER", "DOCUMENT-NAME", "EXTRACTION", "FILENAME",
 "FORMULA-VERSION", "IMAGE", "IMG", "IMG-CAPTION", "INSERTION", "LIBRARY", "LIBRARYLIST", "P",
 "PGBRK", "SECTION-1", "SECTION-2", "SECTION-3", "SPAN", "SUMMARY", "TABLE", "TABLE-GROUP",
 "TBODY", "TD", "TE", "TH", "THEAD", "TITLE", "TOC", "TR", "TU", "WARNING"]
```

`src/stage/doc_vocab/xbrl_aclass.json` — 접두 문자 → stmt. 접미 `_S`/`_C` 는 로더가 판정:
```json
{"BS": "BS", "IS": "IS", "EF": "EF", "CF": "CF", "SA": "SA", "NT": "NT"}
```

`src/stage/doc_vocab/units.json` — 표기 어휘 → 스케일(원 단위 배수, KRW 아닌 것은 null):
```json
{"원": "1", "천원": "1000", "백만원": "1000000", "억원": "100000000", "십억원": "1000000000",
 "조원": "1000000000000", "주": null, "%": null, "명": null, "개": null, "건": null}
```

`src/stage/doc_vocab/section_kind.json` — 코드 접두 → kind (S26·Y510 실측 §1.6·§1.14):
```json
{"D-0-1-": "overview", "D-0-2-": "biz", "L-0-2-": "biz", "D-0-3-3-0": "notes",
 "D-0-3-5-0": "notes", "D-0-3-2-0": "fin", "D-0-3-4-0": "fin", "D-0-11-0-0": "fin_legacy",
 "D-0-3-": "fin_other", "D-0-4-": "mdna", "D-0-5-": "audit", "D-0-6-": "governance",
 "D-0-7-": "shareholder", "D-0-8-": "officer", "D-0-9-": "affiliate", "D-0-10-": "related",
 "D-0-12-": "appendix", "TTL_APPENDIX": "appendix", "TTL_CEO_CERT": "cert", "COVER": "cover",
 "D-3-": "expert"}
```
주의: `D-0-11-0-0` 은 2015-03 이전엔 "재무제표 등", 이후엔 "그 밖의 사항" 이다(§1.2 전환표). 로더는 코드만 매핑하고, 연도 분기는 `parsers_doc` 가 `formula_date < 20150303` 로 판정한다.

- [ ] **Step 4: 로더**

`src/stage/doc_vocab/__init__.py`:
```python
"""문서층 어휘 사전 로더 (DOC_DESIGN v1.1 §2.4) — 사전은 JSON 데이터, 코드는 조회만."""
from __future__ import annotations

import json
import re
from dataclasses import dataclass
from pathlib import Path

_HERE = Path(__file__).parent
_UNIT_RE = re.compile(r"단위\s*[:：]?\s*([^)）,\s]+)")
_XBRL_RE = re.compile(r"^\{XBRL\}([A-Z]{2})(?:_([SC]))?(?:\d|_|$)")


@dataclass(frozen=True)
class DocVocab:
    tags: frozenset[str]
    xbrl_prefix: dict[str, str]
    units: dict[str, str | None]
    section_prefix: tuple[tuple[str, str], ...]     # 긴 접두 우선 정렬

    def xbrl_stmt(self, aclass: str) -> tuple[str, str | None]:
        """`{XBRL}IS_S1` → ("IS", "S"). 접미 없으면 scope None, 모르는 접두는 ("?", None)."""
        m = _XBRL_RE.match(aclass)
        if m is None:
            return "?", None
        return self.xbrl_prefix.get(m.group(1), "?"), m.group(2)

    def unit_scale(self, text: str) -> tuple[str, str | None] | None:
        """'(단위 : 천원)' → ("천원", "1000"). 단위 표기가 없으면 None, 모르는 단위는 (원문, None)."""
        m = _UNIT_RE.search(text)
        if m is None:
            return None
        u = m.group(1)
        return u, self.units.get(u)

    def section_kind(self, code: str) -> str | None:
        for prefix, kind in self.section_prefix:
            if code.startswith(prefix):
                return kind
        return None


def load_vocab(root: Path = _HERE) -> DocVocab:
    tags = json.loads((root / "tags.json").read_text(encoding="utf-8"))
    xbrl = json.loads((root / "xbrl_aclass.json").read_text(encoding="utf-8"))
    units = json.loads((root / "units.json").read_text(encoding="utf-8"))
    kinds = json.loads((root / "section_kind.json").read_text(encoding="utf-8"))
    ordered = tuple(sorted(kinds.items(), key=lambda kv: -len(kv[0])))
    return DocVocab(frozenset(tags), dict(xbrl), dict(units), ordered)
```

- [ ] **Step 5: 통과 확인 + 린트**

Run: `cd workspace/dongmin && ../../.venv/bin/python -m pytest tests/test_stage_doc_vocab.py -q && ../../.venv/bin/ruff check src/stage/doc_vocab tests/test_stage_doc_vocab.py && ../../.venv/bin/pyright src/stage/doc_vocab tests/test_stage_doc_vocab.py`
Expected: `2 passed`, ruff `All checks passed!`, pyright `0 errors`

- [ ] **Step 6: 커밋**

```bash
git add workspace/dongmin/src/stage/doc_vocab workspace/dongmin/tests/test_stage_doc_vocab.py
git commit -m "feat(doc-stage): vocabulary dictionaries and loader (tags, xbrl, units, section kinds)"
```

---

### Task 2: 바이트 디코딩 `decode_bytes`

**Files:**
- Create: `src/stage/parsers_doc.py`
- Test: `tests/test_stage_doc_parsers.py`

- [ ] **Step 1: 실패하는 테스트**

```python
"""parsers_doc — 디코딩·정제·파싱·추출 (DOC_DESIGN v1.1 §1.3~§1.7, §2.2·§2.3)."""
from stage import parsers_doc as pd_


def test_decode_prefers_strict_utf8_then_cp949_then_replace() -> None:
    u = pd_.decode_bytes("<P>매출</P>".encode())
    assert (u.text, u.enc, u.n_repl) == ("<P>매출</P>", "utf-8", 0)
    c = pd_.decode_bytes("<P>매출</P>".encode("cp949"))
    assert (c.text, c.enc, c.n_repl) == ("<P>매출</P>", "cp949", 0)
    bad = pd_.decode_bytes(b"<P>\xff\xfe</P>")
    assert bad.enc == "cp949~" and bad.n_repl >= 1 and bad.text.startswith("<P>")
```

- [ ] **Step 2: 실패 확인**

Run: `cd workspace/dongmin && ../../.venv/bin/python -m pytest tests/test_stage_doc_parsers.py -q -k decode`
Expected: FAIL — `ModuleNotFoundError: No module named 'stage.parsers_doc'`

- [ ] **Step 3: 구현**

`src/stage/parsers_doc.py` (파일 머리 — 이후 태스크가 아래에 함수를 덧붙인다):
```python
"""문서층 파서 — DART 보고서 XML 1건을 stage 행으로 (DOC_DESIGN v1.1 §2.2·§2.3, 실측 §1).

순수 함수만: 바이트 → 텍스트(§1.3) → 정제 5규칙(§2.3) → expat/관대 트리 → 헤더·목차·정정 첫 장·표 계상.
출력 값은 전부 str|None — 캐스팅은 stage 규칙(rules_doc)이 한다. I/O 는 doc_prepass 가 한다.
"""
from __future__ import annotations

import html
import json
import re
import xml.etree.ElementTree as ET
from collections import Counter
from dataclasses import dataclass, field
from enum import Enum
from html.parser import HTMLParser

from .doc_vocab import DocVocab

STD_ENTITIES = frozenset({"amp", "lt", "gt", "quot", "apos"})


@dataclass(frozen=True)
class Decoded:
    text: str
    enc: str            # utf-8 | cp949 | cp949~ (치환 있음)
    n_repl: int


def decode_bytes(raw: bytes) -> Decoded:
    """§1.3 — 선언 무시, 바이트로 판정: utf-8 strict → cp949 strict → cp949 replace(치환 수 기록)."""
    for enc in ("utf-8", "cp949"):
        try:
            return Decoded(raw.decode(enc), enc, 0)
        except UnicodeDecodeError:
            continue
    text = raw.decode("cp949", errors="replace")
    return Decoded(text, "cp949~", text.count("�"))
```

- [ ] **Step 4: 통과 확인**

Run: `cd workspace/dongmin && ../../.venv/bin/python -m pytest tests/test_stage_doc_parsers.py -q -k decode`
Expected: `1 passed`

- [ ] **Step 5: 커밋**

```bash
git add workspace/dongmin/src/stage/parsers_doc.py workspace/dongmin/tests/test_stage_doc_parsers.py
git commit -m "feat(doc-stage): byte-level encoding detection for report XML"
```

---

### Task 3: 정제 5규칙 `sanitize`

**Files:**
- Modify: `src/stage/parsers_doc.py`
- Test: `tests/test_stage_doc_parsers.py`

- [ ] **Step 1: 실패하는 테스트 (규칙마다 하나)**

```python
import pytest
from stage import doc_vocab

V = doc_vocab.load_vocab()


def test_sanitize_cr_entity_becomes_newline_and_other_entities_are_preserved_as_text() -> None:
    s = pd_.sanitize("<P>a&cr;b &nbsp;c</P>", V.tags)
    assert s.text == "<P>a&#10;b [&nbsp;]c</P>"
    assert s.n_ent_other == 1 and s.other_entities == ("nbsp",)


def test_sanitize_escapes_bare_ampersand_but_keeps_standard_entities() -> None:
    s = pd_.sanitize("<P>R&D &amp; A&#10;B</P>", V.tags)
    assert s.text == "<P>R&amp;D &amp; A&#10;B</P>" and s.n_bare_amp == 1


def test_sanitize_escapes_angle_brackets_that_are_not_whitelisted_tags() -> None:
    s = pd_.sanitize("<TD><당기말></TD><P><MS AR 사업></P><P><CP></P>", V.tags)
    assert s.text == "<TD>&lt;당기말></TD><P>&lt;MS AR 사업></P><P>&lt;CP></P>"
    assert s.n_bare_lt == 3 and s.unknown_tags == ("CP", "MS")   # 한글 시작은 토큰이 아니다


def test_sanitize_repairs_doubled_attribute_quote_but_leaves_empty_attributes() -> None:
    s = pd_.sanitize('<TE ENG=""Maximum exposure"><TU AUNIT="" WIDTH="5"/>', V.tags)
    assert s.text == '<TE ENG="Maximum exposure"><TU AUNIT="" WIDTH="5"/>'
    assert s.n_attr_repair == 1


def test_sanitize_strips_control_characters() -> None:
    s = pd_.sanitize("<P>a\x0bb\x1fc</P>", V.tags)
    assert s.text == "<P>abc</P>" and s.n_ctrl == 2


@pytest.mark.parametrize("tag", ["SECTION-1", "TABLE-GROUP", "COVER-TITLE", "P"])
def test_sanitize_keeps_every_whitelisted_tag_untouched(tag: str) -> None:
    s = pd_.sanitize(f"<{tag} A=\"1\">x</{tag}>", V.tags)
    assert s.text == f"<{tag} A=\"1\">x</{tag}>" and s.n_bare_lt == 0
```

- [ ] **Step 2: 실패 확인**

Run: `cd workspace/dongmin && ../../.venv/bin/python -m pytest tests/test_stage_doc_parsers.py -q -k sanitize`
Expected: FAIL — `AttributeError: module 'stage.parsers_doc' has no attribute 'sanitize'`

- [ ] **Step 3: 구현 (parsers_doc.py 에 추가)**

```python
_CTRL_RE = re.compile(r"[\x00-\x08\x0b\x0c\x0e-\x1f]")
_ENT_RE = re.compile(r"&([A-Za-z][\w.-]*);")
_BARE_AMP_RE = re.compile(r"&(?!(?:amp|lt|gt|quot|apos|#\d+|#x[0-9a-fA-F]+);)")
_TOKEN_RE = re.compile(r"<(/?)([A-Za-z][A-Za-z0-9:_.-]*)")     # §1.6 Name 토큰 정의
_ATTR_REPAIR_RE = re.compile(r'=""(?=[^\s"<>])([^"<>=]*)"')


@dataclass(frozen=True)
class Sanitized:
    text: str
    n_ctrl: int
    n_ent_other: int
    n_bare_amp: int
    n_bare_lt: int
    n_attr_repair: int
    unknown_tags: tuple[str, ...]       # 규칙 4 로 강등된 Name 토큰 (정렬, 중복 제거)
    other_entities: tuple[str, ...]     # 규칙 2 의 비표준 엔티티 이름 (cr 제외)


def _tag_regex(tags: frozenset[str]) -> re.Pattern[str]:
    alt = "|".join(sorted(map(re.escape, tags), key=len, reverse=True))
    return re.compile(rf"<(?!/?(?:{alt})(?=[\s/>])|!|\?)")


def sanitize(text: str, tags: frozenset[str]) -> Sanitized:
    """§2.3 정제 5규칙, 순서 고정. 글자는 바꾸지 않는다(§1.12 텍스트 등식이 이를 검증)."""
    t, n_ctrl = _CTRL_RE.subn("", text)
    others: Counter[str] = Counter()

    def ent(m: re.Match[str]) -> str:
        name = m.group(1)
        if name in STD_ENTITIES:
            return m.group(0)
        if name == "cr":
            return "&#10;"
        others[name] += 1
        return f"[&{name};]"

    t = _ENT_RE.sub(ent, t)
    t, n_amp = _BARE_AMP_RE.subn("&amp;", t)
    tag_re = _tag_regex(tags)
    unknown: Counter[str] = Counter()
    for m in tag_re.finditer(t):
        tok = _TOKEN_RE.match(t, m.start())
        if tok is not None:
            unknown[tok.group(2)] += 1
    t, n_lt = tag_re.subn("&lt;", t)
    t, n_attr = _ATTR_REPAIR_RE.subn(r'="\1"', t)
    return Sanitized(t, n_ctrl, sum(others.values()), n_amp, n_lt, n_attr,
                     tuple(sorted(unknown)), tuple(sorted(others)))
```

- [ ] **Step 4: 통과 확인 + 린트**

Run: `cd workspace/dongmin && ../../.venv/bin/python -m pytest tests/test_stage_doc_parsers.py -q && ../../.venv/bin/ruff check src/stage/parsers_doc.py tests/test_stage_doc_parsers.py && ../../.venv/bin/pyright src/stage/parsers_doc.py`
Expected: `10 passed`, 린트 0

- [ ] **Step 5: 커밋**

```bash
git add workspace/dongmin/src/stage/parsers_doc.py workspace/dongmin/tests/test_stage_doc_parsers.py
git commit -m "feat(doc-stage): five sanitize rules with counters and unknown-tag census"
```

---

### Task 4: 트리 파싱 `parse_tree` — expat, 실패 시 관대 파서

**Files:**
- Modify: `src/stage/parsers_doc.py`
- Test: `tests/test_stage_doc_parsers.py`

- [ ] **Step 1: 실패하는 테스트**

```python
DOC_MIN = ('<?xml version="1.0" encoding="utf-8"?><DOCUMENT xsi:noNamespaceSchemaLocation="dart3.xsd"'
           ' xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"><BODY><SECTION-1>'
           '<TITLE ATOC="Y">I. 회사의 개요</TITLE><TABLE><TR><TD>x</TD></TR></TABLE>'
           '</SECTION-1></BODY></DOCUMENT>')


def test_parse_tree_strict_ok_keeps_attributes() -> None:
    r = pd_.parse_tree(DOC_MIN, V.tags)
    assert r.mode is pd_.ParseMode.OK and r.root is not None
    assert r.root.tag == "DOCUMENT" and r.root.find(".//TITLE").attrib["ATOC"] == "Y"


def test_parse_tree_lenient_recovers_mismatched_tags_and_uppercases_attributes() -> None:
    broken = DOC_MIN.replace("<TD>x</TD></TR>", "<TD><SPAN>x</TD></TR>")   # SPAN 미종료
    r = pd_.parse_tree(broken, V.tags)
    assert r.mode is pd_.ParseMode.LENIENT and r.root is not None
    assert r.root.find(".//TITLE").attrib["ATOC"] == "Y"          # 속성명 대문자 유지
    assert "".join(r.root.find(".//TD").itertext()) == "x"


def test_parse_tree_failed_when_no_document_root_or_empty_tree() -> None:
    r = pd_.parse_tree("<BODY><P>x</P>", V.tags)
    assert r.mode is pd_.ParseMode.FAILED and r.root is None and "DOCUMENT" in str(r.error)
    r2 = pd_.parse_tree("<DOCUMENT><BODY></BODY></DOCUMENT", V.tags)   # 잘린 문서, 목차·표 0
    assert r2.mode is pd_.ParseMode.FAILED
```

- [ ] **Step 2: 실패 확인**

Run: `cd workspace/dongmin && ../../.venv/bin/python -m pytest tests/test_stage_doc_parsers.py -q -k parse_tree`
Expected: FAIL — `AttributeError: ... 'parse_tree'`

- [ ] **Step 3: 구현 (parsers_doc.py 에 추가)**

```python
class ParseMode(Enum):
    OK = "ok"
    LENIENT = "lenient"
    FAILED = "failed"
    HTML = "html"


@dataclass
class TreeResult:
    mode: ParseMode
    root: ET.Element | None
    error: str | None = None
    error_ctx: str | None = None
    lenient_unknown_tags: tuple[str, ...] = ()


class _LenientBuilder(HTMLParser):
    """§2.2 lenient — html.parser 로 트리 재구성. 태그명·속성명 대문자화, 화이트리스트 밖 태그는
    요소를 만들지 않고 자식을 부모에 붙인다. 종료 태그는 스택을 거슬러 닫는다."""

    def __init__(self, tags: frozenset[str]) -> None:
        super().__init__(convert_charrefs=True)
        self.tags = tags
        self.root = ET.Element("ROOT")
        self.stack = [self.root]
        self.unknown: Counter[str] = Counter()

    def handle_starttag(self, tag: str, attrs: list[tuple[str, str | None]]) -> None:
        name = tag.upper()
        if name not in self.tags:
            self.unknown[name] += 1
            return
        el = ET.SubElement(self.stack[-1], name, {k.upper(): (v or "") for k, v in attrs})
        self.stack.append(el)

    def handle_startendtag(self, tag: str, attrs: list[tuple[str, str | None]]) -> None:
        self.handle_starttag(tag, attrs)
        self.handle_endtag(tag)

    def handle_endtag(self, tag: str) -> None:
        name = tag.upper()
        for i in range(len(self.stack) - 1, 0, -1):
            if self.stack[i].tag == name:
                del self.stack[i:]
                return

    def handle_data(self, data: str) -> None:
        el = self.stack[-1]
        if len(el):
            el[-1].tail = (el[-1].tail or "") + data
        else:
            el.text = (el.text or "") + data


def _error_ctx(text: str, err: ET.ParseError) -> str:
    m = re.search(r"line (\d+), column (\d+)", str(err))
    if m is None:
        return ""
    ln, col = int(m.group(1)), int(m.group(2))
    lines = text.split("\n")
    if ln - 1 >= len(lines):
        return ""
    s = lines[ln - 1]
    return s[max(0, col - 60):col + 40]


def _has_content(root: ET.Element) -> bool:
    return any(e.attrib.get("ATOC") == "Y" for e in root.iter("TITLE")) or root.find(".//TABLE") is not None


def parse_tree(text: str, tags: frozenset[str]) -> TreeResult:
    """정제된 텍스트 → 트리. expat 성공 = ok, ParseError 면 관대 파서 = lenient, 그것도 안 되면 failed."""
    try:
        root = ET.fromstring(text)
        return TreeResult(ParseMode.OK, root)
    except ET.ParseError as e:
        strict_err, ctx = str(e), _error_ctx(text, e)
    b = _LenientBuilder(tags)
    try:
        b.feed(text)
        b.close()
    except Exception as e:  # noqa: BLE001  # reason: html.parser 예외는 도메인 실패 → failed 상태
        return TreeResult(ParseMode.FAILED, None, f"strict: {strict_err}; lenient: {e!r}", ctx)
    doc = next((c for c in b.root if c.tag == "DOCUMENT"), None)
    if doc is None:
        return TreeResult(ParseMode.FAILED, None, f"strict: {strict_err}; lenient: no DOCUMENT root", ctx)
    if not _has_content(doc):
        return TreeResult(ParseMode.FAILED, None,
                          f"strict: {strict_err}; lenient: empty tree (toc=0, tables=0)", ctx)
    return TreeResult(ParseMode.LENIENT, doc, strict_err, ctx, tuple(sorted(b.unknown)))
```

- [ ] **Step 4: 통과 확인 + 린트**

Run: `cd workspace/dongmin && ../../.venv/bin/python -m pytest tests/test_stage_doc_parsers.py -q && ../../.venv/bin/ruff check src/stage/parsers_doc.py tests/test_stage_doc_parsers.py && ../../.venv/bin/pyright src/stage/parsers_doc.py tests/test_stage_doc_parsers.py`
Expected: `13 passed`, 린트 0 (`r.root.find(...)` 의 Optional 접근이 pyright 에 걸리면 테스트에서 `assert r.root is not None` 뒤 지역 변수로 받는다)

- [ ] **Step 5: 커밋**

```bash
git add workspace/dongmin/src/stage/parsers_doc.py workspace/dongmin/tests/test_stage_doc_parsers.py
git commit -m "feat(doc-stage): strict expat parse with lenient html.parser fallback"
```

---

### Task 5: 멤버 역할 · HTML 판정 · 세대 · 헤더 필드

**Files:**
- Modify: `src/stage/parsers_doc.py`
- Test: `tests/test_stage_doc_parsers.py`

- [ ] **Step 1: 실패하는 테스트**

```python
G1_HEAD = ('<?xml version="1.0" encoding="utf-8"?><DOCUMENT xmlns:xsi="x" '
           'xsi:noNamespaceSchemaLocation="dart3.xsd"><DOCUMENT-HEADER AEXT-CLASS="Y">'
           '<DOCUMENT-NAME ACODE="11011">사업보고서</DOCUMENT-NAME>'
           '<FORMULA-VERSION ADATE="20200113">3.9</FORMULA-VERSION>'
           '<COMPANY-NAME AREGCIK="00138057">써니전자(주)</COMPANY-NAME>'
           '<SUMMARY><EXTRACTION ACODE="LINK_FLAG" AFEATURE="BOTH">C</EXTRACTION>'
           '<EXTRACTION ACODE="FIN_TYPE" AFEATURE="BOTH">A</EXTRACTION></SUMMARY>'
           '</DOCUMENT-HEADER><BODY><COVER><COVER-TITLE ATOC="Y">사 업 보 고 서</COVER-TITLE>'
           '<TABLE-GROUP ACLASS="COVER"><TABLE ACLASS="EXTRACTION"><TR>'
           '<TU AUNIT="PERIODFROM" AUNITVALUE="20190101">2019년 01월 01일</TU>'
           '<TU AUNIT="PERIODTO" AUNITVALUE="20191231">2019년 12월 31일</TU>'
           '<TE ACODE="CRP_NM">써니전자 주식회사</TE></TR></TABLE></TABLE-GROUP></COVER>'
           '{BODY}</BODY></DOCUMENT>')
G3_HEAD = ('<?xml version="1.0" encoding="utf-8"?><DOCUMENT xmlns:xsi="x" '
           'xsi:noNamespaceSchemaLocation="dart4.xsd"><DOCUMENT-NAME ACODE="11013">분기보고서'
           '</DOCUMENT-NAME><FORMULA-VERSION SUBVER="1" ADATE="20231229">5.5</FORMULA-VERSION>'
           '<COMPANY-NAME AREGCIK="00151605">씨비아이(주)</COMPANY-NAME><SUMMARY>'
           '<EXTRACTION ACODE="IFRS_YN" AFEATURE="BOTH">Y</EXTRACTION></SUMMARY>'
           '<BODY ATOCID="61"><COVER><COVER-TITLE ATOC="Y" ATOCID="1">분기보고서</COVER-TITLE></COVER>'
           '{BODY}</BODY></DOCUMENT>')


def _root(xml: str) -> ET.Element:
    r = pd_.parse_tree(pd_.sanitize(xml, V.tags).text, V.tags)
    assert r.root is not None, r.error
    return r.root


def test_member_role_and_html_detection() -> None:
    assert pd_.member_role("20200327001141.xml", "20200327001141") == "main"
    assert pd_.member_role("/20160329000533_00760.xml", "20160329000533") == "audit"
    assert pd_.member_role("20200327001141_00761.xml", "20200327001141") == "audit_cons"
    assert pd_.member_role("readme.txt", "20200327001141") == "other"
    assert pd_.is_html("<html>\n <head>") and not pd_.is_html(G1_HEAD)


def test_generation_and_header_fields_g1() -> None:
    root = _root(G1_HEAD.replace("{BODY}", ""))
    assert pd_.generation(root) == ("dart3_hdr", "dart3.xsd")
    h = pd_.header_fields(root)
    assert h["doc_acode"] == "11011" and h["doc_name"] == "사업보고서"
    assert h["formula_version"] == "3.9" and h["formula_date"] == "20200113"
    assert h["corp_cik"] == "00138057" and h["company_name_doc"] == "써니전자(주)"
    assert json.loads(h["summary"] or "{}") == {"LINK_FLAG": "C", "FIN_TYPE": "A"}
    assert h["period_from"] == "20190101" and h["period_to"] == "20191231"
    assert json.loads(h["cover"] or "{}") == {"CRP_NM": "써니전자 주식회사"}


def test_generation_and_header_fields_g3_flat() -> None:
    root = _root(G3_HEAD.replace("{BODY}", ""))
    assert pd_.generation(root) == ("dart4", "dart4.xsd")
    h = pd_.header_fields(root)
    assert h["doc_acode"] == "11013" and h["formula_version"] == "5.5"
    assert h["period_from"] is None and h["cover"] == "{}"
```

(`import json`, `import xml.etree.ElementTree as ET` 를 테스트 파일 상단에 추가.)

- [ ] **Step 2: 실패 확인**

Run: `cd workspace/dongmin && ../../.venv/bin/python -m pytest tests/test_stage_doc_parsers.py -q -k "member_role or generation"`
Expected: FAIL — `AttributeError: ... 'member_role'`

- [ ] **Step 3: 구현 (parsers_doc.py 에 추가)**

```python
_HTML_RE = re.compile(r"<html[\s>]", re.I)
_MEMBER_RE = re.compile(r"^(\d{14})(?:_(\d{5}))?\.xml$")
_ROLE_BY_SUFFIX = {None: "main", "00760": "audit", "00761": "audit_cons"}
_XSI_LOC = "{http://www.w3.org/2001/XMLSchema-instance}noNamespaceSchemaLocation"


def member_role(member_name: str, rcept_no: str) -> str:
    """ZIP 멤버 basename 으로 역할 판정 (§1.4 — 2015~2016 은 `/` 접두, 순서는 무작위)."""
    m = _MEMBER_RE.match(member_name.rsplit("/", 1)[-1])
    if m is None or m.group(1) != rcept_no:
        return "other"
    return _ROLE_BY_SUFFIX.get(m.group(2), "other")


def is_html(text: str) -> bool:
    return _HTML_RE.search(text[:4000]) is not None


def text_of(el: ET.Element | None) -> str:
    return "" if el is None else " ".join("".join(el.itertext()).split())


def generation(root: ET.Element) -> tuple[str, str | None]:
    """(gen, xsd) — dart4 / dart3_hdr / dart3_flat / unknown (§1.2)."""
    xsd = root.attrib.get(_XSI_LOC) or root.attrib.get("xsi:noNamespaceSchemaLocation")
    if xsd == "dart4.xsd":
        return "dart4", xsd
    if xsd == "dart3.xsd":
        return ("dart3_hdr" if root.find("DOCUMENT-HEADER") is not None else "dart3_flat"), xsd
    return "unknown", xsd


def header_fields(root: ET.Element) -> dict[str, str | None]:
    """헤더·SUMMARY·표지 TE/TU 를 평탄한 dict 로 (§1.6·§1.10). 없는 값은 None, JSON 은 문자열."""
    hdr = root.find("DOCUMENT-HEADER")
    src = hdr if hdr is not None else root
    name = src.find("DOCUMENT-NAME")
    fv = src.find("FORMULA-VERSION")
    comp = src.find("COMPANY-NAME")
    summary = {e.attrib.get("ACODE", ""): text_of(e) for e in src.iter("EXTRACTION")
               if src.find("SUMMARY") is not None}
    cover = root.find("BODY/COVER")
    cover_te = {e.attrib["ACODE"]: text_of(e) for e in (cover.iter("TE") if cover is not None else ())
                if e.attrib.get("ACODE")}
    period: dict[str, str | None] = {"PERIODFROM": None, "PERIODTO": None}
    for tu in root.iter("TU"):
        unit = tu.attrib.get("AUNIT")
        if unit in period and period[unit] is None:
            period[unit] = tu.attrib.get("AUNITVALUE") or None
    return {
        "doc_acode": name.attrib.get("ACODE") if name is not None else None,
        "doc_name": text_of(name) or None,
        "formula_version": text_of(fv) or None,
        "formula_date": (fv.attrib.get("ADATE") if fv is not None else None) or None,
        "corp_cik": comp.attrib.get("AREGCIK") if comp is not None else None,
        "company_name_doc": text_of(comp) or None,
        "summary": json.dumps(summary, ensure_ascii=False),
        "cover": json.dumps(cover_te, ensure_ascii=False),
        "period_from": period["PERIODFROM"],
        "period_to": period["PERIODTO"],
    }
```

- [ ] **Step 4: 통과 확인 + 린트** — `16 passed`, 린트 0
- [ ] **Step 5: 커밋** — `git commit -m "feat(doc-stage): member role, generation and header field extraction"`

---

### Task 6: 목차 행 `toc_rows` (stg_doc_section)

**Files:**
- Modify: `src/stage/parsers_doc.py`
- Test: `tests/test_stage_doc_parsers.py`

- [ ] **Step 1: 실패하는 테스트**

```python
BODY_TOC = ('<SECTION-1><TITLE ATOC="Y">I. 회사의 개요</TITLE>'
            '<SECTION-2><TITLE ATOC="Y" AASSOCNOTE="D-0-1-1-0">1. 회사의 개요</TITLE>'
            '<P>당사는&cr;회사입니다.</P><TABLE><TR><TD>a</TD></TR></TABLE></SECTION-2></SECTION-1>'
            '<SECTION-1><TITLE ATOC="Y" AASSOCNOTE="D-0-3-0-0">III. 재무에 관한 사항</TITLE>'
            '<INSERTION><LIBRARY><SECTION-2><TITLE ATOC="Y" AASSOCNOTE="D-0-3-2-0">2. 연결재무제표</TITLE>'
            '<TABLE-GROUP ACLASS="{XBRL}BS"><TABLE><TR><TD>h</TD></TR></TABLE>'
            '<TABLE><TR><TD>b</TD></TR></TABLE></TABLE-GROUP></SECTION-2></LIBRARY></INSERTION>'
            '</SECTION-1>')


def test_toc_rows_carry_code_kind_level_path_and_counts() -> None:
    root = _root(G1_HEAD.replace("{BODY}", BODY_TOC))
    rows = pd_.toc_rows(root, V, formula_date="20200113")
    assert [r["title"] for r in rows] == ["사 업 보 고 서", "I. 회사의 개요", "1. 회사의 개요",
                                          "III. 재무에 관한 사항", "2. 연결재무제표"]
    assert [r["ordinal"] for r in rows] == ["0", "1", "2", "3", "4"]
    r1, r2, r4 = rows[1], rows[2], rows[4]
    assert r1["section_code"] is None and r1["level"] == "1" and r1["section_kind"] == "overview"
    assert r2["section_code"] == "D-0-1-1-0" and r2["level"] == "2" and r2["path"] == "SECTION-1/SECTION-2"
    assert r2["n_free_tables"] == "1" and r2["n_paragraphs"] == "1" and int(r2["n_chars"]) > 0
    assert r4["path"] == "SECTION-1/INSERTION/LIBRARY/SECTION-2" and r4["n_xbrl_groups"] == "1"
    assert r4["n_free_tables"] == "0" and r4["section_kind"] == "fin"


def test_toc_rows_legacy_d_0_11_is_fin_before_2015_03_and_other_after() -> None:
    body = '<SECTION-1><TITLE ATOC="Y" AASSOCNOTE="D-0-11-0-0">XI. 재무제표 등</TITLE></SECTION-1>'
    root = _root(G1_HEAD.replace("{BODY}", body))
    assert pd_.toc_rows(root, V, formula_date="20140217")[1]["section_kind"] == "fin_legacy"
    assert pd_.toc_rows(root, V, formula_date="20150303")[1]["section_kind"] == "other"
```

- [ ] **Step 2: 실패 확인** — `AttributeError: ... 'toc_rows'`

- [ ] **Step 3: 구현 (parsers_doc.py 에 추가)**

```python
_SECTION_TAGS = ("SECTION-1", "SECTION-2", "SECTION-3")


def _parent_map(root: ET.Element) -> dict[ET.Element, ET.Element]:
    return {c: p for p in root.iter() for c in p}


def _path(el: ET.Element, parents: dict[ET.Element, ET.Element]) -> str:
    out: list[str] = []
    cur: ET.Element | None = parents.get(el)
    while cur is not None and cur.tag != "BODY":
        out.append(cur.tag)
        cur = parents.get(cur)
    return "/".join(reversed(out))


def _xbrl_tables(root: ET.Element) -> set[ET.Element]:
    return {t for tg in root.iter("TABLE-GROUP") if tg.attrib.get("ACLASS", "").startswith("{XBRL}")
            for t in tg.iter("TABLE")}


def _section_kind(code: str | None, vocab: DocVocab, formula_date: str | None) -> str | None:
    if code is None:
        return None
    kind = vocab.section_kind(code)
    if code == "D-0-11-0-0" and kind == "fin_legacy" and (formula_date or "") >= "20150303":
        return "other"                      # 2015-03 서식 개정 뒤 XI 은 "그 밖의 사항" (§1.2)
    return kind


def _counts(scope: ET.Element, xbrl: set[ET.Element]) -> dict[str, str]:
    n_form = n_x = n_free = n_p = 0
    for e in scope.iter():
        if e.tag == "TABLE":
            if e.attrib.get("ACLASS") == "EXTRACTION":
                n_form += 1
            elif e in xbrl:
                n_x += 1
            else:
                n_free += 1
        elif e.tag == "P" and text_of(e):
            n_p += 1
    groups = sum(1 for tg in scope.iter("TABLE-GROUP") if tg.attrib.get("ACLASS", "").startswith("{XBRL}"))
    return {"n_form_tables": str(n_form), "n_xbrl_groups": str(groups), "n_free_tables": str(n_free),
            "n_paragraphs": str(n_p), "n_chars": str(len(text_of(scope)))}


def toc_rows(root: ET.Element, vocab: DocVocab, formula_date: str | None) -> list[dict[str, str | None]]:
    """`TITLE[@ATOC="Y"]`(표지 COVER-TITLE 포함) 마다 1행 — stg_doc_section (§3.2)."""
    parents = _parent_map(root)
    xbrl = _xbrl_tables(root)
    rows: list[dict[str, str | None]] = []
    ordinal = 0
    for i, el in enumerate(root.iter()):
        if el.tag not in ("TITLE", "COVER-TITLE") or el.attrib.get("ATOC") != "Y":
            continue
        scope = parents.get(el)
        if scope is None:
            continue
        code = el.attrib.get("AASSOCNOTE") or ("COVER" if el.tag == "COVER-TITLE" else None)
        level = next((str(k + 1) for k, t in enumerate(_SECTION_TAGS) if scope.tag == t), None)
        kind = _section_kind(code, vocab, formula_date)
        if kind is None and level == "1":
            kind = next((_section_kind(c.attrib.get("AASSOCNOTE"), vocab, formula_date)
                         for c in scope.iter("TITLE") if c is not el and c.attrib.get("AASSOCNOTE")), None)
        row: dict[str, str | None] = {"ordinal": str(ordinal), "section_code": code, "section_kind": kind,
                                      "atocid": el.attrib.get("ATOCID"), "level": level,
                                      "title": text_of(el), "path": _path(el, parents),
                                      "elem_start": str(i)}
        row.update(_counts(scope, xbrl))
        rows.append(row)
        ordinal += 1
    return rows
```

- [ ] **Step 4: 통과 확인 + 린트** — `18 passed`
- [ ] **Step 5: 커밋** — `git commit -m "feat(doc-stage): table-of-contents rows with section codes and per-section table counts"`

---

### Task 7: 정정 첫 장 `correction_page`

**Files:**
- Modify: `src/stage/parsers_doc.py`
- Test: `tests/test_stage_doc_parsers.py`

- [ ] **Step 1: 실패하는 테스트**

```python
CORR_G1 = ('<INSERTION><LIBRARY><CORRECTION><TITLE ATOC="Y">정 정 신 고 (보고)</TITLE>'
           '<TABLE><TR><TD>2020년 03월 30일</TD></TR></TABLE>'
           '<P>1. 정정대상 공시서류 : 사업보고서</P><P>2. 정정대상 공시서류의 최초제출일 : 2020년 03월 30일</P>'
           '<P>3. 정정사유 : 당기순이익 기재 오류</P><P>4. 정정사항</P>'
           '<TABLE><THEAD><TR><TH>항  목</TH><TH>정정사유</TH><TH>정 정 전</TH><TH>정 정 후</TH></TR></THEAD>'
           '<TBODY><TR><TD>포괄손익계산서</TD><TD>기재 오류</TD><TD>266,596,241</TD><TD>266,596,242</TD></TR>'
           '</TBODY></TABLE></CORRECTION></LIBRARY></INSERTION>')
CORR_G3_TD = ('<LIBRARY><CORRECTION><TITLE ATOC="Y">정 정 신 고 (보고)</TITLE>'
              '<TABLE><TR><TD>2025 년 3 월 14 일</TD></TR><TR><TD>1. 정정대상 공시서류 : 사업보고서</TD></TR>'
              '<TR><TD>2. 정정대상 공시서류의 최초제출일 : 2025 년 3 월 12 일</TD></TR>'
              '<TR><TD>3. 정정사항</TD></TR></TABLE>'
              '<TABLE><TR><TH>항 목</TH><TH>정정요구ㆍ명령관련 여부</TH><TH>정정사유</TH><TH>정 정 전</TH>'
              '<TH>정 정 후</TH></TR><TR><TD>V. 감사의견</TD><TD>-</TD><TD>착오</TD><TD>해당없음</TD>'
              '<TD>있음</TD></TR></TABLE></CORRECTION></LIBRARY>')


def test_correction_page_from_p_lines_g1() -> None:
    root = _root(G1_HEAD.replace("{BODY}", CORR_G1))
    c = pd_.correction_page(root)
    assert c is not None
    assert c["target_raw"] == "사업보고서" and c["filed_raw"] == "2020년 03월 30일"
    assert c["filed_date"] == "20200330" and c["filed_date_status"] == "parsed"
    assert c["reason_raw"] == "당기순이익 기재 오류" and c["n_items"] == "1"
    items = json.loads(c["items"] or "[]")
    assert items[0] == {"항목": "포괄손익계산서", "정정사유": "기재 오류", "정정 전": "266,596,241",
                        "정정 후": "266,596,242"}


def test_correction_page_from_td_cells_g3_and_anchored_date() -> None:
    root = _root(G3_HEAD.replace("{BODY}", CORR_G3_TD))
    c = pd_.correction_page(root)
    assert c is not None and c["filed_date"] == "20250312"     # 첫 날짜(제출일 표)가 아니라 앵커 뒤
    assert c["reason_raw"] is None and json.loads(c["items"] or "[]")[0]["정정요구ㆍ명령관련 여부"] == "-"


def test_correction_page_absent_and_unparsed_date() -> None:
    assert pd_.correction_page(_root(G1_HEAD.replace("{BODY}", ""))) is None
    bad = CORR_G1.replace("2020년 03월 30일</P>", "미상</P>", 1)
    c = pd_.correction_page(_root(G1_HEAD.replace("{BODY}", bad)))
    assert c is not None and c["filed_date"] is None and c["filed_date_status"] == "unparsed"
```

- [ ] **Step 2: 실패 확인** — `AttributeError: ... 'correction_page'`

- [ ] **Step 3: 구현 (parsers_doc.py 에 추가)**

```python
_FILED_ANCHOR_RE = re.compile(r"최초\s*제출일\s*[:：]?\s*(.{0,40})")
_DATE_RE = re.compile(r"(\d{4})\s*[년.\-/월]\s*(\d{1,2})\s*[년월.\-/]\s*(\d{1,2})")
_TARGET_RE = re.compile(r"1\.\s*정정대상\s*공시서류\s*[:：]?\s*(.*?)(?=\s*2\.\s*정정대상|$)")
_REASON_RE = re.compile(r"3\.\s*정정사유\s*[:：]?\s*(.*?)(?=\s*4\.\s*정정사항|$)")
_ITEM_HDR_RE = re.compile(r"^항\s*목$")


def _norm_cell(s: str) -> str:
    return " ".join(s.split())


def correction_page(root: ET.Element) -> dict[str, str | None] | None:
    """`CORRECTION` 첫 장 → 원문 필드 (§1.7·§3.3). 요소 평문에 앵커 정규식 — P 줄·TD 셀 모두 잡는다."""
    corr = root.find(".//CORRECTION")
    if corr is None:
        return None
    flat = text_of(corr)
    tgt = _TARGET_RE.search(flat)
    rsn = _REASON_RE.search(flat)
    filed_raw: str | None = None
    filed: str | None = None
    m = _FILED_ANCHOR_RE.search(flat)
    if m is not None:
        d = _DATE_RE.search(m.group(1))
        filed_raw = (m.group(1) if d is None else m.group(1)[:d.end()]).strip() or None
        if d is not None:
            filed = f"{int(d.group(1)):04d}{int(d.group(2)):02d}{int(d.group(3)):02d}"
    items: list[dict[str, str]] = []
    for tbl in corr.iter("TABLE"):
        trs = list(tbl.iter("TR"))
        if not trs:
            continue
        hdr = [_norm_cell(text_of(c)) for c in trs[0]]
        if not any(_ITEM_HDR_RE.match(h.replace(" ", "")) for h in hdr):
            continue
        hdr = [h.replace("항  목", "항목").replace("항 목", "항목") for h in hdr]
        for tr in trs[1:]:
            cells = [_norm_cell(text_of(c)) for c in tr]
            items.append({hdr[i]: cells[i] for i in range(min(len(hdr), len(cells)))})
        break
    return {
        "page_found": "true",
        "target_raw": (tgt.group(1).strip() or None) if tgt else None,
        "filed_raw": filed_raw,
        "filed_date": filed,
        "filed_date_status": "parsed" if filed else "unparsed",
        "reason_raw": (rsn.group(1).strip() or None) if rsn else None,
        "n_items": str(len(items)),
        "items": json.dumps(items, ensure_ascii=False),
        "corr_text_chars": str(len(flat)),
    }
```

- [ ] **Step 4: 통과 확인 + 린트** — `21 passed`
- [ ] **Step 5: 커밋** — `git commit -m "feat(doc-stage): correction front-page extraction with anchored filing-date regex"`

---

### Task 8: 표 계상 `table_census` (meta 의 표 수·XBRL/양식 이름)

**Files:**
- Modify: `src/stage/parsers_doc.py`
- Test: `tests/test_stage_doc_parsers.py`

- [ ] **Step 1: 실패하는 테스트**

```python
BODY_TABLES = ('<SECTION-1><TITLE ATOC="Y">I</TITLE>'
               '<TABLE-GROUP ACLASS="TOT_STK"><TABLE ACLASS="EXTRACTION"><TR><TE ACODE="ISU_STK1">1</TE>'
               '</TR></TABLE></TABLE-GROUP><TABLE><TR><TD>free</TD></TR></TABLE>'
               '<TABLE-GROUP ACLASS="{XBRL}BS_S"><TABLE><TR><TD>h</TD></TR></TABLE>'
               '<TABLE><TR><TD>b</TD></TR></TABLE></TABLE-GROUP>'
               '<TABLE-GROUP ACLASS="{XBRL}IS_S1"><TABLE><TR><TD>h</TD></TR></TABLE></TABLE-GROUP>'
               '</SECTION-1>')


def test_table_census_counts_by_kind_and_lists_class_names() -> None:
    root = _root(G1_HEAD.replace("{BODY}", BODY_TABLES))
    c = pd_.table_census(root)
    assert c["n_tables"] == "6" and c["n_form_groups"] == "2"        # TOT_STK + COVER(헤더 픽스처)
    assert c["n_xbrl_groups"] == "2" and c["n_free_tables"] == "1"
    assert json.loads(c["xbrl_aclass"] or "[]") == ["{XBRL}BS_S", "{XBRL}IS_S1"]
    assert json.loads(c["form_aclass"] or "[]") == ["COVER", "TOT_STK"]
    assert c["n_elements"].isdigit() and c["toc_n"] == "2"           # COVER-TITLE + I
```

- [ ] **Step 2: 실패 확인** — `AttributeError: ... 'table_census'`

- [ ] **Step 3: 구현 (parsers_doc.py 에 추가)**

```python
def table_census(root: ET.Element) -> dict[str, str | None]:
    """문서 단위 표 계상 — stg_doc_meta 의 n_* 열 (§1.14 분류: 서식표 / XBRL / 자유표)."""
    xbrl = _xbrl_tables(root)
    xbrl_cls: list[str] = []
    form_cls: list[str] = []
    for tg in root.iter("TABLE-GROUP"):
        cls = tg.attrib.get("ACLASS", "")
        (xbrl_cls if cls.startswith("{XBRL}") else form_cls).append(cls)
    n_tables = n_form = n_free = 0
    for t in root.iter("TABLE"):
        n_tables += 1
        if t.attrib.get("ACLASS") == "EXTRACTION":
            n_form += 1
        elif t not in xbrl:
            n_free += 1
    return {
        "n_elements": str(sum(1 for _ in root.iter())),
        "n_tables": str(n_tables),
        "n_form_groups": str(len(form_cls)),
        "n_xbrl_groups": str(len(xbrl_cls)),
        "n_free_tables": str(n_free),
        "toc_n": str(sum(1 for e in root.iter() if e.tag in ("TITLE", "COVER-TITLE")
                         and e.attrib.get("ATOC") == "Y")),
        "xbrl_aclass": json.dumps(sorted(xbrl_cls), ensure_ascii=False),
        "form_aclass": json.dumps(sorted(set(form_cls)), ensure_ascii=False),
    }
```

- [ ] **Step 4: 통과 확인 + 린트** — `22 passed`
- [ ] **Step 5: 커밋** — `git commit -m "feat(doc-stage): per-document table census (form, xbrl, free)"`

---

### Task 9: `parse_member` · `parse_zip` — 4테이블 행 조립 + 텍스트 등식(D10)

**Files:**
- Modify: `src/stage/parsers_doc.py`
- Test: `tests/test_stage_doc_parsers.py`

- [ ] **Step 1: 실패하는 테스트**

```python
import io
import zipfile


def _zip(members: dict[str, bytes]) -> bytes:
    buf = io.BytesIO()
    with zipfile.ZipFile(buf, "w") as zf:
        for n, b in members.items():
            zf.writestr(n, b)
    return buf.getvalue()


FULL_G1 = G1_HEAD.replace("{BODY}", CORR_G1 + BODY_TOC + BODY_TABLES)
AUDIT_XML = ('<?xml version="1.0" encoding="utf-8"?><DOCUMENT xmlns:xsi="x" '
             'xsi:noNamespaceSchemaLocation="dart3.xsd"><DOCUMENT-HEADER>'
             '<DOCUMENT-NAME ACODE="00760">감사보고서</DOCUMENT-NAME><SUMMARY>'
             '<EXTRACTION ACODE="AUDIT_CIK">00260055</EXTRACTION></SUMMARY></DOCUMENT-HEADER>'
             '<BODY><SECTION-1><TITLE ATOC="Y">독립된 감사인의 감사보고서</TITLE></SECTION-1></BODY></DOCUMENT>')
HTML_DOC = "<html>\n <head><title>동화기업/주식분할결정</title></head><body><table></table></body></html>"


def test_parse_zip_emits_rows_for_four_tables_from_three_members() -> None:
    z = _zip({"20200327001141_00760.xml": AUDIT_XML.encode("cp949"),
              "20200327001141.xml": FULL_G1.encode("cp949")})
    out = pd_.parse_zip("20200327001141", z, "2026-09-01T03:20:50", V)
    assert [m["member_role"] for m in out.meta] == ["audit", "main"]
    main = out.meta[1]
    assert main["parse_mode"] == "ok" and main["byte_enc"] == "cp949" and main["gen"] == "dart3_hdr"
    assert main["has_correction_page"] == "true" and main["n_xbrl_groups"] == "2"
    assert main["fetched_at"] == "2026-09-01T03:20:50" and main["text_equal"] == "true"
    assert len(out.section) == 1 + 7                 # 감사 1 + 본문(COVER·I·1·III·2·I·… 표지 포함)
    assert out.correction and out.correction[0]["rcept_no"] == "20200327001141"
    assert out.correction[0]["member_name"] == "20200327001141.xml"
    assert [r["parse_mode"] for r in out.parse_log] == ["ok", "ok"]
    assert json.loads(out.meta[0]["summary"] or "{}") == {"AUDIT_CIK": "00260055"}


def test_parse_zip_html_member_and_bad_zip_are_values_not_exceptions() -> None:
    out = pd_.parse_zip("20240311901285", _zip({"20240311901285.xml": HTML_DOC.encode()}),
                        "2026-09-01T00:00:00", V)
    m = out.meta[0]
    assert m["parse_mode"] == "html" and m["format"] == "html" and m["gen"] == "html"
    assert out.section == [] and out.correction == []
    bad = pd_.parse_zip("20240311901285", b"not a zip", "2026-09-01T00:00:00", V)
    assert bad.meta == [] and bad.parse_log[0]["parse_mode"] == "failed"
    assert "BadZipFile" in str(bad.parse_log[0]["error"])
```

- [ ] **Step 2: 실패 확인** — `AttributeError: ... 'parse_zip'`

- [ ] **Step 3: 구현 (parsers_doc.py 에 추가)**

```python
import io      # ← 파일 상단 import 블록으로 옮긴다 (ruff I001)
import zipfile  # ← 동일

_COMMENT_RE = re.compile(r"<!--.*?-->", re.S)
_PI_RE = re.compile(r"<\?.*?\?>", re.S)
_ANY_TAG_RE = re.compile(r"<[^>]+>")


@dataclass
class DocRows:
    meta: list[dict[str, str | None]] = field(default_factory=list)
    section: list[dict[str, str | None]] = field(default_factory=list)
    correction: list[dict[str, str | None]] = field(default_factory=list)
    parse_log: list[dict[str, str | None]] = field(default_factory=list)


def text_equal(sanitized: str, root: ET.Element, lenient: bool) -> bool:
    """§1.12 텍스트 등식 — 트리 텍스트 = 정제 원문에서 주석·선언·태그를 지우고 unescape 한 문자열."""
    stripped = _ANY_TAG_RE.sub("", _PI_RE.sub("", _COMMENT_RE.sub("", sanitized)))
    expected = html.unescape(stripped)
    got = "".join(root.itertext())
    if lenient:
        return " ".join(expected.split()) == " ".join(got.split())
    return expected.strip() == got.strip()


def parse_member(rcept_no: str, member_name: str, raw: bytes, fetched_at: str,
                 vocab: DocVocab) -> DocRows:
    """멤버 1개 → 행. 실패는 parse_mode=failed 행으로 남고 예외는 올리지 않는다."""
    out = DocRows()
    role = member_role(member_name, rcept_no)
    dec = decode_bytes(raw)
    base: dict[str, str | None] = {"rcept_no": rcept_no, "member_name": member_name,
                                   "member_role": role, "fetched_at": fetched_at,
                                   "byte_enc": dec.enc, "n_repl": str(dec.n_repl),
                                   "bytes_xml": str(len(raw))}
    if is_html(dec.text):
        n_tab = len(re.findall(r"<table[\s>]", dec.text, re.I))
        out.meta.append({**base, "format": "html", "gen": "html", "xsd": None, "parse_mode": "html",
                         "n_tables": str(n_tab), "text_equal": None, **_empty_meta()})
        out.parse_log.append({**base, "parse_mode": "html", "error": None, "error_ctx": None,
                              "unknown_tags": "[]", "lenient_unknown_tags": "[]",
                              **_zero_counters()})
        return out
    san = sanitize(dec.text, vocab.tags)
    counters = {"n_ctrl": str(san.n_ctrl), "n_ent_other": str(san.n_ent_other),
                "n_bare_amp": str(san.n_bare_amp), "n_bare_lt": str(san.n_bare_lt),
                "n_attr_repair": str(san.n_attr_repair)}
    tree = parse_tree(san.text, vocab.tags)
    log: dict[str, str | None] = {**base, "parse_mode": tree.mode.value, "error": tree.error,
                                  "error_ctx": tree.error_ctx,
                                  "unknown_tags": json.dumps(list(san.unknown_tags), ensure_ascii=False),
                                  "lenient_unknown_tags": json.dumps(list(tree.lenient_unknown_tags),
                                                                     ensure_ascii=False), **counters}
    out.parse_log.append(log)
    if tree.root is None:
        return out
    root = tree.root
    gen, xsd = generation(root)
    hdr = header_fields(root)
    census = table_census(root)
    corr = correction_page(root)
    eq = text_equal(san.text, root, tree.mode is ParseMode.LENIENT)
    out.meta.append({**base, "format": "xml", "gen": gen, "xsd": xsd, "parse_mode": tree.mode.value,
                     **hdr, **census, **counters,
                     "has_correction_page": "true" if corr else "false",
                     "text_equal": "true" if eq else "false"})
    for r in toc_rows(root, vocab, hdr["formula_date"]):
        out.section.append({"rcept_no": rcept_no, "member_name": member_name, "fetched_at": fetched_at, **r})
    if corr is not None:
        out.correction.append({"rcept_no": rcept_no, "member_name": member_name,
                               "fetched_at": fetched_at, **corr})
    return out


def _empty_meta() -> dict[str, str | None]:
    keys = ("doc_acode", "doc_name", "formula_version", "formula_date", "corp_cik", "company_name_doc",
            "summary", "cover", "period_from", "period_to", "n_elements", "n_form_groups",
            "n_xbrl_groups", "n_free_tables", "toc_n", "xbrl_aclass", "form_aclass",
            "has_correction_page")
    return dict.fromkeys(keys) | _zero_counters()


def _zero_counters() -> dict[str, str | None]:
    return {"n_ctrl": "0", "n_ent_other": "0", "n_bare_amp": "0", "n_bare_lt": "0", "n_attr_repair": "0"}


def parse_zip(rcept_no: str, zip_bytes: bytes, fetched_at: str, vocab: DocVocab) -> DocRows:
    """ZIP 1개 → 멤버별 행. ZIP 자체가 깨졌으면 parse_log 에 failed 1행(meta 없음)."""
    out = DocRows()
    try:
        zf = zipfile.ZipFile(io.BytesIO(zip_bytes))
    except zipfile.BadZipFile as e:
        out.parse_log.append({"rcept_no": rcept_no, "member_name": "", "member_role": "other",
                              "fetched_at": fetched_at, "byte_enc": None, "n_repl": None,
                              "bytes_xml": str(len(zip_bytes)), "parse_mode": "failed",
                              "error": f"BadZipFile: rcept_no={rcept_no} bytes={len(zip_bytes)} {e!r}",
                              "error_ctx": None, "unknown_tags": "[]", "lenient_unknown_tags": "[]",
                              **_zero_counters()})
        return out
    with zf:
        for name in zf.namelist():
            r = parse_member(rcept_no, name, zf.read(name), fetched_at, vocab)
            out.meta += r.meta
            out.section += r.section
            out.correction += r.correction
            out.parse_log += r.parse_log
    return out
```

- [ ] **Step 4: 통과 확인 + 린트** — `24 passed`, 린트 0. 컬럼 집합이 행마다 같은지(meta 는 html 행도 같은 키) `assert set(out.meta[0]) == set(out.meta[1])` 를 테스트에 한 줄 추가한다 — read_json 은 열 집합이 고정이어야 한다.
- [ ] **Step 5: 커밋** — `git commit -m "feat(doc-stage): assemble meta/section/correction/parse_log rows per ZIP with text-equality check"`

---

### Task 10: 프리패스 `doc_prepass` — 스냅샷 doc_store → JSONL 캐시 + summary.json + CLI

**Files:**
- Create: `src/stage/doc_prepass.py`
- Test: `tests/test_stage_doc_prepass.py`

- [ ] **Step 1: 실패하는 테스트**

```python
"""doc_prepass — ZIP 1회 파싱 → 테이블 × 연도 JSON Lines 캐시 (DOC_DESIGN v1.1 §2.2·§2.5 ①)."""
import io
import json
import sqlite3
import zipfile
from pathlib import Path

from stage import doc_prepass

from test_stage_doc_parsers import AUDIT_XML, FULL_G1, HTML_DOC


def _zip(members: dict[str, bytes]) -> bytes:
    buf = io.BytesIO()
    with zipfile.ZipFile(buf, "w") as zf:
        for n, b in members.items():
            zf.writestr(n, b)
    return buf.getvalue()


def _setup(tmp_path: Path) -> tuple[Path, Path]:
    docs = tmp_path / "documents"
    (docs / "2020").mkdir(parents=True)
    (docs / "2024").mkdir()
    (docs / "2020" / "20200327001141.zip").write_bytes(
        _zip({"20200327001141_00760.xml": AUDIT_XML.encode("cp949"),
              "20200327001141.xml": FULL_G1.encode("cp949")}))
    (docs / "2024" / "20240311901285.zip").write_bytes(
        _zip({"20240311901285.xml": HTML_DOC.encode()}))
    (docs / "2024" / "20240399999999.zip").write_bytes(b"broken")
    db = tmp_path / "dart.db"
    con = sqlite3.connect(db)
    con.execute("CREATE TABLE doc_store (rcept_no TEXT PRIMARY KEY, bytes INTEGER, sha256 TEXT,"
                " n_files INTEGER, zip_ok INTEGER, http_status TEXT, fetched_at TEXT)")
    con.executemany("INSERT INTO doc_store VALUES (?,?,?,?,?,?,?)", [
        ("20200327001141", 10, "s", 2, 1, "000", "2026-09-01T03:20:50"),
        ("20240311901285", 10, "s", 1, 1, "000", "2026-09-02T00:00:00"),
        ("20240399999999", 6, "s", 0, 1, "000", "2026-09-02T00:00:00"),
        ("20240100000000", 0, "s", 0, 0, "014", "2026-09-02T00:00:00"),   # zip_ok=0 → 제외
    ])
    con.commit()
    con.close()
    return docs, db


def test_prepass_writes_jsonl_per_table_and_year_with_summary(tmp_path: Path) -> None:
    docs, db = _setup(tmp_path)
    s = doc_prepass.run(db, docs, tmp_path / "cache", snapshot_id="snap_t", workers=1)
    assert s.n_docs == 3 and s.tables["stg_doc_meta"] == 3 and s.tables["stg_doc_correction"] == 1
    assert s.tables["stg_doc_parse_log"] == 4 and s.modes == {"ok": 2, "html": 1, "failed": 1}
    assert s.status == "ok"
    meta_2020 = (tmp_path / "cache" / "snap_t" / "stg_doc_meta" / "year=2020.jsonl").read_text().splitlines()
    assert len(meta_2020) == 2 and json.loads(meta_2020[1])["member_role"] == "main"
    summary = json.loads((tmp_path / "cache" / "snap_t" / "summary.json").read_text())
    assert summary["tables"]["stg_doc_section"] == 8 and summary["d10_text_equal_violations"] == 0


def test_prepass_fails_d3_when_an_unknown_tag_is_frequent(tmp_path: Path) -> None:
    docs, db = _setup(tmp_path)
    p = docs / "2020" / "20200327001141.zip"
    xml = FULL_G1.replace("<P>당사는", "<NEWTAG>x</NEWTAG><P>당사는")
    p.write_bytes(_zip({"20200327001141.xml": xml.encode("cp949")}))
    s = doc_prepass.run(db, docs, tmp_path / "cache", snapshot_id="snap_t", workers=1, d3_limit=0.0)
    assert s.status == "gate_failed" and "NEWTAG" in s.detail
```

- [ ] **Step 2: 실패 확인** — `ModuleNotFoundError: No module named 'stage.doc_prepass'`

- [ ] **Step 3: 구현**

`src/stage/doc_prepass.py`:
```python
"""문서 프리패스 — 스냅샷 doc_store(zip_ok=1) 의 ZIP 을 한 번만 파싱해 테이블 × 연도 JSON Lines 로 캐시.

산출: <cache_root>/<snapshot_id>/<table>/year=YYYY.jsonl + summary.json (DOC_DESIGN v1.1 §2.2·§2.5 ①).
게이트 D0(ZIP 열기 실패 0)·D2(파싱 실패율)·D3(어휘 폐쇄)·D10(텍스트 등식) 을 여기서 판정한다.
CLI: PYTHONPATH=src python -m stage.doc_prepass --snapshot-id S [--workers 3] [--years 2020,2021]
"""
from __future__ import annotations

import argparse
import json
import os
import sqlite3
import sys
from collections import Counter
from concurrent.futures import ProcessPoolExecutor
from dataclasses import asdict, dataclass, field
from pathlib import Path

from . import parsers_doc
from .doc_vocab import DocVocab, load_vocab

TABLES: tuple[str, ...] = ("stg_doc_meta", "stg_doc_section", "stg_doc_correction",
                           "stg_doc_parse_log")
_ROWS_ATTR = {"stg_doc_meta": "meta", "stg_doc_section": "section",
              "stg_doc_correction": "correction", "stg_doc_parse_log": "parse_log"}


@dataclass
class ShardResult:
    year: str
    n_docs: int
    n_zip_missing: int
    modes: Counter[str]
    tables: Counter[str]
    unknown_tags: Counter[str]          # 문서 빈도
    other_entities: Counter[str]
    text_equal_violations: int
    missing: list[str] = field(default_factory=list)


@dataclass
class PrepassSummary:
    snapshot_id: str
    n_docs: int
    modes: dict[str, int]
    tables: dict[str, int]
    d0_zip_missing: int
    d2_failed_ratio: float
    d3_unknown_tags_over_limit: list[str]
    d10_text_equal_violations: int
    status: str                          # ok | gate_failed
    detail: str


def _docs_by_year(db: Path, years: set[str] | None) -> dict[str, list[tuple[str, str]]]:
    con = sqlite3.connect(f"file:{db}?mode=ro", uri=True)
    try:
        rows = con.execute("SELECT rcept_no, fetched_at FROM doc_store WHERE zip_ok = 1 "
                           "ORDER BY rcept_no").fetchall()
    finally:
        con.close()
    out: dict[str, list[tuple[str, str]]] = {}
    for rno, at in rows:
        y = str(rno)[:4]
        if years is None or y in years:
            out.setdefault(y, []).append((str(rno), str(at)))
    return out


def _run_shard(args: tuple[str, list[tuple[str, str]], Path, Path]) -> ShardResult:
    year, docs, docs_dir, cache = args
    vocab: DocVocab = load_vocab()
    res = ShardResult(year, 0, 0, Counter(), Counter(), Counter(), Counter(), 0)
    files = {t: open(cache / t / f"year={year}.jsonl", "w", encoding="utf-8") for t in TABLES}
    try:
        for rno, fetched_at in docs:
            path = docs_dir / year / f"{rno}.zip"
            if not path.exists():
                res.n_zip_missing += 1
                res.missing.append(rno)
                continue
            rows = parsers_doc.parse_zip(rno, path.read_bytes(), fetched_at, vocab)
            res.n_docs += 1
            for t in TABLES:
                rs: list[dict[str, str | None]] = getattr(rows, _ROWS_ATTR[t])
                res.tables[t] += len(rs)
                for r in rs:
                    files[t].write(json.dumps(r, ensure_ascii=False))
                    files[t].write("\n")
            for r in rows.parse_log:
                res.modes[str(r["parse_mode"])] += 1
                for tag in json.loads(r["unknown_tags"] or "[]"):
                    res.unknown_tags[tag] += 1
            for m in rows.meta:
                if m.get("text_equal") == "false":
                    res.text_equal_violations += 1
                for ent in json.loads(m.get("other_entities") or "[]"):
                    res.other_entities[ent] += 1
    finally:
        for f in files.values():
            f.close()
    return res


def run(db: Path, docs_dir: Path, cache_root: Path, snapshot_id: str, workers: int = 3,
        years: set[str] | None = None, d2_limit: float = 0.005, d3_limit: float = 0.01,
        limit_per_year: int | None = None) -> PrepassSummary:
    """프리패스 실행. 결과는 값(PrepassSummary.status)으로, 예외는 환경 오류에만."""
    cache = cache_root / snapshot_id
    for t in TABLES:
        (cache / t).mkdir(parents=True, exist_ok=True)
    by_year = _docs_by_year(db, years)
    if limit_per_year is not None:
        by_year = {y: d[:limit_per_year] for y, d in by_year.items()}
    jobs = [(y, d, docs_dir, cache) for y, d in sorted(by_year.items())]
    results: list[ShardResult] = []
    if workers <= 1:
        results = [_run_shard(j) for j in jobs]
    else:
        with ProcessPoolExecutor(max_workers=workers) as ex:
            results = list(ex.map(_run_shard, jobs))
    modes: Counter[str] = Counter()
    tables: Counter[str] = Counter()
    unknown: Counter[str] = Counter()
    n_docs = n_missing = n_eq = 0
    for r in results:
        modes += r.modes
        tables += r.tables
        unknown += r.unknown_tags
        n_docs += r.n_docs
        n_missing += r.n_zip_missing
        n_eq += r.text_equal_violations
    n_xml = modes["ok"] + modes["lenient"] + modes["failed"]
    d2 = modes["failed"] / n_xml if n_xml else 0.0
    d3 = sorted(t for t, c in unknown.items() if n_docs and c / n_docs >= d3_limit)
    reasons: list[str] = []
    if n_missing:
        reasons.append(f"D0 zip missing={n_missing} (first: {results[0].missing[:3] if results else []})")
    if d2 > d2_limit:
        reasons.append(f"D2 failed ratio {d2:.4f} > {d2_limit}")
    if d3:
        reasons.append(f"D3 unknown tags over {d3_limit:.0%} of docs: {d3[:10]}")
    if n_eq:
        reasons.append(f"D10 text equality violations={n_eq}")
    summary = PrepassSummary(snapshot_id, n_docs, dict(modes), dict(tables), n_missing, d2, d3, n_eq,
                             "gate_failed" if reasons else "ok", "; ".join(reasons) or "ok")
    (cache / "summary.json").write_text(
        json.dumps({**asdict(summary), "unknown_tags_top": unknown.most_common(50)},
                   ensure_ascii=False, indent=1), encoding="utf-8")
    return summary


def main(argv: list[str] | None = None) -> int:
    base = Path(os.environ.get("QL_HOME") or Path(__file__).resolve().parents[2])
    ap = argparse.ArgumentParser(description=__doc__)
    ap.add_argument("--snapshot-id", required=True)
    ap.add_argument("--snapshot-root", type=Path, default=base / "data" / "snapshots")
    ap.add_argument("--docs-dir", type=Path, default=base / "data" / "raw" / "documents")
    ap.add_argument("--stage-root", type=Path, default=base / "data" / "stage")
    ap.add_argument("--workers", type=int, default=3)
    ap.add_argument("--years", help="쉼표 구분 연도 부분집합")
    ap.add_argument("--limit-per-year", type=int)
    a = ap.parse_args(argv)
    db = a.snapshot_root / a.snapshot_id / "dart.db"
    s = run(db, a.docs_dir, a.stage_root / "_tmp" / "doc", a.snapshot_id, a.workers,
            set(a.years.split(",")) if a.years else None, limit_per_year=a.limit_per_year)
    print(f"{s.status} snapshot={s.snapshot_id} docs={s.n_docs:,} modes={s.modes} tables={s.tables}")
    print(f"  D0 missing={s.d0_zip_missing} D2 failed={s.d2_failed_ratio:.5f} "
          f"D3 over-limit={s.d3_unknown_tags_over_limit} D10 violations={s.d10_text_equal_violations}")
    if s.status != "ok":
        print(f"  {s.detail}")
    return 0 if s.status == "ok" else 1


if __name__ == "__main__":
    sys.exit(main())
```
`parse_member` 의 meta 행에 `other_entities`(JSON 문자열, `san.other_entities`)를 추가한다(Task 9 의 `_empty_meta` 키 목록에도).

- [ ] **Step 4: 통과 확인 + 린트** — `2 passed`, 린트 0. `Counter[str]` 필드의 dataclass 는 pyright basic 에서 통과한다(`from collections import Counter`).
- [ ] **Step 5: 커밋** — `git commit -m "feat(doc-stage): parse pre-pass writing per-table year-sharded JSONL cache with D0/D2/D3/D10 verdicts"`

---

### Task 11: `FileSource` · `_load_file_source` · G0 · `rules_doc` 4테이블 · 레지스트리 · 빌드 테스트

**Files:**
- Modify: `src/stage/model.py` (BlobSource 아래에 `FileSource`, `TableRule.file_source`)
- Modify: `src/stage/build.py:364-436` (`_load_file_source` 추가, `build_table` 분기)
- Modify: `src/stage/gates.py:75-99` (G0 required_columns)
- Create: `src/stage/rules_doc.py`
- Modify: `src/stage/rules.py` (등록)
- Test: `tests/test_stage_doc_build.py`

- [ ] **Step 1: 실패하는 테스트**

```python
"""stg_doc_* 4테이블 빌드 — 프리패스 캐시(JSONL) → FileSource → stage parquet (DOC_DESIGN v1.1 §3·§4)."""
import sqlite3
from pathlib import Path

import duckdb
from stage import build, doc_prepass, gates, rules, snapshot

from test_stage_doc_prepass import _setup

DISC_COLS = ["row_hash", "corp_cls", "corp_code", "corp_name", "flr_nm", "rcept_dt", "rcept_no",
             "report_nm", "rm", "stock_code", "req_bgn_de", "req_end_de", "req_page_no", "dup_seq",
             "collected_at"]


def _write_disclosure(db: Path) -> None:
    con = sqlite3.connect(db)
    con.execute(f"CREATE TABLE dart_disclosure ({', '.join(c + ' TEXT' for c in DISC_COLS)})")
    con.executemany(f"INSERT INTO dart_disclosure VALUES ({','.join('?' * len(DISC_COLS))})", [
        ("h1", "Y", "00138057", "써니전자", "써니전자", "20200327", "20200327001141",
         "사업보고서 (2019.12)", "", "004770", "20200101", "20201231", "0001", "0", "2026-08-30T10:00:00"),
        ("h2", "K", "00151605", "동화기업", "동화기업", "20240311", "20240311901285",
         "주식분할결정", "", "025900", "20240101", "20241231", "0001", "0", "2026-08-30T10:00:00"),
    ])
    con.commit()
    con.close()


def _prepared(tmp_path: Path) -> tuple[snapshot.Snapshot, Path]:
    docs, db = _setup(tmp_path)          # doc_store + ZIP 3개 (ok 2 · html 1 · broken 1)
    _write_disclosure(db)
    snap = snapshot.make_snapshot({"dart": db}, tmp_path / "snapshots", snapshot_id="snap_doc")
    stage_root = tmp_path / "stage"
    r0 = build.build_table(rules.RULES["stg_rcept_dt_map"], snap, stage_root)
    assert r0.ok
    s = doc_prepass.run(snap.dir / "dart.db", docs, stage_root / "_tmp" / "doc", "snap_doc", workers=1)
    assert s.status == "ok"
    return snap, stage_root


def _read(stage_root: Path, r: build.BuildResult) -> duckdb.DuckDBPyConnection:
    con = duckdb.connect()
    glob = str(stage_root / r.table / f"v={r.build_id}" / "year=*" / "*.parquet")
    con.execute(f"CREATE VIEW t AS SELECT * FROM read_parquet('{glob}', hive_partitioning=true)")
    return con


def test_rules_doc_declarations() -> None:
    for name in ("stg_doc_meta", "stg_doc_section", "stg_doc_correction", "stg_doc_parse_log"):
        rule = rules.RULES[name]
        assert rule.file_source is not None and rule.blob_source is None
        assert rule.partition_class == "receipt_axis" and rule.partition_expr == "substr(rcept_no, 1, 4)"
        assert rule.observed_src == "fetched_at" and rule.write_mode == "append_only"
        assert rule.available.kind == "lookup" and rule.available.table == "stg_rcept_dt_map"
        assert rule.fanout == 1 and rule.key_unique


def test_build_meta_from_prepass_cache_passes_gates_and_derives_available_date(tmp_path: Path) -> None:
    snap, stage_root = _prepared(tmp_path)
    r = build.build_table(rules.RULES["stg_doc_meta"], snap, stage_root)
    assert r.ok, [g.as_dict() for g in r.gates if g.status is gates.GateStatus.FAIL]
    assert r.n_rows == 3 and r.n_src == 3 and r.n_reject == 0
    con = _read(stage_root, r)
    row = con.execute("SELECT gen, parse_mode, available_date, available_basis, observed_date, "
                      "n_xbrl_groups, has_correction_page, period_from FROM t "
                      "WHERE rcept_no='20200327001141' AND member_role='main'").fetchone()
    assert row is not None
    assert row[0] == "dart3_hdr" and row[1] == "ok" and str(row[2]) == "2020-03-27" and row[3] == "derived"
    assert str(row[4]) == "2026-09-01" and row[5] == 2 and row[6] is True and str(row[7]) == "2019-01-01"
    g8 = next(g for g in r.gates if g.name == "G8")
    assert g8.status is gates.GateStatus.PASS and g8.metrics["n_rows_emitted"] == 3


def test_build_section_correction_and_parse_log(tmp_path: Path) -> None:
    snap, stage_root = _prepared(tmp_path)
    for name, n in (("stg_doc_section", 8), ("stg_doc_correction", 1), ("stg_doc_parse_log", 4)):
        r = build.build_table(rules.RULES[name], snap, stage_root)
        assert r.ok and r.n_rows == n, (name, [g.as_dict() for g in r.gates])
    con = _read(stage_root, r)                # parse_log
    modes = dict(con.execute("SELECT parse_mode, count(*) FROM t GROUP BY 1").fetchall())
    assert modes == {"ok": 2, "html": 1, "failed": 1}


def test_build_without_prepass_cache_raises_with_instructions(tmp_path: Path) -> None:
    docs, db = _setup(tmp_path)
    _write_disclosure(db)
    snap = snapshot.make_snapshot({"dart": db}, tmp_path / "snapshots", snapshot_id="snap_x")
    stage_root = tmp_path / "stage"
    assert build.build_table(rules.RULES["stg_rcept_dt_map"], snap, stage_root).ok
    import pytest
    with pytest.raises(FileNotFoundError, match="doc_prepass"):
        build.build_table(rules.RULES["stg_doc_meta"], snap, stage_root)
```

- [ ] **Step 2: 실패 확인** — `KeyError: 'stg_doc_meta'`

- [ ] **Step 3a: `model.py` — `FileSource` + 필드**

`BlobSource` 클래스 바로 아래에 추가하고 `TableRule` 에 `file_source: FileSource | None = None` 필드를 `blob_source` 다음 줄에 넣는다:
```python
@dataclass(frozen=True)
class FileSource:
    """§1 예외 — 원장 파일(ZIP)을 파이썬 파서로 N행 언네스트. 파싱은 doc_prepass 가 미리 해
    `<stage_root>/_tmp/doc/<snapshot_id>/<table>/year=*.jsonl` 에 캐시하고, 빌더는 그 캐시를 읽는다.
    sources[0] 은 원장 메타(dart.doc_store) — ATTACH·G0 대상."""

    table: str                                   # 캐시 디렉토리 이름 = stage 테이블 이름
    columns: tuple[str, ...]                     # JSONL 행의 컬럼(전부 VARCHAR)
    required_columns: tuple[str, ...] = ("rcept_no", "zip_ok", "fetched_at")   # doc_store 실물 계약
```

- [ ] **Step 3b: `build.py` — `_load_file_source` (`_load_blob_source` 아래) + 분기**

```python
def _load_file_source(con: duckdb.DuckDBPyConnection, rule: TableRule, snap: Snapshot,
                      stage_root: Path) -> tuple[list[str], dict[str, object]]:
    """doc_prepass 캐시(JSONL) → `src_all`. 캐시가 없으면 프리패스를 먼저 돌리라는 예외."""
    fs = rule.file_source
    if fs is None:
        raise ValueError(f"_load_file_source called without file_source: {rule.name}")
    cache = stage_root / "_tmp" / "doc" / snap.snapshot_id
    summary_path = cache / "summary.json"
    if not summary_path.exists():
        raise FileNotFoundError(
            f"doc prepass cache not found — run `python -m stage.doc_prepass --snapshot-id "
            f"{snap.snapshot_id}` first: expected {summary_path}")
    summary = json.loads(summary_path.read_text(encoding="utf-8"))
    if summary.get("status") != "ok":
        raise RuntimeError(f"doc prepass gate_failed for snapshot={snap.snapshot_id}: "
                           f"{summary.get('detail')}")
    glob = str(cache / fs.table / "year=*.jsonl")
    schema = ", ".join(f"{_q(c)}: 'VARCHAR'" for c in fs.columns)
    con.execute(f"CREATE OR REPLACE TEMP TABLE src_all AS SELECT *, 'doc_zip' AS _src "
                f"FROM read_json('{glob}', format='newline_delimited', columns={{{schema}}}, "
                f"union_by_name=false)")
    emitted = int(summary["tables"].get(fs.table, 0))
    return list(fs.columns), {"n_rows_emitted": emitted, "n_parse_failed": 0,
                              "n_docs": summary.get("n_docs"), "modes": summary.get("modes")}
```
`build_table` 의 소스 적재 분기(현재 `if rule.blob_source is not None:`)를:
```python
        if rule.file_source is not None:
            src_cols, parse_metrics = _load_file_source(con, rule, snap, stage_root)
        elif rule.blob_source is not None:
            src_cols, parse_metrics = _load_blob_source(con, rule, tmp_root)
        else:
```
빈 캐시(연도 파일 0개)는 `read_json` 이 예외를 내므로, 캐시 디렉토리에 파일이 하나도 없으면 `FileNotFoundError` 로 같은 메시지를 낸다(`if not any((cache / fs.table).glob("year=*.jsonl"))`).

- [ ] **Step 3c: `gates.py` G0** — `if ctx.rule.blob_source is not None:` 앞에:
```python
        if ctx.rule.file_source is not None:
            need = set(ctx.rule.file_source.required_columns)   # doc_store 실물 계약
        elif ctx.rule.blob_source is not None:
```

- [ ] **Step 3d: `rules_doc.py`**

```python
"""문서층 stage 테이블 선언 — 원장 = 보고서 ZIP(doc_store 메타), 파서 = parsers_doc (DOC_DESIGN v1.1 §3).

값은 프리패스 JSONL(전부 VARCHAR) 에서 오고 캐스팅·결측·게이트는 stage 규칙이 한다. 공통 골격:
available = stg_rcept_dt_map 참조(derived) · observed = doc_store.fetched_at · 판본 1개(append_only).
"""
from __future__ import annotations

from .model import (
    KIND_BOOL,
    KIND_DATE_YMD8,
    KIND_NUMERIC,
    KIND_TEXT,
    AvailableRule,
    ColumnRule,
    FileSource,
    SourceRef,
    TableRule,
)

_DOC = SourceRef("dart", "doc_store", "doc_zip")
_RCEPT_LOOKUP = AvailableRule("lookup", table="stg_rcept_dt_map", local_key="rcept_no",
                              lookup_key="rcept_no", lookup_value="rcept_dt")
_CNT = (12, 0)


def _n(name: str) -> ColumnRule:
    return ColumnRule(name, name, KIND_NUMERIC, *_CNT)


def _t(name: str, key: bool = False, expected_len: int | None = None) -> ColumnRule:
    return ColumnRule(name, name, KIND_TEXT, key=key, expected_len=expected_len)


def _table(name: str, columns: tuple[ColumnRule, ...], natural_key: tuple[str, ...]) -> TableRule:
    src_cols = tuple(c.src for c in columns) + ("fetched_at",)
    return TableRule(
        name=name, sources=(_DOC,), columns=columns, natural_key=natural_key,
        partition_class="receipt_axis", partition_expr="substr(rcept_no, 1, 4)",
        partition_src="rcept_no", observed_src="fetched_at", write_mode="append_only", fanout=1,
        payload_exclude=("fetched_at",), lag_known=False, available=_RCEPT_LOOKUP,
        payload_columns=tuple(c.src for c in columns), key_unique=True,
        file_source=FileSource(name, src_cols),
    )


_COUNTERS = tuple(_n(c) for c in ("n_ctrl", "n_ent_other", "n_bare_amp", "n_bare_lt", "n_attr_repair"))

STG_DOC_META = _table("stg_doc_meta", (
    _t("rcept_no", key=True, expected_len=14), _t("member_name", key=True), _t("member_role"),
    _t("format"), _t("gen"), _t("xsd"), _t("formula_version"),
    ColumnRule("formula_date", "formula_date", KIND_DATE_YMD8),
    _t("doc_acode"), _t("doc_name"), _t("corp_cik"), _t("company_name_doc"), _t("byte_enc"),
    _n("n_repl"), *_COUNTERS, _t("parse_mode"), _n("n_elements"), _n("n_tables"),
    _n("n_form_groups"), _n("n_xbrl_groups"), _n("n_free_tables"), _n("toc_n"),
    ColumnRule("period_from", "period_from", KIND_DATE_YMD8),
    ColumnRule("period_to", "period_to", KIND_DATE_YMD8),
    ColumnRule("has_correction_page", "has_correction_page", KIND_BOOL),
    ColumnRule("text_equal", "text_equal", KIND_BOOL),
    _t("xbrl_aclass"), _t("form_aclass"), _t("summary"), _t("cover"), _t("other_entities"),
    _n("bytes_xml"),
), ("rcept_no", "member_name"))

STG_DOC_SECTION = _table("stg_doc_section", (
    _t("rcept_no", key=True, expected_len=14), _t("member_name", key=True),
    _n("ordinal"), _t("section_code"), _t("section_kind"), _t("atocid"), _n("level"), _t("title"),
    _t("path"), _n("elem_start"), _n("n_form_tables"), _n("n_xbrl_groups"), _n("n_free_tables"),
    _n("n_paragraphs"), _n("n_chars"),
), ("rcept_no", "member_name", "ordinal"))
# ordinal 은 키인데 KIND_NUMERIC 이라 key 플래그를 ColumnRule 로 다시 준다:
STG_DOC_SECTION = STG_DOC_SECTION.__class__(**{**STG_DOC_SECTION.__dict__, "columns": tuple(
    ColumnRule(c.src, c.name, c.kind, c.precision, c.scale, key=(c.name in ("rcept_no", "member_name", "ordinal")),
               expected_len=c.expected_len) for c in STG_DOC_SECTION.columns)})

STG_DOC_CORRECTION = _table("stg_doc_correction", (
    _t("rcept_no", key=True, expected_len=14), _t("member_name"),
    ColumnRule("page_found", "page_found", KIND_BOOL),
    _t("target_raw"), _t("filed_raw"), ColumnRule("filed_date", "filed_date", KIND_DATE_YMD8),
    _t("filed_date_status"), _t("reason_raw"), _n("n_items"), _t("items"), _n("corr_text_chars"),
), ("rcept_no",))

STG_DOC_PARSE_LOG = _table("stg_doc_parse_log", (
    _t("rcept_no", key=True, expected_len=14), _t("member_name", key=True), _t("member_role"),
    _t("byte_enc"), _n("n_repl"), _n("bytes_xml"), _t("parse_mode"), _t("error"), _t("error_ctx"),
    _t("unknown_tags"), _t("lenient_unknown_tags"), *_COUNTERS,
), ("rcept_no", "member_name"))

TABLES: tuple[TableRule, ...] = (STG_DOC_META, STG_DOC_SECTION, STG_DOC_CORRECTION,
                                 STG_DOC_PARSE_LOG)
```
`STG_DOC_SECTION` 의 재생성 트릭 대신 `_n("ordinal", key=True)` 를 지원하도록 `_n(name, key=False)` 시그니처로 바꾸는 편이 깔끔하다 — 구현 시 그렇게 하고 위 재생성 3줄은 지운다. `stg_doc_parse_log` 의 깨진 ZIP 행은 `member_name=""` 인데 키의 `''` 는 `key_missing` reject 가 되므로 `ColumnRule(..., blank_is_value=True)` 를 `member_name` 에 준다(parse_log 만).

- [ ] **Step 3e: `rules.py`** — `from . import rules_dart, rules_dart_events, rules_doc, …` 와 `_MODULES` 에 `rules_doc` 추가.

- [ ] **Step 4: 통과 확인 + 전체 회귀**

Run: `cd workspace/dongmin && ../../.venv/bin/python -m pytest tests -q && ../../.venv/bin/ruff check src/stage tests && ../../.venv/bin/pyright src/stage/model.py src/stage/build.py src/stage/gates.py src/stage/rules_doc.py src/stage/rules.py tests/test_stage_doc_build.py`
Expected: 기존 194 + 새 테스트 전부 통과, 린트 0. 기존 `rules` 선언 대조 테스트(`test_stage_dart.py` 등)가 테이블 수를 세면 4 늘어난 값으로 갱신한다.

- [ ] **Step 5: 커밋**

```bash
git add workspace/dongmin/src/stage workspace/dongmin/tests/test_stage_doc_build.py
git commit -m "feat(doc-stage): FileSource cache loader, four stg_doc_* table rules, G0 support"
```

---

### Task 12: 빌드 후 기록형 검사 `doc_checks` (D6 · D8 · D12) + 골든 픽스처 생성 스크립트

**Files:**
- Create: `src/stage/doc_checks.py`
- Create: `scripts/doc_fixtures_from_sample.py` (S26 → `src/stage/fixtures/stg_doc_meta.json`)
- Test: `tests/test_stage_doc_checks.py`

- [ ] **Step 1: 실패하는 테스트**

```python
"""doc_checks — 빌드 후 기록형 검사 D6·D8·D12 (DOC_DESIGN v1.1 §4)."""
from pathlib import Path

from stage import build, doc_checks, rules

from test_stage_doc_build import _prepared


def test_checks_report_correction_page_rule_xbrl_share_and_vocab_coverage(tmp_path: Path) -> None:
    snap, stage_root = _prepared(tmp_path)
    for name in ("stg_doc_meta", "stg_doc_correction", "stg_doc_parse_log"):
        assert build.build_table(rules.RULES[name], snap, stage_root).ok
    rep = doc_checks.run(stage_root)
    assert rep["d6"]["n_corrected_docs_with_zip"] == 0        # 표본엔 [기재정정] 문서가 없다
    assert rep["d8"]["by_year"]["2020"] == {"n_main_annual": 1, "n_xbrl_ge4": 0}
    assert rep["d12"]["n_docs"] == 3 and rep["d12"]["unknown_tag_docs"] == 0
    assert rep["status"] == "ok"
```

- [ ] **Step 2: 실패 확인** — `ModuleNotFoundError: ... 'stage.doc_checks'`

- [ ] **Step 3: 구현**

`src/stage/doc_checks.py`:
```python
"""빌드 후 문서 검사 — 테이블을 가로지르는 기록형 게이트 D6·D8·D12 (DOC_DESIGN v1.1 §4).

stage 게이트는 테이블 하나만 보므로 여기서 parquet 를 읽어 보고서를 만든다. 폐기형 항(D6 의
"[기재정정]·zip_ok·파싱 ok 인데 첫 장 없음")은 status=gate_failed 로 올린다.
CLI: PYTHONPATH=src python -m stage.doc_checks [--stage-root data/stage]
"""
from __future__ import annotations

import argparse
import json
import os
import sys
from pathlib import Path

import duckdb

from . import manifest


def _glob(stage_root: Path, table: str) -> str:
    m = manifest.load(stage_root / table / "MANIFEST.json")
    if m.current_build is None:
        raise FileNotFoundError(f"no committed build for {table} under {stage_root}")
    return str(stage_root / table / f"v={m.current_build}" / "**" / "*.parquet")


def run(stage_root: Path, doc_name_prefix_corrected: tuple[str, ...] = ("[기재정정]",)) -> dict[str, object]:
    con = duckdb.connect()
    for t in ("stg_doc_meta", "stg_doc_correction", "stg_doc_parse_log"):
        con.execute(f"CREATE VIEW {t} AS SELECT * FROM read_parquet('{_glob(stage_root, t)}', "
                    f"hive_partitioning=true)")
    # D6: 정정 문서(doc_name 접두로 판정 불가 — 접두는 report_nm 에만 있다) → main 멤버에 CORRECTION 이
    # 있어야 하는 집합은 "has_correction_page=true 인 문서" 의 보집합으로 잡을 수 없으므로,
    # 여기서는 zip 있는 main·ok 문서 중 첫 장이 있는 비율과 filed_date 해석률만 기록하고,
    # 접두 기반 폐기형 판정은 equity 가 stg_disclosure 를 붙여 수행한다(§8.1 E-G6a).
    d6 = con.execute("""
        SELECT count(*) FILTER (WHERE m.has_correction_page) AS n_with_page,
               count(*) FILTER (WHERE c.filed_date_status = 'parsed') AS n_filed_parsed,
               count(*) FILTER (WHERE c.rcept_no IS NOT NULL) AS n_corr_rows
        FROM stg_doc_meta m LEFT JOIN stg_doc_correction c ON c.rcept_no = m.rcept_no
        WHERE m.member_role = 'main' AND m.parse_mode IN ('ok', 'lenient')""").fetchone()
    d8_rows = con.execute("""
        SELECT year, count(*) AS n_main_annual, count(*) FILTER (WHERE n_xbrl_groups >= 4) AS n_xbrl_ge4
        FROM stg_doc_meta WHERE member_role = 'main' AND doc_acode = '11011' GROUP BY 1 ORDER BY 1
        """).fetchall()
    d12 = con.execute("""
        SELECT count(*) AS n_docs,
               count(*) FILTER (WHERE unknown_tags <> '[]') AS unknown_tag_docs,
               count(*) FILTER (WHERE parse_mode = 'lenient') AS lenient_docs
        FROM stg_doc_parse_log WHERE parse_mode IN ('ok', 'lenient')""").fetchone()
    con.close()
    if d6 is None or d12 is None:
        raise RuntimeError(f"doc_checks query returned no row: stage_root={stage_root}")
    report: dict[str, object] = {
        "d6": {"n_with_page": int(d6[0]), "n_filed_parsed": int(d6[1]), "n_corr_rows": int(d6[2]),
               "n_corrected_docs_with_zip": int(d6[2])},
        "d8": {"by_year": {str(y): {"n_main_annual": int(n), "n_xbrl_ge4": int(x)} for y, n, x in d8_rows}},
        "d12": {"n_docs": int(d12[0]), "unknown_tag_docs": int(d12[1]), "lenient_docs": int(d12[2])},
        "status": "ok",
    }
    return report


def main(argv: list[str] | None = None) -> int:
    base = Path(os.environ.get("QL_HOME") or Path(__file__).resolve().parents[2])
    ap = argparse.ArgumentParser(description=__doc__)
    ap.add_argument("--stage-root", type=Path, default=base / "data" / "stage")
    ap.add_argument("--out", type=Path)
    a = ap.parse_args(argv)
    rep = run(a.stage_root)
    text = json.dumps(rep, ensure_ascii=False, indent=1)
    if a.out:
        a.out.write_text(text, encoding="utf-8")
    print(text)
    return 0 if rep["status"] == "ok" else 1


if __name__ == "__main__":
    sys.exit(main())
```
설계 §4 D6 의 폐기형 항("`[기재정정]` 인데 첫 장 없음")은 접두가 `report_nm`(원장 공시 목록)에만 있어 stage 문서 표만으로는 판정할 수 없다 — 구현 시 이 사실을 DOC_DESIGN §4 D6 에 반영해 "폐기형 항은 equity E-G6a 로 이관, stage 는 기록형" 으로 고친다(Task 13 문서 갱신에 포함).

`scripts/doc_fixtures_from_sample.py` — 서버에서 S26 26건만 프리패스한 캐시(`--rcept-list`)로 G4 픽스처를 만든다. 프리패스 CLI 에 `--rcept-list FILE`(접수번호 한 줄씩) 옵션을 Task 10 의 `run()` 에 `rcept_list: set[str] | None` 로 추가하고, 이 스크립트는 캐시 `stg_doc_meta/year=*.jsonl` 을 읽어 `main` 멤버 행에서 `(rcept_no, member_name)` 키로 `gen`·`byte_enc`·`doc_acode`·`formula_version`·`toc_n`·`has_correction_page`·`n_xbrl_groups`·`period_from`·`period_to` 를 `expect` 로 뽑아 `src/stage/fixtures/stg_doc_meta.json` 에 쓴다(`measured_sql` 대신 `measured_by: "doc_prepass --rcept-list S26"`, `measured_at`). 사람이 DOC_DESIGN §1.1 표(toc_n·XBRL 그룹 수·인코딩·세대)와 대조한 뒤 커밋한다 — 표와 다르면 파서 결함이므로 픽스처를 고치지 말고 파서를 고친다.

- [ ] **Step 4: 통과 확인 + 린트** — `1 passed`, 린트 0
- [ ] **Step 5: 커밋** — `git commit -m "feat(doc-stage): post-build document checks D6/D8/D12 and fixture generator"`

---

### Task 13: 서버 실행 — 스냅샷 · 프리패스 · 4테이블 빌드 · 검사 · 픽스처 · baseline · PR

**Files:**
- Modify: `docs/DOC_DESIGN.md` §4 D6 문구, §7 실측 기록
- Create: `src/stage/fixtures/stg_doc_meta.json`
- Modify: `data/stage/baseline.json`(서버) — `stg_doc_*` 항목

- [ ] **Step 1: 배포** — 로컬 전체 테스트·린트 통과 확인 뒤 `rsync -avz --exclude='.venv' --exclude='__pycache__' workspace/dongmin/src/ kael-server:~/quant-ledger/src/` 와 `scripts/`.
- [ ] **Step 2: 표본 픽스처** — 서버에서 S26 접수번호 26개를 파일로 만들고
  `cd ~/quant-ledger && .venv/bin/python -m stage.doc_prepass --snapshot-id <최신 dart 스냅샷> --workers 1 --rcept-list /tmp/s26.txt` (스냅샷은 `python -m stage --table stg_rcept_dt_map` 이 만든 것을 재사용하거나 새로 VACUUM). 캐시로 `scripts/doc_fixtures_from_sample.py` 실행 → 픽스처 JSON 을 로컬로 가져와 DOC_DESIGN §1.1 표와 26행 대조(gen·byte_enc·ACODE·FV·toc_n·XBRL 그룹 수) → 커밋.
- [ ] **Step 3: 전량 프리패스** — `.venv/bin/python -m stage.doc_prepass --snapshot-id <S> --workers 3` (예상 1.2h). `summary.json` 의 D0·D2·D3·D10 이 ok 인지, `unknown_tags_top` 을 읽어 태그 사전에 넣을 것이 있는지 판단(있으면 `tags.json` 갱신 → 재실행).
- [ ] **Step 4: 4테이블 빌드** — `python -m stage --table stg_doc_meta --snapshot-id <S>` 순으로 4개. G0~G9 결과와 시간·RSS 기록. G4 는 픽스처 26건 전부 일치해야 한다.
- [ ] **Step 5: 검사·baseline** — `python -m stage.doc_checks --out data/stage/doc_checks.json`; D8 연도별 XBRL 존재율과 D12 어휘 커버리지를 `baseline.json` 의 `stg_doc_*` 항목으로 기록(`python -m stage.baseline` 규약 따름). 재빌드 1회로 G5 Δ=0 확인.
- [ ] **Step 6: 문서·PR** — DOC_DESIGN §7 에 실측(문서 수·모드 분포·시간·게이트)을 적고 §4 D6 문구를 고친다. PR 본문은 `.claude/rules/pr-review.md` 양식. 캐시 `_tmp/doc/<S>` 는 P2 가 같은 스냅샷을 쓰므로 P2 완료 전까지 지우지 않는다(디스크 여유 확인).

---

## 후속 (별도 계획)
- **P2** `stg_fin_asreported`: 격자 전개(ROWSPAN/COLSPAN), 헤더 표 기간 라벨·단위, 본문 표 값·`period_slot`·`vintage_kind`, 연도 샤드 빌드(빌더 변경 ③), D9·D11·D13. 계획 문서 `2026-09-xx-doc-stage-p2.md`.
- **P3** `stg_doc_form_cell`·`stg_doc_table`, 양식·셀 코드 사전(`form_aclass.json`·`acode.json`) census.
