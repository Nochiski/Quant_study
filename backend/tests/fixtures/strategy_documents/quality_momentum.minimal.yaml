# schema 1.2 생략형 fixture — 기본값과 같은 섹션·필드를 모두 생략해도 verbose 문서와 같은 spec_hash를 낸다.
schema_version: "1.2"
title: "퀄리티 모멘텀"
factors:
  - factor_id: momentum
    label: "모멘텀"
    direction: high
    weight: 0.6
    graph:
      nodes:
        - kind: field
          node_id: close
          field_id: price.close
        - kind: time_series
          node_id: mom_252
          operator: momentum
          input_node_id: close
          window: 252
      output_node_id: mom_252
portfolio:
  selection_count: 20
risk:
  max_name_weight: 0.05
