# P0-01 golden authoring fixture — WORKFLOW 2.2의 완전한 verbose YAML 예시.
# identity(strategy_id/revision)는 문서 밖 revision envelope가 소유한다.
schema_version: "1.0"
title: "퀄리티 모멘텀"
description: ""
data:
  market: KRX
  start: "2021-01-01"
  end: "2026-08-31"
  universe_id: krx.common-stock
  frequency: daily
eligibility:
  rules: []
factors:
  factors:
    - factor_id: momentum
      label: "모멘텀"
      direction: high
      weight: 0.6
      graph:
        nodes:
          - node_id: close
            field_id: price.close
            kind: field
          - node_id: mom_252
            operator: momentum
            input_node_id: close
            window: 252
            kind: time_series
        output_node_id: mom_252
        missing_policy: drop
signal:
  method: weighted_sum
portfolio:
  selection_count: 20
  rebalance: monthly
risk:
  max_name_weight: 0.05
execution:
  timing: next_open
  fee_bps: 15.0
parameters: []
